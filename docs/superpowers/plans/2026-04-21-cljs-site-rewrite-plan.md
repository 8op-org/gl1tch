# CLJS Site Rewrite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the Astro static site with a shadow-cljs + Reagent SPA, making the entire site toolchain Clojure-native with REPL access to the running app.

**Architecture:** Single-page app compiled by shadow-cljs, rendered by Reagent, styled by Garden, routed by Reitit with hash-based URLs. Content is hiccup EDN generated by modular glitch workflows. SEO via headless Chrome prerendering.

**Tech Stack:** shadow-cljs, Reagent, Reitit, Garden, Highlight.js, ClojureScript

**Spec:** `docs/superpowers/specs/2026-04-21-cljs-site-rewrite-design.md`

---

## File Map

### New Files (site/)

| File | Responsibility |
|------|---------------|
| `shadow-cljs.edn` | Build config, dev server, nREPL port, npm deps |
| `deps.edn` | Clojure deps (reagent, reitit, garden) |
| `package.json` | shadow-cljs + highlight.js npm deps |
| `resources/public/index.html` | Shell HTML that mounts the app |
| `resources/public/CNAME` | 8op.org (copied from existing `site/public/CNAME`) |
| `src/gl1tch/site/core.cljs` | Entry point, mount Reagent root |
| `src/gl1tch/site/router.cljs` | Reitit browser router, route definitions |
| `src/gl1tch/site/state.cljs` | Reagent atoms (current route, content index) |
| `src/gl1tch/site/content.cljs` | Load and index EDN content files |
| `src/gl1tch/site/content.clj` | Compile-time macro to read EDN files |
| `src/gl1tch/site/styles/theme.cljs` | Color/font/spacing tokens |
| `src/gl1tch/site/styles/global.cljs` | Garden CSS compilation + injection |
| `src/gl1tch/site/components/nav.cljs` | Top navigation bar |
| `src/gl1tch/site/components/sidebar.cljs` | Doc sidebar (derived from content index) |
| `src/gl1tch/site/components/toc.cljs` | In-page table of contents |
| `src/gl1tch/site/components/code.cljs` | Highlight.js code blocks |
| `src/gl1tch/site/components/hex_rain.cljs` | Canvas hex rain effect |
| `src/gl1tch/site/diagrams/svg.cljs` | SVG primitives (box, arrow, label) |
| `src/gl1tch/site/diagrams/core.cljs` | Layout engine (topological sort, positioning) |
| `src/gl1tch/site/diagrams/flowchart.cljs` | Flowchart renderer |
| `src/gl1tch/site/pages/home.cljs` | Landing page |
| `src/gl1tch/site/pages/docs.cljs` | Doc index + individual doc page |
| `src/gl1tch/site/pages/labs.cljs` | Lab index + individual lab page |
| `src/gl1tch/site/pages/changelog.cljs` | Changelog page |
| `test/gl1tch/site/diagrams/core_test.cljs` | Layout engine tests |
| `test/gl1tch/site/diagrams/flowchart_test.cljs` | Flowchart rendering tests |
| `test/gl1tch/site/content_test.cljs` | Content loading/indexing tests |
| `test/gl1tch/site/router_test.cljs` | Route matching tests |
| `content/docs/getting-started.edn` | Sample doc (seed content) |
| `content/labs/repl-driven-docs.edn` | Sample lab (seed content) |
| `content/changelog/2026-04-21.edn` | Sample changelog (seed content) |

### Modified Files

| File | Change |
|------|--------|
| `bb.edn` | Update `site:dev` and `site:build` tasks, add `site:repl` |
| `.github/workflows/pages.yml` | Replace astro build with shadow-cljs release |
| `site/conventions.md` | Add EDN/hiccup examples for LLM prompts |

### Deleted Files (after migration complete)

| File | Reason |
|------|--------|
| `site/astro.config.mjs` | Replaced by shadow-cljs.edn |
| `site/src/content.config.ts` | Replaced by EDN schemas in content.cljs |
| `site/src/pages/*.astro` | Replaced by CLJS page components |
| `site/src/layouts/*.astro` | Replaced by CLJS layout in core.cljs |
| `site/src/components/*.astro` | Replaced by CLJS components |
| `site/src/styles/global.css` | Replaced by Garden styles |
| `site/src/content/` | Replaced by content/ EDN directory |

### New Workflow Files

| File | Responsibility |
|------|---------------|
| `workflows/site/generate-doc.glitch` | Generate single doc EDN from slug |
| `workflows/site/generate-lab.glitch` | Generate single lab EDN from slug |
| `workflows/site/generate-changelog.glitch` | Generate changelog EDN from date range |
| `workflows/site/gate-syntax.glitch` | Validate EDN readability |
| `workflows/site/gate-structure.glitch` | Schema validation on EDN content |
| `workflows/site/gate-links.glitch` | Walk hiccup for link targets, verify routes |
| `workflows/site/gate-hallucinations.glitch` | Grounding check against source code |
| `workflows/site/sync.glitch` | Orchestrator: generate all + run all gates |
| `workflows/site/deploy.glitch` | Build + commit + push |

---

## Task 1: Scaffold shadow-cljs Project

**Files:**
- Create: `site/shadow-cljs.edn`
- Create: `site/deps.edn`
- Create: `site/package.json`
- Create: `site/resources/public/index.html`
- Copy: `site/public/CNAME` -> `site/resources/public/CNAME`

- [ ] **Step 1: Create `site/package.json`**

```json
{
  "name": "gl1tch-site",
  "private": true,
  "scripts": {
    "dev": "shadow-cljs watch app",
    "release": "shadow-cljs release app",
    "test": "shadow-cljs compile test"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.0"
  },
  "dependencies": {
    "highlight.js": "^11.11.0"
  }
}
```

- [ ] **Step 2: Create `site/deps.edn`**

```clojure
{:paths ["src" "test" "resources"]
 :deps {reagent/reagent {:mvn/version "1.2.0"}
        metosin/reitit {:mvn/version "0.7.2"}
        garden/garden {:mvn/version "1.3.10"}}}
```

- [ ] **Step 3: Create `site/shadow-cljs.edn`**

```clojure
{:deps true
 :nrepl {:port 7888}
 :dev-http {3000 "resources/public"}
 :builds
 {:app
  {:target :browser
   :output-dir "resources/public/js"
   :asset-path "/js"
   :modules {:main {:init-fn gl1tch.site.core/init}}
   :devtools {:after-load gl1tch.site.core/reload}}
  :test
  {:target :node-test
   :output-to "out/test.js"
   :autorun true}}}
```

- [ ] **Step 4: Create `site/resources/public/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>gl1tch</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <div id="app"></div>
  <script src="/js/main.js"></script>
</body>
</html>
```

- [ ] **Step 5: Copy CNAME**

```bash
cp site/public/CNAME site/resources/public/CNAME
```

- [ ] **Step 6: Install dependencies**

```bash
cd site && npm install
```

Expected: `node_modules/` created, `package-lock.json` generated.

- [ ] **Step 7: Commit**

```bash
git add site/shadow-cljs.edn site/deps.edn site/package.json site/resources/
git commit -m "feat(site): scaffold shadow-cljs + Reagent project"
```

---

## Task 2: Theme + Garden Styles

**Files:**
- Create: `site/src/gl1tch/site/styles/theme.cljs`
- Create: `site/src/gl1tch/site/styles/global.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/styles/theme.cljs`**

```clojure
(ns gl1tch.site.styles.theme)

(def colors
  {:bg       "#0a0a0f"
   :bg-alt   "#12121a"
   :bg-card  "#161722"
   :fg       "#c0c0c8"
   :fg-dim   "#606070"
   :accent   "#00ff9f"
   :accent-2 "#7b61ff"
   :danger   "#ff4040"
   :border   "#1a1a2e"
   :glow     "rgba(0, 255, 159, 0.08)"})

(def fonts
  {:mono "'JetBrains Mono', 'Fira Code', monospace"})

(def sizes
  {:base   "15px"
   :sm     "0.82rem"
   :xs     "0.72rem"
   :xxs    "0.65rem"
   :h1     "clamp(3rem, 8vw, 5rem)"
   :h2     "2rem"
   :h3     "1.05rem"})

(def spacing
  {:xs 4 :sm 8 :md 16 :lg 24 :xl 40})

(def radius "3px")
```

- [ ] **Step 2: Create `site/src/gl1tch/site/styles/global.cljs`**

```clojure
(ns gl1tch.site.styles.global
  (:require [garden.core :as garden]
            [garden.stylesheet :refer [at-keyframes]]
            [gl1tch.site.styles.theme :as t]))

(def reset
  [[:* :*::before :*::after
    {:margin 0 :padding 0 :box-sizing :border-box}]])

(def base
  [[:html {:scroll-behavior :smooth}]
   [:body
    {:font-family (t/fonts :mono)
     :background (t/colors :bg)
     :color (t/colors :fg)
     :line-height 1.7
     :overflow-x :hidden
     :-webkit-font-smoothing :antialiased
     :font-size (t/sizes :base)}]
   [:a {:color (t/colors :accent)
        :text-decoration :none}]
   [:a:hover {:text-decoration :underline}]])

(def code-block
  [[:pre.code
    {:background "#1a1b2e"
     :border [["1px" :solid "rgba(0, 255, 159, 0.1)"]]
     :border-radius "6px"
     :padding "20px 24px"
     :font-family (t/fonts :mono)
     :font-size (t/sizes :sm)
     :line-height 1.9
     :overflow-x :auto
     :margin-bottom "24px"
     :white-space :pre}]
   [:code
    {:font-family (t/fonts :mono)
     :font-size "0.88em"
     :color (t/colors :accent)
     :background "rgba(0, 255, 159, 0.06)"
     :padding "2px 6px"
     :border-radius t/radius}]
   ["pre.code > code"
    {:background :none :padding 0 :color :inherit}]])

(def glitch-animation
  [(at-keyframes :glitch-1
     [:0% {:clip-path "inset(40% 0 61% 0)" :transform "translate(-2px, -1px)"}]
     [:20% {:clip-path "inset(92% 0 1% 0)" :transform "translate(1px, 2px)"}]
     [:40% {:clip-path "inset(43% 0 1% 0)" :transform "translate(-1px, 1px)"}]
     [:60% {:clip-path "inset(25% 0 58% 0)" :transform "translate(2px, -1px)"}]
     [:80% {:clip-path "inset(54% 0 7% 0)" :transform "translate(-1px, 2px)"}]
     [:100% {:clip-path "inset(58% 0 43% 0)" :transform "translate(0px, -2px)"}])
   (at-keyframes :glitch-2
     [:0% {:clip-path "inset(65% 0 13% 0)" :transform "translate(2px, 1px)"}]
     [:20% {:clip-path "inset(15% 0 62% 0)" :transform "translate(-2px, -1px)"}]
     [:40% {:clip-path "inset(79% 0 2% 0)" :transform "translate(1px, 1px)"}]
     [:60% {:clip-path "inset(2% 0 78% 0)" :transform "translate(-1px, 2px)"}]
     [:80% {:clip-path "inset(40% 0 34% 0)" :transform "translate(2px, -1px)"}]
     [:100% {:clip-path "inset(67% 0 5% 0)" :transform "translate(-2px, 0px)"}])])

(def nav-styles
  [[:.site-nav
    {:position :fixed
     :top 0 :left 0 :right 0
     :z-index 100
     :background "rgba(10, 10, 15, 0.85)"
     :backdrop-filter "blur(12px)"
     :-webkit-backdrop-filter "blur(12px)"
     :border-bottom [["1px" :solid (t/colors :border)]]}]])

(def layout
  [[:.wrap {:max-width "900px" :margin "0 auto" :padding "0 40px"
            :position :relative :z-index 1}]
   [:.wrap-wide {:max-width "1120px" :margin "0 auto" :padding "0 40px"
                 :position :relative :z-index 1}]
   [:section {:padding "120px 0" :position :relative}]])

(defn stylesheet []
  (garden/css
    (concat reset base code-block glitch-animation nav-styles layout)))

(defn inject! []
  (let [style-el (or (.getElementById js/document "gl1tch-styles")
                     (let [el (.createElement js/document "style")]
                       (set! (.-id el) "gl1tch-styles")
                       (.appendChild (.-head js/document) el)
                       el))]
    (set! (.-textContent style-el) (stylesheet))))
```

- [ ] **Step 3: Commit**

```bash
git add site/src/gl1tch/site/styles/
git commit -m "feat(site): Garden theme tokens and global stylesheet"
```

---

## Task 3: Router + App Shell

**Files:**
- Create: `site/src/gl1tch/site/state.cljs`
- Create: `site/src/gl1tch/site/router.cljs`
- Create: `site/src/gl1tch/site/core.cljs`
- Create: `site/test/gl1tch/site/router_test.cljs`

- [ ] **Step 1: Write router test**

```clojure
(ns gl1tch.site.router-test
  (:require [cljs.test :refer-macros [deftest is testing]]
            [gl1tch.site.router :as router]))

(deftest route-matching
  (testing "matches root"
    (is (= :home (:name (router/match "/")))))
  (testing "matches doc slug"
    (let [m (router/match "/docs/getting-started")]
      (is (= :docs/page (:name m)))
      (is (= "getting-started" (get-in m [:params :slug])))))
  (testing "matches labs index"
    (is (= :labs/index (:name (router/match "/labs")))))
  (testing "matches lab slug"
    (let [m (router/match "/labs/repl-driven-docs")]
      (is (= :labs/page (:name m)))
      (is (= "repl-driven-docs" (get-in m [:params :slug])))))
  (testing "matches changelog"
    (is (= :changelog (:name (router/match "/changelog"))))))
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd site && npx shadow-cljs compile test
```

Expected: FAIL — `gl1tch.site.router` namespace not found.

- [ ] **Step 3: Create `site/src/gl1tch/site/state.cljs`**

```clojure
(ns gl1tch.site.state
  (:require [reagent.core :as r]))

(defonce current-match (r/atom nil))
```

- [ ] **Step 4: Create `site/src/gl1tch/site/router.cljs`**

```clojure
(ns gl1tch.site.router
  (:require [reitit.frontend :as rf]
            [reitit.frontend.easy :as rfe]
            [gl1tch.site.state :as state]))

(def routes
  [["/" {:name :home}]
   ["/docs" {:name :docs/index}]
   ["/docs/:slug" {:name :docs/page}]
   ["/labs" {:name :labs/index}]
   ["/labs/:slug" {:name :labs/page}]
   ["/changelog" {:name :changelog}]])

(def router (rf/router routes))

(defn match [path]
  (let [m (rf/match-by-path router path)]
    (when m
      {:name (get-in m [:data :name])
       :params (:path-params m)})))

(defn start! []
  (rfe/start!
    router
    (fn [match _]
      (reset! state/current-match match))
    {:use-fragment true}))
```

- [ ] **Step 5: Create `site/src/gl1tch/site/core.cljs`**

```clojure
(ns gl1tch.site.core
  (:require [reagent.dom :as rdom]
            [gl1tch.site.router :as router]
            [gl1tch.site.state :as state]
            [gl1tch.site.styles.global :as styles]))

(defn current-page []
  (let [match @state/current-match
        name (get-in match [:data :name])]
    [:div.app
     [:main
      (case name
        :home [:div.wrap [:h1 "gl1tch"]]
        :docs/index [:div.wrap [:h1 "Docs"]]
        :docs/page [:div.wrap [:h1 (str "Doc: " (get-in match [:path-params :slug]))]]
        :labs/index [:div.wrap [:h1 "Labs"]]
        :labs/page [:div.wrap [:h1 (str "Lab: " (get-in match [:path-params :slug]))]]
        :changelog [:div.wrap [:h1 "Changelog"]]
        [:div.wrap [:h1 "404"]])]]))

(defn ^:export init []
  (styles/inject!)
  (router/start!)
  (rdom/render [current-page] (.getElementById js/document "app")))

(defn ^:dev/after-load reload []
  (styles/inject!)
  (rdom/render [current-page] (.getElementById js/document "app")))
```

- [ ] **Step 6: Run tests**

```bash
cd site && npx shadow-cljs compile test
```

Expected: All 5 route tests pass.

- [ ] **Step 7: Verify dev server starts**

```bash
cd site && npx shadow-cljs watch app &
sleep 5 && curl -s http://localhost:3000 | head -20
```

Expected: HTML with `<div id="app">` returned. Kill the dev server after.

- [ ] **Step 8: Commit**

```bash
git add site/src/gl1tch/site/state.cljs site/src/gl1tch/site/router.cljs site/src/gl1tch/site/core.cljs site/test/
git commit -m "feat(site): Reitit router, app shell, route tests"
```

---

## Task 4: Nav Component

**Files:**
- Create: `site/src/gl1tch/site/components/nav.cljs`
- Modify: `site/src/gl1tch/site/core.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/components/nav.cljs`**

```clojure
(ns gl1tch.site.components.nav
  (:require [reitit.frontend.easy :as rfe]
            [gl1tch.site.styles.theme :as t]))

(defn nav-link [route-name label]
  [:a {:href (rfe/href route-name)
       :style {:font-size (t/sizes :xs)
               :color (t/colors :fg-dim)
               :text-decoration :none
               :letter-spacing "0.04em"}}
   label])

(defn nav []
  [:nav.site-nav
   [:div {:style {:max-width "1120px"
                  :margin "0 auto"
                  :padding "0 40px"
                  :height "52px"
                  :display :flex
                  :align-items :center
                  :justify-content :space-between}}
    [:a {:href (rfe/href :home)
         :style {:font-size "0.9rem"
                 :font-weight 700
                 :color (t/colors :fg)
                 :text-decoration :none
                 :letter-spacing "-0.02em"}}
     "gl1tch"]
    [:div {:style {:display :flex :gap "24px"}}
     [nav-link :docs/index "Docs"]
     [nav-link :labs/index "Labs"]
     [nav-link :changelog "Changelog"]
     [:a {:href "https://github.com/8op-org/gl1tch"
          :style {:font-size (t/sizes :xs)
                  :color (t/colors :fg-dim)
                  :text-decoration :none
                  :letter-spacing "0.04em"}}
      "GitHub"]]]])
```

- [ ] **Step 2: Wire nav into core.cljs**

Update `site/src/gl1tch/site/core.cljs` — full file:

```clojure
(ns gl1tch.site.core
  (:require [reagent.dom :as rdom]
            [gl1tch.site.router :as router]
            [gl1tch.site.state :as state]
            [gl1tch.site.styles.global :as styles]
            [gl1tch.site.components.nav :as nav]))

(defn current-page []
  (let [match @state/current-match
        name (get-in match [:data :name])]
    [:div.app
     [nav/nav]
     [:main {:style {:padding-top "52px"}}
      (case name
        :home [:div.wrap [:h1 "gl1tch"]]
        :docs/index [:div.wrap [:h1 "Docs"]]
        :docs/page [:div.wrap [:h1 (str "Doc: " (get-in match [:path-params :slug]))]]
        :labs/index [:div.wrap [:h1 "Labs"]]
        :labs/page [:div.wrap [:h1 (str "Lab: " (get-in match [:path-params :slug]))]]
        :changelog [:div.wrap [:h1 "Changelog"]]
        [:div.wrap [:h1 "404"]])]]))

(defn ^:export init []
  (styles/inject!)
  (router/start!)
  (rdom/render [current-page] (.getElementById js/document "app")))

(defn ^:dev/after-load reload []
  (styles/inject!)
  (rdom/render [current-page] (.getElementById js/document "app")))
```

- [ ] **Step 3: Commit**

```bash
git add site/src/gl1tch/site/components/nav.cljs site/src/gl1tch/site/core.cljs
git commit -m "feat(site): top navigation component"
```

---

## Task 5: Code Block Component (Highlight.js)

**Files:**
- Create: `site/src/gl1tch/site/components/code.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/components/code.cljs`**

Highlight.js loaded via npm, shadow-cljs handles interop. The `dangerouslySetInnerHTML` is safe here because content comes exclusively from Highlight.js processing our own EDN content — never from user input.

```clojure
(ns gl1tch.site.components.code
  (:require ["highlight.js/lib/core" :as hljs]
            ["highlight.js/lib/languages/bash" :as bash]
            ["highlight.js/lib/languages/clojure" :as clojure-lang]
            ["highlight.js/lib/languages/json" :as json-lang]
            ["highlight.js/lib/languages/yaml" :as yaml-lang]))

;; Register languages once
(defonce _register
  (do
    (.registerLanguage hljs "bash" bash)
    (.registerLanguage hljs "clojure" clojure-lang)
    (.registerLanguage hljs "json" json-lang)
    (.registerLanguage hljs "yaml" yaml-lang)
    true))

(defn highlight [code lang]
  (if (and lang (.getLanguage hljs lang))
    (.-value (.highlight hljs code #js {:language lang}))
    code))

(defn code-block
  "Renders a syntax-highlighted code block.
   Usage in hiccup EDN: [:code {:lang \"bash\"} \"echo hello\"]"
  [{:keys [lang]} code]
  (let [highlighted (highlight code (or lang "plaintext"))]
    [:pre.code
     [:code {:dangerouslySetInnerHTML {:__html highlighted}}]]))

(defn inline-code
  "Renders inline code. Usage: [:code \"some-thing\"]"
  [text]
  [:code text])
```

- [ ] **Step 2: Commit**

```bash
git add site/src/gl1tch/site/components/code.cljs
git commit -m "feat(site): Highlight.js code block component"
```

---

## Task 6: Diagram DSL — SVG Primitives

**Files:**
- Create: `site/src/gl1tch/site/diagrams/svg.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/diagrams/svg.cljs`**

```clojure
(ns gl1tch.site.diagrams.svg
  (:require [gl1tch.site.styles.theme :as t]))

(defn box [{:keys [x y w h label style]}]
  (let [fill (or (:fill style) (t/colors :bg-alt))
        stroke (or (:stroke style) (t/colors :accent))
        text-fill (or (:color style) (t/colors :fg))]
    [:g
     [:rect {:x x :y y :width w :height h
             :rx 4 :ry 4
             :fill fill
             :stroke stroke
             :stroke-width 1.5}]
     [:text {:x (+ x (/ w 2))
             :y (+ y (/ h 2) 5)
             :text-anchor "middle"
             :fill text-fill
             :font-family (t/fonts :mono)
             :font-size 12}
      label]]))

(defn arrow [{:keys [path style]}]
  (let [stroke (or (:stroke style) (t/colors :fg-dim))
        points (apply str (interpose " " (map (fn [[px py]] (str px "," py)) path)))]
    [:polyline {:points points
                :fill "none"
                :stroke stroke
                :stroke-width 1.5
                :marker-end "url(#arrowhead)"}]))

(defn arrowhead-marker []
  [:defs
   [:marker {:id "arrowhead"
             :markerWidth 10 :markerHeight 7
             :refX 10 :refY 3.5
             :orient "auto"}
    [:polygon {:points "0 0, 10 3.5, 0 7"
               :fill (t/colors :fg-dim)}]]])

(defn label [{:keys [x y text style]}]
  [:text {:x x :y y
          :fill (or (:color style) (t/colors :fg-dim))
          :font-family (t/fonts :mono)
          :font-size 11}
   text])
```

- [ ] **Step 2: Commit**

```bash
git add site/src/gl1tch/site/diagrams/svg.cljs
git commit -m "feat(site): SVG diagram primitives (box, arrow, label)"
```

---

## Task 7: Diagram DSL — Layout Engine + Flowchart

**Files:**
- Create: `site/src/gl1tch/site/diagrams/core.cljs`
- Create: `site/src/gl1tch/site/diagrams/flowchart.cljs`
- Create: `site/test/gl1tch/site/diagrams/core_test.cljs`
- Create: `site/test/gl1tch/site/diagrams/flowchart_test.cljs`

- [ ] **Step 1: Write layout engine tests**

```clojure
(ns gl1tch.site.diagrams.core-test
  (:require [cljs.test :refer-macros [deftest is testing]]
            [gl1tch.site.diagrams.core :as layout]))

(deftest topological-layers
  (testing "linear chain produces sequential layers"
    (let [nodes [{:id :a} {:id :b} {:id :c}]
          edges [[:a :b] [:b :c]]
          layers (layout/assign-layers nodes edges)]
      (is (= 3 (count layers)))
      (is (= [:a] (first layers)))
      (is (= [:b] (second layers)))
      (is (= [:c] (nth layers 2)))))
  (testing "diamond produces 3 layers"
    (let [nodes [{:id :a} {:id :b} {:id :c} {:id :d}]
          edges [[:a :b] [:a :c] [:b :d] [:c :d]]
          layers (layout/assign-layers nodes edges)]
      (is (= 3 (count layers)))
      (is (= [:a] (first layers)))
      (is (= #{:b :c} (set (second layers))))
      (is (= [:d] (nth layers 2))))))

(deftest position-nodes
  (testing "positions are computed for lr direction"
    (let [spec {:direction :lr
                :nodes [{:id :a :label "A"} {:id :b :label "B"}]
                :edges [[:a :b]]}
          result (layout/compute spec)]
      (is (every? #(contains? % :x) (:nodes result)))
      (is (every? #(contains? % :y) (:nodes result)))
      (is (< (:x (first (:nodes result)))
             (:x (second (:nodes result))))))))
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd site && npx shadow-cljs compile test
```

Expected: FAIL — `gl1tch.site.diagrams.core` not found.

- [ ] **Step 3: Create `site/src/gl1tch/site/diagrams/core.cljs`**

```clojure
(ns gl1tch.site.diagrams.core)

(def ^:private node-w 120)
(def ^:private node-h 40)
(def ^:private gap-x 60)
(def ^:private gap-y 30)
(def ^:private padding 20)

(defn assign-layers
  "Topological sort into layers. Nodes with no incoming edges go first."
  [nodes edges]
  (let [ids (set (map :id nodes))
        in-map (reduce (fn [m [from to]] (update m to (fnil conj #{}) from))
                       {} edges)]
    (loop [remaining ids
           layers []]
      (if (empty? remaining)
        layers
        (let [ready (filterv #(every? (fn [dep] (not (remaining dep)))
                                       (get in-map % #{}))
                             remaining)]
          (if (empty? ready)
            (conj layers (vec remaining))
            (recur (apply disj remaining ready)
                   (conj layers ready))))))))

(defn- node-center [node]
  [(+ (:x node) (/ (:w node) 2))
   (+ (:y node) (/ (:h node) 2))])

(defn- compute-edge-path [from-node to-node direction]
  (let [[fx fy] (node-center from-node)
        [tx ty] (node-center to-node)]
    (if (= direction :lr)
      [[(+ (:x from-node) (:w from-node)) fy]
       [(:x to-node) ty]]
      [[fx (+ (:y from-node) (:h from-node))]
       [tx (:y to-node)]])))

(defn compute
  "Takes a flowchart spec {:direction :lr/:tb :nodes [...] :edges [...]}
   Returns {:nodes [...with :x :y :w :h...] :edges [...with :path...]
            :width :height}"
  [{:keys [direction nodes edges]
    :or {direction :lr}}]
  (let [layers (assign-layers nodes edges)
        node-map (into {} (map (juxt :id identity) nodes))
        positioned
        (reduce
          (fn [acc [layer-idx layer]]
            (reduce
              (fn [acc2 [pos-in-layer node-id]]
                (let [node (get node-map node-id)
                      [x y] (if (= direction :lr)
                              [(+ padding (* layer-idx (+ node-w gap-x)))
                               (+ padding (* pos-in-layer (+ node-h gap-y)))]
                              [(+ padding (* pos-in-layer (+ node-w gap-x)))
                               (+ padding (* layer-idx (+ node-h gap-y)))])]
                  (assoc acc2 node-id
                         (merge node {:x x :y y :w node-w :h node-h}))))
              acc
              (map-indexed vector layer)))
          {}
          (map-indexed vector layers))
        edge-results
        (mapv (fn [[from to]]
                {:from from :to to
                 :path (compute-edge-path (positioned from) (positioned to) direction)})
              edges)
        all-nodes (mapv positioned (mapcat identity layers))
        max-x (apply max (map #(+ (:x %) (:w %)) all-nodes))
        max-y (apply max (map #(+ (:y %) (:h %)) all-nodes))]
    {:nodes all-nodes
     :edges edge-results
     :width (+ max-x padding)
     :height (+ max-y padding)}))
```

- [ ] **Step 4: Run layout engine tests**

```bash
cd site && npx shadow-cljs compile test
```

Expected: All topology and positioning tests pass.

- [ ] **Step 5: Write flowchart render test**

```clojure
(ns gl1tch.site.diagrams.flowchart-test
  (:require [cljs.test :refer-macros [deftest is testing]]
            [gl1tch.site.diagrams.flowchart :as fc]))

(deftest renders-svg
  (testing "flowchart returns an svg hiccup vector"
    (let [spec {:direction :lr
                :nodes [{:id :a :label "Start"} {:id :b :label "End"}]
                :edges [[:a :b]]}
          result (fc/render spec)]
      (is (= :svg (first result)))
      (is (map? (second result))))))

(deftest parses-inline-form
  (testing "parses inline diagram EDN to spec"
    (let [inline [[:node :a "Start"]
                  [:node :b "End"]
                  [:edge :a :b]]
          spec (fc/parse-inline {:type :flowchart :direction :lr} inline)]
      (is (= 2 (count (:nodes spec))))
      (is (= 1 (count (:edges spec))))
      (is (= :lr (:direction spec))))))
```

- [ ] **Step 6: Create `site/src/gl1tch/site/diagrams/flowchart.cljs`**

```clojure
(ns gl1tch.site.diagrams.flowchart
  (:require [gl1tch.site.diagrams.core :as layout]
            [gl1tch.site.diagrams.svg :as svg]))

(defn parse-inline
  "Parses inline diagram children from content EDN into a flowchart spec.
   Input attrs: {:type :flowchart :direction :lr}
   Input children: [[:node :a \"Start\"] [:node :b \"End\"] [:edge :a :b]]
   Returns: {:direction :lr :nodes [...] :edges [...]}"
  [attrs children]
  (let [nodes (filterv #(= :node (first %)) children)
        edges (filterv #(= :edge (first %)) children)]
    {:direction (or (:direction attrs) :lr)
     :nodes (mapv (fn [[_ id label]] {:id id :label label}) nodes)
     :edges (mapv (fn [[_ from to]] [from to]) edges)}))

(defn render
  "Renders a flowchart spec as hiccup SVG."
  [spec]
  (let [{:keys [nodes edges width height]} (layout/compute spec)]
    [:svg {:width width :height height
           :viewBox (str "0 0 " width " " height)
           :xmlns "http://www.w3.org/2000/svg"
           :style {:display :block :margin "16px 0"}}
     [svg/arrowhead-marker]
     (for [node nodes]
       ^{:key (:id node)} [svg/box node])
     (for [edge edges]
       ^{:key (str (:from edge) "-" (:to edge))} [svg/arrow edge])]))
```

- [ ] **Step 7: Run all tests**

```bash
cd site && npx shadow-cljs compile test
```

Expected: All diagram tests pass.

- [ ] **Step 8: Commit**

```bash
git add site/src/gl1tch/site/diagrams/ site/test/gl1tch/site/diagrams/
git commit -m "feat(site): diagram DSL — layout engine + flowchart renderer"
```

---

## Task 8: Content Loading

**Files:**
- Create: `site/src/gl1tch/site/content.clj` (compile-time macro)
- Create: `site/src/gl1tch/site/content.cljs`
- Create: `site/content/docs/getting-started.edn`
- Create: `site/content/labs/repl-driven-docs.edn`
- Create: `site/content/changelog/2026-04-21.edn`
- Create: `site/test/gl1tch/site/content_test.cljs`

- [ ] **Step 1: Create seed content — `site/content/docs/getting-started.edn`**

```clojure
{:title "Getting Started"
 :description "Install glitch and run your first workflow"
 :order 1
 :sections
 [{:heading "Install"
   :level 2
   :body [[:p "Install via Homebrew:"]
          [:code {:lang "bash"} "brew install 8op-org/tap/glitch"]]}
  {:heading "Your first workflow"
   :level 2
   :body [[:p "Create " [:code ".glitch/workflows/hello.glitch"] ":"]
          [:code {:lang "clojure"} "(workflow \"hello\"\n  (step \"greet\"\n    (llm :provider \"copilot\"\n      :prompt \"Summarize this repo\")))"]
          [:p "Run it:"]
          [:code {:lang "bash"} "glitch run hello"]]}]}
```

- [ ] **Step 2: Create seed content — `site/content/labs/repl-driven-docs.edn`**

```clojure
{:title "REPL-Driven Docs"
 :slug "repl-driven-docs"
 :description "Build documentation from a live REPL session"
 :date "2026-04-15"
 :duration "15min"
 :steps
 [{:heading "Connect your REPL"
   :body [[:p "Start glitch with a REPL server:"]
          [:code {:lang "bash"} "glitch repl --nrepl 7888"]]}
  {:heading "Query your workflows"
   :body [[:p "From Emacs, evaluate:"]
          [:code {:lang "clojure"} "(glitch/list-workflows)"]]}]}
```

- [ ] **Step 3: Create seed content — `site/content/changelog/2026-04-21.edn`**

```clojure
{:date "2026-04-21"
 :entries
 [{:type :feat :summary "code intelligence — ast-grep indexer, ES storage, CLI/MCP/REPL queries"}
  {:type :docs :summary "add REPL-driven docs lab — Emacs + glitch repl walkthrough"}
  {:type :fix :summary "static require for glitch.repl so uberscript bundles it"}]}
```

- [ ] **Step 4: Write content loading test**

```clojure
(ns gl1tch.site.content-test
  (:require [cljs.test :refer-macros [deftest is testing]]
            [gl1tch.site.content :as content]))

(deftest doc-index-sorting
  (testing "docs sort by :order"
    (let [docs [{:slug "b" :order 2 :title "B"}
                {:slug "a" :order 1 :title "A"}]
          sorted (content/sort-docs docs)]
      (is (= "a" (:slug (first sorted))))
      (is (= "b" (:slug (second sorted)))))))

(deftest extract-toc
  (testing "extracts headings from sections"
    (let [doc {:sections [{:heading "Install" :level 2 :body []}
                          {:heading "Config" :level 2 :body []}
                          {:heading "Advanced" :level 3 :body []}]}
          toc (content/extract-toc doc)]
      (is (= 3 (count toc)))
      (is (= "Install" (:heading (first toc))))
      (is (= 3 (:level (nth toc 2)))))))
```

- [ ] **Step 5: Run test to verify it fails**

```bash
cd site && npx shadow-cljs compile test
```

Expected: FAIL — `gl1tch.site.content` not found.

- [ ] **Step 6: Create compile-time macro — `site/src/gl1tch/site/content.clj`**

This Clojure file runs at compile time via shadow-cljs:

```clojure
(ns gl1tch.site.content
  (:require [clojure.edn :as edn]
            [clojure.java.io :as io]
            [clojure.string :as str]))

(defmacro load-content [dir]
  (let [content-dir (io/file "content" dir)
        files (when (.exists content-dir)
                (->> (.listFiles content-dir)
                     (filter #(str/ends-with? (.getName %) ".edn"))
                     (mapv (fn [f]
                             (let [slug (str/replace (.getName f) #"\.edn$" "")
                                   data (edn/read-string (slurp f))]
                               (assoc data :slug slug))))))]
    (vec (or files []))))
```

- [ ] **Step 7: Create `site/src/gl1tch/site/content.cljs`**

```clojure
(ns gl1tch.site.content
  (:require-macros [gl1tch.site.content :refer [load-content]]))

(defn sort-docs [docs]
  (sort-by :order docs))

(defn extract-toc [doc]
  (mapv (fn [s] {:heading (:heading s) :level (:level s)})
        (:sections doc)))

(defn find-by-slug [items slug]
  (first (filter #(= slug (:slug %)) items)))

;; Content index — populated at compile time via macro
(def docs (load-content "docs"))
(def labs (load-content "labs"))
(def changelogs (load-content "changelog"))

(defn docs-index []
  (sort-docs docs))

(defn doc-by-slug [slug]
  (find-by-slug docs slug))

(defn labs-index []
  (sort-by :date #(compare %2 %1) labs))

(defn lab-by-slug [slug]
  (find-by-slug labs slug))

(defn changelog-entries []
  (sort-by :date #(compare %2 %1) changelogs))
```

- [ ] **Step 8: Run tests**

```bash
cd site && npx shadow-cljs compile test
```

Expected: All content tests pass.

- [ ] **Step 9: Commit**

```bash
git add site/src/gl1tch/site/content.clj site/src/gl1tch/site/content.cljs site/content/ site/test/gl1tch/site/content_test.cljs
git commit -m "feat(site): content loading system with compile-time EDN macros"
```

---

## Task 9: Doc Pages (Sidebar + TOC + Content Renderer)

**Files:**
- Create: `site/src/gl1tch/site/components/sidebar.cljs`
- Create: `site/src/gl1tch/site/components/toc.cljs`
- Create: `site/src/gl1tch/site/pages/docs.cljs`
- Modify: `site/src/gl1tch/site/core.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/components/sidebar.cljs`**

```clojure
(ns gl1tch.site.components.sidebar
  (:require [reitit.frontend.easy :as rfe]
            [gl1tch.site.content :as content]
            [gl1tch.site.state :as state]
            [gl1tch.site.styles.theme :as t]))

(defn sidebar []
  (let [match @state/current-match
        current-slug (get-in match [:path-params :slug])
        docs (content/docs-index)]
    [:nav {:style {:position :sticky
                   :top "80px"
                   :max-height "calc(100vh - 100px)"
                   :overflow-y :auto}}
     [:div {:style {:display :flex
                    :flex-direction :column
                    :gap "2px"}}
      (for [doc docs]
        ^{:key (:slug doc)}
        [:a {:href (rfe/href :docs/page {:slug (:slug doc)})
             :style {:font-size (t/sizes :xs)
                     :color (if (= current-slug (:slug doc))
                              (t/colors :accent)
                              (t/colors :fg-dim))
                     :text-decoration :none
                     :padding "5px 12px"
                     :border-radius t/radius
                     :border-left (str "2px solid "
                                       (if (= current-slug (:slug doc))
                                         (t/colors :accent)
                                         "transparent"))}}
         (:title doc)])]]))
```

- [ ] **Step 2: Create `site/src/gl1tch/site/components/toc.cljs`**

```clojure
(ns gl1tch.site.components.toc
  (:require [gl1tch.site.styles.theme :as t]
            [clojure.string :as str]))

(defn- heading->id [heading]
  (-> heading str/lower-case (str/replace #"[^a-z0-9]+" "-") (str/replace #"^-|-$" "")))

(defn toc [sections]
  (when (seq sections)
    [:nav {:style {:position :sticky
                   :top "80px"}}
     [:div {:style {:font-size (t/sizes :xxs)
                    :text-transform :uppercase
                    :letter-spacing "0.2em"
                    :color (t/colors :accent)
                    :margin-bottom "16px"
                    :opacity 0.6}}
      "On this page"]
     [:div {:style {:display :flex
                    :flex-direction :column
                    :gap "8px"}}
      (for [{:keys [heading level]} sections]
        ^{:key heading}
        [:a {:href (str "#" (heading->id heading))
             :style {:font-size (t/sizes :xs)
                     :color (t/colors :fg-dim)
                     :text-decoration :none
                     :padding-left (str (* (- level 2) 12) "px")
                     :border-left "1px solid transparent"}}
         heading])]]))
```

- [ ] **Step 3: Create `site/src/gl1tch/site/pages/docs.cljs`**

```clojure
(ns gl1tch.site.pages.docs
  (:require [gl1tch.site.content :as content]
            [gl1tch.site.components.sidebar :as sidebar]
            [gl1tch.site.components.toc :as toc]
            [gl1tch.site.components.code :as code]
            [gl1tch.site.diagrams.flowchart :as flowchart]
            [gl1tch.site.styles.theme :as t]
            [clojure.string :as str]))

(defn- heading->id [heading]
  (-> heading str/lower-case (str/replace #"[^a-z0-9]+" "-") (str/replace #"^-|-$" "")))

(defn render-hiccup
  "Renders a hiccup body vector, resolving :code and :diagram tags."
  [element]
  (cond
    (string? element) element
    (nil? element) nil
    (vector? element)
    (let [[tag & rest] element
          [attrs children] (if (map? (first rest))
                             [(first rest) (vec (next rest))]
                             [nil (vec rest)])]
      (case tag
        :p (into [:p] (map render-hiccup children))
        :code (if attrs
                [code/code-block attrs (first children)]
                [code/inline-code (first children)])
        :diagram (case (:type attrs)
                   :flowchart [flowchart/render
                               (flowchart/parse-inline attrs children)]
                   [:div "Unknown diagram type"])
        :a (into [:a attrs] (map render-hiccup children))
        :strong (into [:strong] (map render-hiccup children))
        :em (into [:em] (map render-hiccup children))
        :ul (into [:ul] (map render-hiccup children))
        :ol (into [:ol] (map render-hiccup children))
        :li (into [:li] (map render-hiccup children))
        ;; Default: pass through
        (if attrs
          (into [tag attrs] (map render-hiccup children))
          (into [tag] (map render-hiccup children)))))
    :else (str element)))

(defn- render-section [{:keys [heading level body]}]
  [:section {:id (heading->id heading)
             :style {:margin-top (if (= level 2) "56px" "36px")}}
   [(keyword (str "h" level))
    {:style {:color (if (= level 2) (t/colors :accent) (t/colors :accent-2))
             :margin-bottom "12px"}}
    heading]
   (into [:div] (map render-hiccup body))])

(defn page []
  (fn [slug]
    (if-let [doc (content/doc-by-slug slug)]
      [:div {:style {:max-width "1280px"
                     :margin "0 auto"
                     :padding "80px 40px 0"
                     :display :grid
                     :grid-template-columns "220px 1fr 200px"
                     :gap "0 48px"}}
       [:div [sidebar/sidebar]]
       [:article {:style {:min-width 0 :max-width "720px"}}
        [:h1 {:style {:font-size (t/sizes :h2)
                      :margin-bottom "12px"}}
         (:title doc)]
        (when (:description doc)
          [:p {:style {:color (t/colors :fg-dim)
                       :margin-bottom "40px"}}
           (:description doc)])
        (for [section (:sections doc)]
          ^{:key (:heading section)}
          [render-section section])]
       [:div [toc/toc (content/extract-toc doc)]]]
      [:div.wrap [:h1 "Doc not found"]])))

(defn index []
  [:div.wrap {:style {:padding-top "80px"}}
   [:h1 "Documentation"]
   [:div {:style {:margin-top "40px"}}
    (for [doc (content/docs-index)]
      ^{:key (:slug doc)}
      [:div {:style {:margin-bottom "24px"}}
       [:a {:href (str "#/docs/" (:slug doc))
            :style {:color (t/colors :accent)
                    :font-size "1.05rem"
                    :font-weight 700
                    :text-decoration :none}}
        (:title doc)]
       (when (:description doc)
         [:p {:style {:color (t/colors :fg-dim)
                      :font-size (t/sizes :sm)}}
          (:description doc)])])]])
```

- [ ] **Step 4: Wire docs pages into core.cljs**

Full updated `site/src/gl1tch/site/core.cljs`:

```clojure
(ns gl1tch.site.core
  (:require [reagent.dom :as rdom]
            [gl1tch.site.router :as router]
            [gl1tch.site.state :as state]
            [gl1tch.site.styles.global :as styles]
            [gl1tch.site.components.nav :as nav]
            [gl1tch.site.pages.docs :as docs]))

(defn current-page []
  (let [match @state/current-match
        name (get-in match [:data :name])
        slug (get-in match [:path-params :slug])]
    [:div.app
     [nav/nav]
     [:main {:style {:padding-top "52px"}}
      (case name
        :home [:div.wrap [:h1 "gl1tch"]]
        :docs/index [docs/index]
        :docs/page [docs/page slug]
        :labs/index [:div.wrap [:h1 "Labs"]]
        :labs/page [:div.wrap [:h1 (str "Lab: " slug)]]
        :changelog [:div.wrap [:h1 "Changelog"]]
        [:div.wrap [:h1 "404"]])]]))

(defn ^:export init []
  (styles/inject!)
  (router/start!)
  (rdom/render [current-page] (.getElementById js/document "app")))

(defn ^:dev/after-load reload []
  (styles/inject!)
  (rdom/render [current-page] (.getElementById js/document "app")))
```

- [ ] **Step 5: Commit**

```bash
git add site/src/gl1tch/site/components/sidebar.cljs site/src/gl1tch/site/components/toc.cljs site/src/gl1tch/site/pages/docs.cljs site/src/gl1tch/site/core.cljs
git commit -m "feat(site): doc pages with sidebar, TOC, and hiccup content renderer"
```

---

## Task 10: Lab Pages

**Files:**
- Create: `site/src/gl1tch/site/pages/labs.cljs`
- Modify: `site/src/gl1tch/site/core.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/pages/labs.cljs`**

```clojure
(ns gl1tch.site.pages.labs
  (:require [gl1tch.site.content :as content]
            [gl1tch.site.pages.docs :as docs]
            [gl1tch.site.styles.theme :as t]))

(defn page []
  (fn [slug]
    (if-let [lab (content/lab-by-slug slug)]
      [:div.wrap {:style {:padding-top "80px" :max-width "720px"}}
       [:h1 {:style {:margin-bottom "8px"}} (:title lab)]
       [:div {:style {:display :flex :gap "16px" :margin-bottom "40px"
                      :color (t/colors :fg-dim) :font-size (t/sizes :sm)}}
        [:span (:date lab)]
        (when (:duration lab)
          [:span {:style {:color (t/colors :accent)
                          :background "rgba(0, 255, 159, 0.06)"
                          :padding "2px 8px"
                          :border-radius t/radius}}
           (:duration lab)])]
       (for [[i step] (map-indexed vector (:steps lab))]
         ^{:key i}
         [:section {:style {:margin-bottom "48px"}}
          [:div {:style {:display :flex :align-items :baseline :gap "12px"
                         :margin-bottom "16px"}}
           [:span {:style {:font-size (t/sizes :xxs)
                           :color (t/colors :accent)
                           :opacity 0.5
                           :letter-spacing "0.15em"}}
            (str "step " (inc i))]
           [:h2 {:style {:font-size (t/sizes :h3)
                         :color (t/colors :accent-2)
                         :margin 0}}
            (:heading step)]]
          (into [:div] (map docs/render-hiccup (:body step)))])]
      [:div.wrap [:h1 "Lab not found"]])))

(defn index []
  [:div.wrap {:style {:padding-top "80px"}}
   [:h1 "Labs"]
   [:p {:style {:color (t/colors :fg-dim)
                :margin-bottom "40px"}}
    "Focused 15-minute walkthroughs."]
   [:div {:style {:display :grid :gap "1px"
                  :background (t/colors :border)
                  :border (str "1px solid " (t/colors :border))
                  :border-radius "8px"
                  :overflow :hidden}}
    (for [lab (content/labs-index)]
      ^{:key (:slug lab)}
      [:a {:href (str "#/labs/" (:slug lab))
           :style {:display :block
                   :background (t/colors :bg-card)
                   :padding "24px"
                   :text-decoration :none}}
       [:div {:style {:display :flex :justify-content :space-between
                      :align-items :center}}
        [:div
         [:div {:style {:font-weight 700
                        :color (t/colors :fg)
                        :margin-bottom "6px"}}
          (:title lab)]
         [:div {:style {:font-size (t/sizes :sm)
                        :color (t/colors :fg-dim)}}
          (:description lab)]]
        (when (:duration lab)
          [:span {:style {:font-size (t/sizes :xs)
                          :color (t/colors :accent)
                          :white-space :nowrap}}
           (:duration lab)])]])]])
```

- [ ] **Step 2: Wire labs into core.cljs**

Update requires and case in `site/src/gl1tch/site/core.cljs`:

```clojure
(ns gl1tch.site.core
  (:require [reagent.dom :as rdom]
            [gl1tch.site.router :as router]
            [gl1tch.site.state :as state]
            [gl1tch.site.styles.global :as styles]
            [gl1tch.site.components.nav :as nav]
            [gl1tch.site.pages.docs :as docs]
            [gl1tch.site.pages.labs :as labs]))

(defn current-page []
  (let [match @state/current-match
        name (get-in match [:data :name])
        slug (get-in match [:path-params :slug])]
    [:div.app
     [nav/nav]
     [:main {:style {:padding-top "52px"}}
      (case name
        :home [:div.wrap [:h1 "gl1tch"]]
        :docs/index [docs/index]
        :docs/page [docs/page slug]
        :labs/index [labs/index]
        :labs/page [labs/page slug]
        :changelog [:div.wrap [:h1 "Changelog"]]
        [:div.wrap [:h1 "404"]])]]))

(defn ^:export init []
  (styles/inject!)
  (router/start!)
  (rdom/render [current-page] (.getElementById js/document "app")))

(defn ^:dev/after-load reload []
  (styles/inject!)
  (rdom/render [current-page] (.getElementById js/document "app")))
```

- [ ] **Step 3: Commit**

```bash
git add site/src/gl1tch/site/pages/labs.cljs site/src/gl1tch/site/core.cljs
git commit -m "feat(site): lab pages — index cards + step-by-step renderer"
```

---

## Task 11: Changelog Page

**Files:**
- Create: `site/src/gl1tch/site/pages/changelog.cljs`
- Modify: `site/src/gl1tch/site/core.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/pages/changelog.cljs`**

```clojure
(ns gl1tch.site.pages.changelog
  (:require [gl1tch.site.content :as content]
            [gl1tch.site.styles.theme :as t]))

(def type-colors
  {:feat (t/colors :accent)
   :fix  (t/colors :danger)
   :docs (t/colors :accent-2)
   :refactor (t/colors :fg-dim)})

(defn page []
  [:div.wrap {:style {:padding-top "80px"}}
   [:h1 "Changelog"]
   [:div {:style {:margin-top "40px"}}
    (for [entry (content/changelog-entries)]
      ^{:key (:date entry)}
      [:div {:style {:margin-bottom "48px"}}
       [:h2 {:style {:font-size "1.1rem"
                     :color (t/colors :accent)
                     :margin-bottom "16px"}}
        (:date entry)]
       [:div {:style {:display :flex :flex-direction :column :gap "8px"}}
        (for [[i item] (map-indexed vector (:entries entry))]
          ^{:key i}
          [:div {:style {:display :flex :gap "12px" :align-items :baseline}}
           [:span {:style {:font-size (t/sizes :xxs)
                           :font-weight 700
                           :text-transform :uppercase
                           :letter-spacing "0.1em"
                           :color (get type-colors (:type item) (t/colors :fg-dim))
                           :min-width "60px"}}
            (name (:type item))]
           [:span {:style {:font-size (t/sizes :sm)
                           :color (t/colors :fg)}}
            (:summary item)]])]])]])
```

- [ ] **Step 2: Wire changelog into core.cljs**

Final `site/src/gl1tch/site/core.cljs`:

```clojure
(ns gl1tch.site.core
  (:require [reagent.dom :as rdom]
            [gl1tch.site.router :as router]
            [gl1tch.site.state :as state]
            [gl1tch.site.styles.global :as styles]
            [gl1tch.site.components.nav :as nav]
            [gl1tch.site.pages.docs :as docs]
            [gl1tch.site.pages.labs :as labs]
            [gl1tch.site.pages.changelog :as changelog]))

(defn current-page []
  (let [match @state/current-match
        name (get-in match [:data :name])
        slug (get-in match [:path-params :slug])]
    [:div.app
     [nav/nav]
     [:main {:style {:padding-top "52px"}}
      (case name
        :home [:div.wrap [:h1 "gl1tch"]]
        :docs/index [docs/index]
        :docs/page [docs/page slug]
        :labs/index [labs/index]
        :labs/page [labs/page slug]
        :changelog [changelog/page]
        [:div.wrap [:h1 "404"]])]]))

(defn ^:export init []
  (styles/inject!)
  (router/start!)
  (rdom/render [current-page] (.getElementById js/document "app")))

(defn ^:dev/after-load reload []
  (styles/inject!)
  (rdom/render [current-page] (.getElementById js/document "app")))
```

- [ ] **Step 3: Commit**

```bash
git add site/src/gl1tch/site/pages/changelog.cljs site/src/gl1tch/site/core.cljs
git commit -m "feat(site): changelog page with type-colored entries"
```

---

## Task 12: Landing Page + HexRain

**Files:**
- Create: `site/src/gl1tch/site/components/hex_rain.cljs`
- Create: `site/src/gl1tch/site/pages/home.cljs`
- Modify: `site/src/gl1tch/site/core.cljs`

- [ ] **Step 1: Create `site/src/gl1tch/site/components/hex_rain.cljs`**

Port the existing canvas effect to a Reagent component:

```clojure
(ns gl1tch.site.components.hex-rain
  (:require [reagent.core :as r]))

(def ^:private col-w 28)
(def ^:private row-h 18)

(defn- hex-byte []
  (-> (js/Math.random) (* 256) js/Math.floor (.toString 16) (.padStart 2 "0") .toUpperCase))

(defn- setup [canvas]
  (let [w (.-innerWidth js/window)
        h (.-innerHeight js/window)
        cols (+ (js/Math.ceil (/ w col-w)) 2)
        rows (+ (js/Math.ceil (/ h row-h)) 3)
        grid-h (* rows 4)]
    (set! (.-width canvas) w)
    (set! (.-height canvas) h)
    {:cols cols :rows rows
     :offsets (vec (repeatedly cols #(* (js/Math.random) rows row-h)))
     :speeds (vec (repeatedly cols #(+ 0.25 (* (js/Math.random) 0.45))))
     :grid (vec (repeatedly grid-h
                  (fn [] (vec (repeatedly cols hex-byte)))))}))

(defn hex-rain []
  (let [state (r/atom nil)
        canvas-ref (r/atom nil)
        raf-id (r/atom nil)]
    (r/create-class
      {:component-did-mount
       (fn [_]
         (when-let [canvas @canvas-ref]
           (reset! state (setup canvas))
           (let [ctx (.getContext canvas "2d")]
             (letfn [(draw []
                       (let [{:keys [cols rows offsets speeds grid]} @state]
                         (.clearRect ctx 0 0 (.-width canvas) (.-height canvas))
                         (set! (.-font ctx) "12px 'JetBrains Mono', monospace")
                         (set! (.-fillStyle ctx) "rgba(135, 135, 175, 0.07)")
                         (let [new-offsets
                               (mapv (fn [col]
                                       (let [off (mod (+ (nth offsets col) (nth speeds col))
                                                      (* (count grid) row-h))
                                             start-row (js/Math.floor (/ off row-h))
                                             sub-px (mod off row-h)]
                                         (doseq [row (range (+ rows 2))]
                                           (let [y (- (* row row-h) sub-px)
                                                 grid-row (mod (+ start-row row) (count grid))]
                                             (.fillText ctx (get-in grid [grid-row col]) (* col col-w) y)))
                                         off))
                                     (range cols))]
                           (swap! state assoc :offsets new-offsets))
                         (reset! raf-id (js/requestAnimationFrame draw))))]
               (draw)))))
       :component-will-unmount
       (fn [_]
         (when @raf-id
           (js/cancelAnimationFrame @raf-id)))
       :reagent-render
       (fn []
         [:canvas {:ref #(reset! canvas-ref %)
                   :style {:position :fixed
                           :top 0 :left 0
                           :width "100%" :height "100%"
                           :z-index 0
                           :pointer-events :none
                           :opacity 0.04}}])})))
```

- [ ] **Step 2: Create `site/src/gl1tch/site/pages/home.cljs`**

Port the landing page hero and feature sections:

```clojure
(ns gl1tch.site.pages.home
  (:require [gl1tch.site.components.hex-rain :as hex-rain]
            [gl1tch.site.styles.theme :as t]))

(defn- code-span [cls text]
  [:span {:style {:color (case cls
                           "k" "#7dcfff"
                           "s" "#e0af68"
                           "r" "#7a88a8"
                           "p" "#7dcfff"
                           (t/colors :fg))}}
   text])

(defn hero []
  [:section {:style {:min-height "100vh"
                     :display :flex
                     :align-items :center
                     :padding "120px 0 80px"}}
   [:div {:style {:max-width "1120px"
                  :margin "0 auto"
                  :padding "0 40px"
                  :display :grid
                  :grid-template-columns "1fr 1.2fr"
                  :gap "60px"
                  :align-items :center
                  :position :relative
                  :z-index 1}}
    [:div
     [:h1 {:style {:font-size (t/sizes :h1)
                   :font-weight 700
                   :letter-spacing "-0.04em"
                   :line-height 1.05
                   :margin-bottom "32px"}}
      "gl1tch"]
     [:p {:style {:font-size "1.15rem"
                  :opacity 0.7
                  :line-height 2
                  :margin-bottom "40px"}}
      "Shell does the work." [:br]
      "LLM does the thinking." [:br]
      "You own the workflow."]
     [:pre.code {:style {:margin-bottom "16px" :max-width "460px"}}
      [code-span "p" "$ "] "brew install 8op-org/tap/glitch"]
     [:div {:style {:display :flex :gap "12px"}}
      [:a {:href "https://github.com/8op-org/gl1tch"
           :style {:display :inline-block
                   :padding "12px 28px"
                   :background (t/colors :accent)
                   :color (t/colors :bg)
                   :border-radius "4px"
                   :font-weight 600
                   :font-size "0.8rem"
                   :text-decoration :none}}
       "GitHub"]
      [:a {:href "#/docs/getting-started"
           :style {:display :inline-block
                   :padding "12px 28px"
                   :border (str "1px solid " (t/colors :border))
                   :color (t/colors :fg-dim)
                   :border-radius "4px"
                   :font-size "0.8rem"
                   :text-decoration :none}}
       "Docs"]]]
    [:pre.code {:style {:font-size "0.78rem"
                        :line-height 2
                        :margin-bottom 0}}
     [code-span "r" ";; agentic mode -- LLM pulls its own context"] "\n\n"
     "(" [code-span "k" "workflow"] " " [code-span "s" "\"pr-review\""] "\n\n"
     "  (" [code-span "k" "step"] " " [code-span "s" "\"fetch\""] "\n"
     "    (" [code-span "k" "sh"] " " [code-span "s" "\"gh pr view ~input --json title,body,files\""] "))\n\n"
     "  (" [code-span "k" "step"] " " [code-span "s" "\"review\""] "\n"
     "    (" [code-span "k" "llm"] " " [code-span "r" ":agentic"] " " [code-span "s" "true"] " "
     [code-span "r" ":max-rounds"] " " [code-span "s" "10"] "\n"
     "      " [code-span "r" ":prompt"] " " [code-span "s" "```\n        PR: ~(ref \"fetch\")\n        Read the changed files.\n        Review as a senior engineer.\n        ```"] ")))"]]])

(defn page []
  [:div
   [hex-rain/hex-rain]
   [hero]])
```

- [ ] **Step 3: Wire home page into core.cljs**

Update the `:home` case in `current-page`:

```clojure
;; Add to requires:
[gl1tch.site.pages.home :as home]

;; Update case:
:home [home/page]
```

- [ ] **Step 4: Commit**

```bash
git add site/src/gl1tch/site/components/hex_rain.cljs site/src/gl1tch/site/pages/home.cljs site/src/gl1tch/site/core.cljs
git commit -m "feat(site): landing page with hero section and hex rain effect"
```

---

## Task 13: Update bb.edn + GitHub Actions

**Files:**
- Modify: `bb.edn`
- Modify: `.github/workflows/pages.yml`

- [ ] **Step 1: Read current bb.edn**

Read `bb.edn` to find the existing `site:dev` and `site:build` task definitions.

- [ ] **Step 2: Update bb.edn site tasks**

Replace existing site tasks with shadow-cljs equivalents:

```clojure
site:dev {:doc "Start site dev server with nREPL"
          :task (shell {:dir "site"} "npx shadow-cljs watch app")}

site:build {:doc "Build site for production"
            :task (shell {:dir "site"} "npx shadow-cljs release app")}

site:repl {:doc "Connect nREPL to site dev server"
           :task (shell {:dir "site"} "npx shadow-cljs cljs-repl app")}
```

- [ ] **Step 3: Update `.github/workflows/pages.yml`**

Replace the entire file:

```yaml
name: Deploy site to GitHub Pages

on:
  push:
    branches: [main]
    paths: ['site/**']

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - name: Install deps
        run: cd site && npm ci
      - name: Build
        run: cd site && npx shadow-cljs release app
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site/resources/public
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 4: Commit**

```bash
git add bb.edn .github/workflows/pages.yml
git commit -m "feat(site): update bb.edn tasks and GitHub Actions for shadow-cljs"
```

---

## Task 14: Modular Glitch Workflows

**Files:**
- Create: `workflows/site/generate-doc.glitch`
- Create: `workflows/site/generate-lab.glitch`
- Create: `workflows/site/generate-changelog.glitch`
- Create: `workflows/site/gate-syntax.glitch`
- Create: `workflows/site/gate-structure.glitch`
- Create: `workflows/site/gate-links.glitch`
- Create: `workflows/site/gate-hallucinations.glitch`
- Create: `workflows/site/sync.glitch`
- Create: `workflows/site/deploy.glitch`
- Modify: `site/conventions.md`

- [ ] **Step 1: Create `workflows/site/generate-doc.glitch`**

```
;; Generate a single doc page as EDN
;; Usage: glitch run workflows/site/generate-doc.glitch --param slug=getting-started

(include "site/shared.glitch")
(include "workflows/site-manifest.glitch")

(step "context"
  (sh "cat bb/src/glitch/core.clj bb/src/glitch/dsl.clj 2>/dev/null || true"))

(step "existing"
  (sh "cat site/content/docs/~(param :slug).edn 2>/dev/null || echo ':none'"))

(step "generate"
  (llm :provider "copilot" :agentic true
    :tools ["glitch_read_file" "glitch_grep"]
    :prompt ```
    Generate documentation for the page slug: ~(param :slug)

    Output MUST be a valid EDN map with this exact schema:
    {:title "Page Title"
     :description "One-line description"
     :order N
     :sections [{:heading "Section Name" :level 2
                 :body [[:p "Text"] [:code {:lang "clojure"} "code"]]}]}

    Body elements are hiccup vectors:
    - [:p "text"] for paragraphs
    - [:p "text " [:code "inline"] " more text"] for inline code
    - [:code {:lang "bash"} "code here"] for code blocks
    - [:a {:href "/docs/other-page"} "link text"] for links
    - [:diagram {:type :flowchart :direction :lr} [:node :a "A"] [:node :b "B"] [:edge :a :b]] for diagrams

    RULES:
    - All code examples MUST come from the actual codebase, never invent syntax
    - Use "your" framing, examples before explanation
    - No internal details (BubbleTea, tmux, SQLite, OTel, lipgloss)
    - Output raw EDN only, no markdown wrapping

    Existing page (if any): ~(ref "existing")
    Source context: ~(ref "context")
    ```))

(step "save"
  (sh "echo '~(ref \"generate\")' > site/content/docs/~(param :slug).edn"))
```

- [ ] **Step 2: Create `workflows/site/generate-lab.glitch`**

```
;; Generate a single lab page as EDN
;; Usage: glitch run workflows/site/generate-lab.glitch --param slug=repl-driven-docs

(include "site/shared.glitch")

(step "generate"
  (llm :provider "copilot" :agentic true
    :tools ["glitch_read_file" "glitch_grep"]
    :prompt ```
    Generate a 15-minute lab tutorial for slug: ~(param :slug)

    Output MUST be a valid EDN map:
    {:title "Lab Title"
     :slug "~(param :slug)"
     :description "One-line description"
     :date "~(sh "date +%Y-%m-%d")"
     :duration "15min"
     :steps [{:heading "Step Name"
              :body [[:p "text"] [:code {:lang "bash"} "command"]]}]}

    Body elements use the same hiccup format as doc pages.
    Keep it focused — one concept, clear steps, working examples.
    Output raw EDN only.
    ```))

(step "save"
  (sh "echo '~(ref \"generate\")' > site/content/labs/~(param :slug).edn"))
```

- [ ] **Step 3: Create `workflows/site/generate-changelog.glitch`**

```
;; Generate changelog EDN from git history
;; Usage: glitch run workflows/site/generate-changelog.glitch --param date=2026-04-21

(step "commits"
  (sh "git log --oneline --since='~(param :date) 00:00:00' --until='~(param :date) 23:59:59' -- bb/ workflows/ site/"))

(step "generate"
  (llm :provider "lmstudio"
    :prompt ```
    Classify these commits into a changelog EDN:

    ~(ref "commits")

    Output MUST be:
    {:date "~(param :date)"
     :entries [{:type :feat :summary "description"}
               {:type :fix :summary "description"}]}

    Types: :feat :fix :docs :refactor
    Output raw EDN only.
    ```))

(step "save"
  (sh "echo '~(ref \"generate\")' > site/content/changelog/~(param :date).edn"))
```

- [ ] **Step 4: Create `workflows/site/gate-syntax.glitch`**

```
;; Validate EDN syntax — if edn/read succeeds, it's valid
;; Usage: glitch run workflows/site/gate-syntax.glitch [--param file=content/docs/foo.edn]

(step "validate"
  (sh ```
    if [ -n "~(param :file "")" ]; then
      bb -e "(clojure.edn/read-string (slurp \"site/~(param :file)\"))" && echo "PASS: ~(param :file)" || echo "FAIL: ~(param :file)"
    else
      for f in site/content/docs/*.edn site/content/labs/*.edn site/content/changelog/*.edn; do
        bb -e "(clojure.edn/read-string (slurp \"$f\"))" 2>/dev/null && echo "PASS: $f" || echo "FAIL: $f"
      done
    fi
    ```))
```

- [ ] **Step 5: Create `workflows/site/gate-structure.glitch`**

```
;; Schema validation — check required keys and types
;; Usage: glitch run workflows/site/gate-structure.glitch

(step "validate"
  (sh ```
    bb -e '
    (require (quote [clojure.edn :as edn]))
    (defn check-doc [f]
      (let [d (edn/read-string (slurp f))]
        (assert (:title d) (str f " missing :title"))
        (assert (number? (:order d)) (str f " missing :order"))
        (assert (vector? (:sections d)) (str f " missing :sections"))
        (doseq [s (:sections d)]
          (assert (:heading s) (str f " section missing :heading"))
          (assert (#{2 3 4} (:level s)) (str f " invalid :level"))
          (assert (vector? (:body s)) (str f " section missing :body")))
        (println "PASS:" f)))
    (defn check-lab [f]
      (let [d (edn/read-string (slurp f))]
        (assert (:title d) (str f " missing :title"))
        (assert (:slug d) (str f " missing :slug"))
        (assert (vector? (:steps d)) (str f " missing :steps"))
        (doseq [s (:steps d)]
          (assert (:heading s) (str f " step missing :heading"))
          (assert (vector? (:body s)) (str f " step missing :body")))
        (println "PASS:" f)))
    (defn check-changelog [f]
      (let [d (edn/read-string (slurp f))]
        (assert (:date d) (str f " missing :date"))
        (assert (vector? (:entries d)) (str f " missing :entries"))
        (doseq [e (:entries d)]
          (assert (:type e) (str f " entry missing :type"))
          (assert (:summary e) (str f " entry missing :summary")))
        (println "PASS:" f)))
    (doseq [f (file-seq (clojure.java.io/file "site/content/docs"))]
      (when (.endsWith (str f) ".edn") (check-doc (str f))))
    (doseq [f (file-seq (clojure.java.io/file "site/content/labs"))]
      (when (.endsWith (str f) ".edn") (check-lab (str f))))
    (doseq [f (file-seq (clojure.java.io/file "site/content/changelog"))]
      (when (.endsWith (str f) ".edn") (check-changelog (str f))))
    '
    ```))
```

- [ ] **Step 6: Create `workflows/site/gate-links.glitch`**

```
;; Walk hiccup trees for [:a {:href}], verify all internal links resolve
;; Usage: glitch run workflows/site/gate-links.glitch

(step "check-links"
  (sh ```
    bb -e '
    (require (quote [clojure.edn :as edn])
             (quote [clojure.java.io :as io])
             (quote [clojure.string :as str]))
    (defn collect-hrefs [form]
      (cond
        (and (vector? form) (= :a (first form)) (map? (second form)))
        [(get (second form) :href)]
        (vector? form) (mapcat collect-hrefs form)
        :else []))
    (defn all-slugs [dir]
      (->> (file-seq (io/file dir))
           (filter #(.endsWith (str %) ".edn"))
           (map #(str/replace (.getName %) #"\.edn$" ""))
           set))
    (let [doc-slugs (all-slugs "site/content/docs")
          lab-slugs (all-slugs "site/content/labs")
          valid-routes (into #{"/docs" "/labs" "/changelog" "/"}
                        (concat
                          (map #(str "/docs/" %) doc-slugs)
                          (map #(str "/labs/" %) lab-slugs)))
          errors (atom [])]
      (doseq [dir ["site/content/docs" "site/content/labs"]
              f (file-seq (io/file dir))
              :when (.endsWith (str f) ".edn")]
        (let [data (edn/read-string (slurp (str f)))
              body (or (:sections data) (:steps data))
              hrefs (mapcat #(collect-hrefs (:body %)) body)]
          (doseq [href hrefs]
            (when (and (str/starts-with? href "/")
                       (not (valid-routes href)))
              (swap! errors conj (str f " -> " href))))))
      (if (empty? @errors)
        (println "PASS: all internal links valid")
        (do (doseq [e @errors] (println "FAIL:" e))
            (System/exit 1))))
    '
    ```))
```

- [ ] **Step 7: Create `workflows/site/gate-hallucinations.glitch`**

```
;; Grounding check — verify claims in content against source code
;; Usage: glitch run workflows/site/gate-hallucinations.glitch [--param file=content/docs/foo.edn]

(include "site/shared.glitch")

(step "content"
  (sh ```
    if [ -n "~(param :file "")" ]; then
      cat "site/~(param :file)"
    else
      cat site/content/docs/*.edn
    fi
    ```))

(step "verify"
  (llm :provider "copilot" :agentic true
    :tools ["glitch_read_file" "glitch_grep"]
    :prompt ```
    You are a verification gate. Check that every technical claim in the
    content below is grounded in the actual codebase.

    For each code example, command, or feature mentioned:
    1. Search the codebase to verify it exists
    2. Verify syntax matches actual usage

    Content to verify:
    ~(ref "content")

    Output a JSON object: {"pass": true/false, "issues": ["description of any ungrounded claims"]}
    If all claims are grounded, return {"pass": true, "issues": []}
    ```))
```

- [ ] **Step 8: Create `workflows/site/sync.glitch`**

```
;; Orchestrator — generate all content + run all gates
;; Usage: glitch run workflows/site/sync.glitch

(step "generate-docs"
  (sh ```
    for slug in getting-started workflow-syntax dsl-reference providers plugins workspaces code-intelligence tool-use local-models phases-and-gates compare; do
      glitch run workflows/site/generate-doc.glitch --param slug=$slug
    done
    ```))

(step "generate-changelog"
  (sh "glitch run workflows/site/generate-changelog.glitch --param date=$(date +%Y-%m-%d)"))

(step "gate-syntax"
  (sh "glitch run workflows/site/gate-syntax.glitch"))

(step "gate-structure"
  (sh "glitch run workflows/site/gate-structure.glitch"))

(step "gate-links"
  (sh "glitch run workflows/site/gate-links.glitch"))

(step "gate-hallucinations"
  (sh "glitch run workflows/site/gate-hallucinations.glitch"))
```

- [ ] **Step 9: Create `workflows/site/deploy.glitch`**

```
;; Build, commit, and push
;; Usage: glitch run workflows/site/deploy.glitch

(step "gates"
  (sh ```
    glitch run workflows/site/gate-syntax.glitch && \
    glitch run workflows/site/gate-structure.glitch && \
    glitch run workflows/site/gate-links.glitch
    ```))

(step "build"
  (sh "cd site && npx shadow-cljs release app"))

(step "commit"
  (sh ```
    git add site/content/ site/resources/public/
    git commit -m "site: update content and build" || true
    ```))
```

- [ ] **Step 10: Update `site/conventions.md`**

Append an EDN content format section:

```markdown
## Content Format (EDN)

All generated content MUST be valid EDN. Output raw EDN, never markdown.

### Doc schema:
{:title "Page Title"
 :description "One-line description"
 :order 1
 :sections [{:heading "Section" :level 2
             :body [[:p "text"]
                    [:code {:lang "bash"} "command"]
                    [:code "inline-code"]
                    [:a {:href "/docs/other"} "link"]
                    [:diagram {:type :flowchart :direction :lr}
                     [:node :a "Start"] [:node :b "End"] [:edge :a :b]]]}]}

### Lab schema:
{:title "Lab Title" :slug "slug" :description "desc"
 :date "2026-04-21" :duration "15min"
 :steps [{:heading "Step" :body [[:p "text"]]}]}

### Changelog schema:
{:date "2026-04-21"
 :entries [{:type :feat :summary "what changed"}]}
```

- [ ] **Step 11: Commit**

```bash
git add workflows/site/ site/conventions.md
git commit -m "feat(site): modular glitch workflows for EDN content generation and gating"
```

---

## Task 15: Clean Up Astro Artifacts

**Files:**
- Delete: `site/astro.config.mjs`
- Delete: `site/src/content.config.ts`
- Delete: `site/src/pages/` (entire directory)
- Delete: `site/src/layouts/` (entire directory)
- Delete: `site/src/components/` (Astro components)
- Delete: `site/src/styles/global.css`
- Delete: `site/src/content/` (old markdown content)

- [ ] **Step 1: Preserve tmLanguage for reference**

```bash
cp site/src/glitch.tmLanguage.json site/resources/ 2>/dev/null || true
```

- [ ] **Step 2: Remove Astro files**

```bash
cd site && rm -f astro.config.mjs src/content.config.ts
rm -rf src/pages src/layouts src/components src/styles src/content
```

- [ ] **Step 3: Remove old package.json Astro deps**

The new `package.json` from Task 1 already replaced this. Verify it only has shadow-cljs and highlight.js:

```bash
cat site/package.json
```

- [ ] **Step 4: Commit**

```bash
git add -A site/
git commit -m "chore(site): remove Astro artifacts, project is now shadow-cljs"
```

---

## Task 16: Final Integration + Smoke Test

**Files:**
- Verify: all pages render
- Verify: shadow-cljs builds cleanly
- Verify: tests pass

- [ ] **Step 1: Run all tests**

```bash
cd site && npx shadow-cljs compile test
```

Expected: All tests pass (router, content, diagrams).

- [ ] **Step 2: Production build**

```bash
cd site && npx shadow-cljs release app
```

Expected: `resources/public/js/main.js` created, no errors.

- [ ] **Step 3: Verify file size is reasonable**

```bash
ls -lh site/resources/public/js/main.js
```

Expected: Under 500KB (Reagent + Reitit + Garden + Highlight.js with 4 languages).

- [ ] **Step 4: Start dev server and verify all routes**

```bash
cd site && npx shadow-cljs watch app &
sleep 5
curl -s http://localhost:3000 | grep -q '<div id="app">' && echo "PASS: index.html"
```

Manually verify in browser:
- `http://localhost:3000` — landing page with hex rain
- `http://localhost:3000#/docs` — docs index
- `http://localhost:3000#/docs/getting-started` — doc page with sidebar + TOC
- `http://localhost:3000#/labs` — labs index
- `http://localhost:3000#/labs/repl-driven-docs` — lab page
- `http://localhost:3000#/changelog` — changelog

- [ ] **Step 5: Run gate workflows on seed content**

```bash
glitch run workflows/site/gate-syntax.glitch
glitch run workflows/site/gate-structure.glitch
glitch run workflows/site/gate-links.glitch
```

Expected: All gates PASS on seed content.

- [ ] **Step 6: Commit any fixes**

```bash
git add -A && git commit -m "fix(site): integration fixes from smoke test"
```

- [ ] **Step 7: Final status check**

```bash
git status
```

Expected: Clean working tree.
