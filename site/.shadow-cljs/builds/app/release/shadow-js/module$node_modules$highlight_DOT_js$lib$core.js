["^ ","~:resource-id",["~:shadow.build.npm/resource","node_modules/highlight_DOT_js/lib/core.js"],"~:js","shadow$provide[9]=function(ha,za,va,D){function t(h){h instanceof Map?h.clear=h.delete=h.set=function(){throw Error(\"map is read-only\");}:h instanceof Set&&(h.add=h.clear=h.delete=function(){throw Error(\"set is read-only\");});Object.freeze(h);Object.getOwnPropertyNames(h).forEach(B=>{B=h[B];const U=typeof B;\"object\"!==U&&\"function\"!==U||Object.isFrozen(B)||t(B)});return h}function O(h){return h.replace(/&/g,\"\\x26amp;\").replace(/</g,\"\\x26lt;\").replace(/>/g,\"\\x26gt;\").replace(/\"/g,\"\\x26quot;\").replace(/'/g,\n\"\\x26#x27;\")}function ba(h,...B){const U=Object.create(null);for(const X in h)U[X]=h[X];B.forEach(function(X){for(const bb in X)U[bb]=X[bb]});return U}function na(h){return h?\"string\"===typeof h?h:h.source:null}function Ya(h){return Aa(\"(?\\x3d\",h,\")\")}function mb(h){return Aa(\"(?:\",h,\")*\")}function ja(h){return Aa(\"(?:\",h,\")?\")}function Aa(...h){return h.map(B=>na(B)).join(\"\")}function Za(h){const B=h[h.length-1];return\"object\"===typeof B&&B.constructor===Object?(h.splice(h.length-1,1),B):{}}function Ba(...h){return\"(\"+\n(Za(h).capture?\"\":\"?:\")+h.map(B=>na(B)).join(\"|\")+\")\"}function Ca(h,{joinWith:B}){let U=0;return h.map(X=>{const bb=U+=1;X=na(X);let Ia=\"\";for(;0<X.length;){const v=Vb.exec(X);if(!v){Ia+=X;break}Ia+=X.substring(0,v.index);X=X.substring(v.index+v[0].length);\"\\\\\"===v[0][0]&&v[1]?Ia+=\"\\\\\"+String(Number(v[1])+bb):(Ia+=v[0],\"(\"===v[0]&&U++)}return Ia}).map(X=>`(${X})`).join(B)}function Kb(h,B){\".\"===h.input[h.index-1]&&B.ignoreMatch()}function wb(h,B){void 0!==h.className&&(h.scope=h.className,delete h.className)}\nfunction lb(h,B){B&&h.beginKeywords&&(h.begin=\"\\\\b(\"+h.beginKeywords.split(\" \").join(\"|\")+\")(?!\\\\.)(?\\x3d\\\\b|\\\\s)\",h.__beforeBegin=Kb,h.keywords=h.keywords||h.beginKeywords,delete h.beginKeywords,void 0===h.relevance&&(h.relevance=0))}function $a(h,B){Array.isArray(h.illegal)&&(h.illegal=Ba(...h.illegal))}function Ha(h,B){if(h.match){if(h.begin||h.end)throw Error(\"begin \\x26 end are not supported with match\");h.begin=h.match;delete h.match}}function oc(h,B){void 0===h.relevance&&(h.relevance=1)}function Ma(h,\nB,U=\"keyword\"){function X(Ia,v){B&&(v=v.map(I=>I.toLowerCase()));v.forEach(function(I){var ka=I.split(\"|\");I=ka[0];var xa=ka[0];xa=(ka=ka[1])?Number(ka):R.includes(xa.toLowerCase())?0:1;bb[I]=[Ia,xa]})}const bb=Object.create(null);\"string\"===typeof h?X(U,h.split(\" \")):Array.isArray(h)?X(U,h):Object.keys(h).forEach(function(Ia){Object.assign(bb,Ma(h[Ia],B,Ia))});return bb}function pa(h,B,{key:U}){let X=0;const bb=h[U],Ia={},v={};for(let I=1;I<=B.length;I++)v[I+X]=bb[I],Ia[I+X]=!0,X+=(new RegExp(B[I-\n1].toString()+\"|\")).exec(\"\").length-1;h[U]=v;h[U]._emit=Ia;h[U]._multi=!0}function Lb(h){h.scope&&\"object\"===typeof h.scope&&null!==h.scope&&(h.beginScope=h.scope,delete h.scope);\"string\"===typeof h.beginScope&&(h.beginScope={_wrap:h.beginScope});\"string\"===typeof h.endScope&&(h.endScope={_wrap:h.endScope});if(Array.isArray(h.begin)){if(h.skip||h.excludeBegin||h.returnBegin)throw console.error(\"skip, excludeBegin, returnBegin not compatible with beginScope: {}\"),Z;if(\"object\"!==typeof h.beginScope||\nnull===h.beginScope)throw console.error(\"beginScope must be object\"),Z;pa(h,h.begin,{key:\"beginScope\"});h.begin=Ca(h.begin,{joinWith:\"\"})}if(Array.isArray(h.end)){if(h.skip||h.excludeEnd||h.returnEnd)throw console.error(\"skip, excludeEnd, returnEnd not compatible with endScope: {}\"),Z;if(\"object\"!==typeof h.endScope||null===h.endScope)throw console.error(\"endScope must be object\"),Z;pa(h,h.end,{key:\"endScope\"});h.end=Ca(h.end,{joinWith:\"\"})}}function ab(h){function B(v,I){return new RegExp(na(v),\n\"m\"+(h.case_insensitive?\"i\":\"\")+(h.unicodeRegex?\"u\":\"\")+(I?\"g\":\"\"))}function U(v){const I=new Ia;v.contains.forEach(ka=>I.addRule(ka.begin,{rule:ka,type:\"begin\"}));v.terminatorEnd&&I.addRule(v.terminatorEnd,{type:\"end\"});v.illegal&&I.addRule(v.illegal,{type:\"illegal\"});return I}function X(v,I){if(v.isCompiled)return v;[wb,Ha,Lb,N].forEach(xa=>xa(v,I));h.compilerExtensions.forEach(xa=>xa(v,I));v.__beforeBegin=null;[lb,$a,oc].forEach(xa=>xa(v,I));v.isCompiled=!0;let ka=null;\"object\"===typeof v.keywords&&\nv.keywords.$pattern&&(v.keywords=Object.assign({},v.keywords),ka=v.keywords.$pattern,delete v.keywords.$pattern);ka=ka||/\\w+/;v.keywords&&(v.keywords=Ma(v.keywords,h.case_insensitive));v.keywordPatternRe=B(ka,!0);I&&(v.begin||(v.begin=/\\B|\\b/),v.beginRe=B(v.begin),v.end||v.endsWithParent||(v.end=/\\B|\\b/),v.end&&(v.endRe=B(v.end)),v.terminatorEnd=na(v.end)||\"\",v.endsWithParent&&I.terminatorEnd&&(v.terminatorEnd+=(v.end?\"|\":\"\")+I.terminatorEnd));v.illegal&&(v.illegalRe=B(v.illegal));v.contains||(v.contains=\n[]);v.contains=[].concat(...v.contains.map(function(xa){return Ub(\"self\"===xa?v:xa)}));v.contains.forEach(function(xa){X(xa,v)});v.starts&&X(v.starts,I);v.matcher=U(v);return v}class bb{constructor(){this.matchIndexes={};this.regexes=[];this.matchAt=1;this.position=0}addRule(v,I){I.position=this.position++;this.matchIndexes[this.matchAt]=I;this.regexes.push([I,v]);this.matchAt+=(new RegExp(v.toString()+\"|\")).exec(\"\").length-1+1}compile(){0===this.regexes.length&&(this.exec=()=>null);const v=this.regexes.map(I=>\nI[1]);this.matcherRe=B(Ca(v,{joinWith:\"|\"}),!0);this.lastIndex=0}exec(v){this.matcherRe.lastIndex=this.lastIndex;v=this.matcherRe.exec(v);if(!v)return null;const I=v.findIndex((xa,Mc)=>0<Mc&&void 0!==xa),ka=this.matchIndexes[I];v.splice(0,I);return Object.assign(v,ka)}}class Ia{constructor(){this.rules=[];this.multiRegexes=[];this.regexIndex=this.lastIndex=this.count=0}getMatcher(v){if(this.multiRegexes[v])return this.multiRegexes[v];const I=new bb;this.rules.slice(v).forEach(([ka,xa])=>I.addRule(ka,\nxa));I.compile();return this.multiRegexes[v]=I}resumingScanAtSamePosition(){return 0!==this.regexIndex}considerAll(){this.regexIndex=0}addRule(v,I){this.rules.push([v,I]);\"begin\"===I.type&&this.count++}exec(v){var I=this.getMatcher(this.regexIndex);I.lastIndex=this.lastIndex;I=I.exec(v);!this.resumingScanAtSamePosition()||I&&I.index===this.lastIndex||(I=this.getMatcher(0),I.lastIndex=this.lastIndex+1,I=I.exec(v));I&&(this.regexIndex+=I.position+1,this.regexIndex===this.count&&this.considerAll());\nreturn I}}h.compilerExtensions||(h.compilerExtensions=[]);if(h.contains&&h.contains.includes(\"self\"))throw Error(\"ERR: contains `self` is not supported at the top-level of a language.  See documentation.\");h.classNameAliases=ba(h.classNameAliases||{});return X(h)}function kb(h){return h?h.endsWithParent||kb(h.starts):!1}function Ub(h){h.variants&&!h.cachedVariants&&(h.cachedVariants=h.variants.map(function(B){return ba(h,{variants:null},B)}));return h.cachedVariants?h.cachedVariants:kb(h)?ba(h,{starts:h.starts?\nba(h.starts):null}):Object.isFrozen(h)?ba(h):h}class Mb{constructor(h){void 0===h.data&&(h.data={});this.data=h.data;this.isMatchIgnored=!1}ignoreMatch(){this.isMatchIgnored=!0}}const Jb=(h,{prefix:B})=>h.startsWith(\"language:\")?h.replace(\"language:\",\"language-\"):h.includes(\".\")?(h=h.split(\".\"),[`${B}${h.shift()}`,...h.map((U,X)=>`${U}${\"_\".repeat(X+1)}`)].join(\" \")):`${B}${h}`;class ob{constructor(h,B){this.buffer=\"\";this.classPrefix=B.classPrefix;h.walk(this)}addText(h){this.buffer+=O(h)}openNode(h){h.scope&&\n(h=Jb(h.scope,{prefix:this.classPrefix}),this.span(h))}closeNode(h){h.scope&&(this.buffer+=\"\\x3c/span\\x3e\")}value(){return this.buffer}span(h){this.buffer+=`<span class=\"${h}\">`}}const qb=(h={})=>{const B={children:[]};Object.assign(B,h);return B};class Pa{constructor(){this.rootNode=qb();this.stack=[this.rootNode]}get top(){return this.stack[this.stack.length-1]}get root(){return this.rootNode}add(h){this.top.children.push(h)}openNode(h){h=qb({scope:h});this.add(h);this.stack.push(h)}closeNode(){if(1<\nthis.stack.length)return this.stack.pop()}closeAllNodes(){for(;this.closeNode(););}toJSON(){return JSON.stringify(this.rootNode,null,4)}walk(h){return this.constructor._walk(h,this.rootNode)}static _walk(h,B){\"string\"===typeof B?h.addText(B):B.children&&(h.openNode(B),B.children.forEach(U=>this._walk(h,U)),h.closeNode(B));return h}static _collapse(h){\"string\"!==typeof h&&h.children&&(h.children.every(B=>\"string\"===typeof B)?h.children=[h.children.join(\"\")]:h.children.forEach(B=>{Pa._collapse(B)}))}}\nclass Bb extends Pa{constructor(h){super();this.options=h}addText(h){\"\"!==h&&this.add(h)}startScope(h){this.openNode(h)}endScope(){this.closeNode()}__addSublanguage(h,B){h=h.root;B&&(h.scope=`language:${B}`);this.add(h)}toHTML(){return(new ob(this,this.options)).value()}finalize(){this.closeAllNodes();return!0}}const Vb=/\\[(?:[^\\\\\\]]|\\\\.)*\\]|\\(\\??|\\\\([1-9][0-9]*)|\\\\./;ha={begin:\"\\\\\\\\[\\\\s\\\\S]\",relevance:0};za={scope:\"string\",begin:\"'\",end:\"'\",illegal:\"\\\\n\",contains:[ha]};D={scope:\"string\",begin:'\"',\nend:'\"',illegal:\"\\\\n\",contains:[ha]};const nb=function(h,B,U={}){h=ba({scope:\"comment\",begin:h,end:B,contains:[]},U);h.contains.push({scope:\"doctag\",begin:\"[ ]*(?\\x3d(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):)\",end:/(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):/,excludeBegin:!0,relevance:0});B=Ba(\"I\",\"a\",\"is\",\"so\",\"us\",\"to\",\"at\",\"if\",\"in\",\"it\",\"on\",/[A-Za-z]+['](d|ve|re|ll|t|s|n)/,/[A-Za-z]+[-][a-z]+/,/[A-Za-z][a-z]{2,}/);h.contains.push({begin:Aa(/[ ]+/,\"(\",B,/[.]?[:]?([.][ ]|[ ])/,\"){3}\")});return h},\nDa=nb(\"//\",\"$\"),E=nb(\"/\\\\*\",\"\\\\*/\"),n=nb(\"#\",\"$\");var u=Object.freeze({__proto__:null,APOS_STRING_MODE:za,BACKSLASH_ESCAPE:ha,BINARY_NUMBER_MODE:{scope:\"number\",begin:\"\\\\b(0b[01]+)\",relevance:0},BINARY_NUMBER_RE:\"\\\\b(0b[01]+)\",COMMENT:nb,C_BLOCK_COMMENT_MODE:E,C_LINE_COMMENT_MODE:Da,C_NUMBER_MODE:{scope:\"number\",begin:\"(-?)(\\\\b0[xX][a-fA-F0-9]+|(\\\\b\\\\d+(\\\\.\\\\d*)?|\\\\.\\\\d+)([eE][-+]?\\\\d+)?)\",relevance:0},C_NUMBER_RE:\"(-?)(\\\\b0[xX][a-fA-F0-9]+|(\\\\b\\\\d+(\\\\.\\\\d*)?|\\\\.\\\\d+)([eE][-+]?\\\\d+)?)\",END_SAME_AS_BEGIN:function(h){return Object.assign(h,\n{\"on:begin\":(B,U)=>{U.data._beginMatch=B[1]},\"on:end\":(B,U)=>{U.data._beginMatch!==B[1]&&U.ignoreMatch()}})},HASH_COMMENT_MODE:n,IDENT_RE:\"[a-zA-Z]\\\\w*\",MATCH_NOTHING_RE:/\\b\\B/,METHOD_GUARD:{begin:\"\\\\.\\\\s*[a-zA-Z_]\\\\w*\",relevance:0},NUMBER_MODE:{scope:\"number\",begin:\"\\\\b\\\\d+(\\\\.\\\\d+)?\",relevance:0},NUMBER_RE:\"\\\\b\\\\d+(\\\\.\\\\d+)?\",PHRASAL_WORDS_MODE:{begin:/\\b(a|an|the|are|I'm|isn't|don't|doesn't|won't|but|just|should|pretty|simply|enough|gonna|going|wtf|so|such|will|you|your|they|like|more)\\b/},QUOTE_STRING_MODE:D,\nREGEXP_MODE:{scope:\"regexp\",begin:/\\/(?=[^/\\n]*\\/)/,end:/\\/[gimuy]*/,contains:[ha,{begin:/\\[/,end:/\\]/,relevance:0,contains:[ha]}]},RE_STARTERS_RE:\"!|!\\x3d|!\\x3d\\x3d|%|%\\x3d|\\x26|\\x26\\x26|\\x26\\x3d|\\\\*|\\\\*\\x3d|\\\\+|\\\\+\\x3d|,|-|-\\x3d|/\\x3d|/|:|;|\\x3c\\x3c|\\x3c\\x3c\\x3d|\\x3c\\x3d|\\x3c|\\x3d\\x3d\\x3d|\\x3d\\x3d|\\x3d|\\x3e\\x3e\\x3e\\x3d|\\x3e\\x3e\\x3d|\\x3e\\x3d|\\x3e\\x3e\\x3e|\\x3e\\x3e|\\x3e|\\\\?|\\\\[|\\\\{|\\\\(|\\\\^|\\\\^\\x3d|\\\\||\\\\|\\x3d|\\\\|\\\\||~\",SHEBANG:(h={})=>{const B=/^#![ ]*\\//;h.binary&&(h.begin=Aa(B,/.*\\b/,h.binary,/\\b.*/));\nreturn ba({scope:\"meta\",begin:B,end:/$/,relevance:0,\"on:begin\":(U,X)=>{0!==U.index&&X.ignoreMatch()}},h)},TITLE_MODE:{scope:\"title\",begin:\"[a-zA-Z]\\\\w*\",relevance:0},UNDERSCORE_IDENT_RE:\"[a-zA-Z_]\\\\w*\",UNDERSCORE_TITLE_MODE:{scope:\"title\",begin:\"[a-zA-Z_]\\\\w*\",relevance:0}});const N=(h,B)=>{if(h.beforeMatch){if(h.starts)throw Error(\"beforeMatch cannot be used with starts\");B=Object.assign({},h);Object.keys(h).forEach(U=>{delete h[U]});h.keywords=B.keywords;h.begin=Aa(B.beforeMatch,Ya(B.begin));h.starts=\n{relevance:0,contains:[Object.assign(B,{endsParent:!0})]};h.relevance=0;delete B.beforeMatch}},R=\"of and for in not or if then parent list value\".split(\" \"),W={},ca=(h,...B)=>{console.log(`WARN: ${h}`,...B)},da=(h,B)=>{W[`${h}/${B}`]||(console.log(`Deprecated as of ${h}. ${B}`),W[`${h}/${B}`]=!0)},Z=Error();class ia extends Error{constructor(h,B){super(h);this.name=\"HTMLInjectionError\";this.html=B}}const Ta=Symbol(\"nomatch\"),Id=function(h){function B(z){var G=z.className+\" \";G+=z.parentNode?z.parentNode.className:\n\"\";const V=Va.languageDetectRe.exec(G);return V?(G=ka(V[1]),G||(ca(\"Could not find the language '{}', did you forget to load/include a language module?\".replace(\"{}\",V[1])),ca(\"Falling back to no-highlight mode for this block.\",z)),G?V[1]:\"no-highlight\"):G.split(/\\s+/).find(aa=>Va.noHighlightRe.test(aa)||ka(aa))}function U(z,G,V){let aa;\"object\"===typeof G?(V=G.ignoreIllegals,aa=G.language):(da(\"10.7.0\",\"highlight(lang, code, ...args) has been deprecated.\"),da(\"10.7.0\",\"Please use highlight(code, options) instead.\\nhttps://github.com/highlightjs/highlight.js/issues/2277\"),\naa=z,z=G);void 0===V&&(V=!0);G={code:z,language:aa};gd(\"before:highlight\",G);V=G.result?G.result:X(G.language,G.code,V);V.code=G.code;gd(\"after:highlight\",V);return V}function X(z,G,V,aa){function Oc(){if(T.keywords){var A=0;T.keywordPatternRe.lastIndex=0;for(var K=T.keywordPatternRe.exec(ma),Y=\"\";K;){Y+=ma.substring(A,K.index);A=eb.case_insensitive?K[0].toLowerCase():K[0];const Ea=T.keywords[A];if(Ea){const [ra,wc]=Ea;Ka.addText(Y);Y=\"\";Pd[A]=(Pd[A]||0)+1;7>=Pd[A]&&(dc+=wc);ra.startsWith(\"_\")?Y+=\nK[0]:db(K[0],eb.classNameAliases[ra]||ra)}else Y+=K[0];A=T.keywordPatternRe.lastIndex;K=T.keywordPatternRe.exec(ma)}Y+=ma.substring(A);Ka.addText(Y)}else Ka.addText(ma)}function cb(){if(null!=T.subLanguage)a:{if(\"\"!==ma){if(\"string\"===typeof T.subLanguage){if(!bc[T.subLanguage]){Ka.addText(ma);break a}var A=X(T.subLanguage,ma,!0,hd[T.subLanguage]);hd[T.subLanguage]=A._top}else A=Ia(ma,T.subLanguage.length?T.subLanguage:null);0<T.relevance&&(dc+=A.relevance);Ka.__addSublanguage(A._emitter,A.language)}}else Oc();\nma=\"\"}function db(A,K){\"\"!==A&&(Ka.startScope(K),Ka.addText(A),Ka.endScope())}function ye(A,K){let Y=1;const Ea=K.length-1;for(;Y<=Ea;){if(!A._emit[Y]){Y++;continue}const ra=eb.classNameAliases[A[Y]]||A[Y],wc=K[Y];ra?db(wc,ra):(ma=wc,Oc(),ma=\"\");Y++}}function Nd(A,K){A.scope&&\"string\"===typeof A.scope&&Ka.openNode(eb.classNameAliases[A.scope]||A.scope);A.beginScope&&(A.beginScope._wrap?(db(ma,eb.classNameAliases[A.beginScope._wrap]||A.beginScope._wrap),ma=\"\"):A.beginScope._multi&&(ye(A.beginScope,\nK),ma=\"\"));return T=Object.create(A,{parent:{value:T}})}function Qc(A,K,Y){var Ea=A.endRe;if(Ea=(Ea=Ea&&Ea.exec(Y))&&0===Ea.index){if(A[\"on:end\"]){const ra=new Mb(A);A[\"on:end\"](K,ra);ra.isMatchIgnored&&(Ea=!1)}if(Ea){for(;A.endsParent&&A.parent;)A=A.parent;return A}}if(A.endsWithParent)return Qc(A.parent,K,Y)}function Od(A,K){var Y=K&&K[0];ma+=A;if(null==Y)return cb(),0;if(\"begin\"===Xb.type&&\"end\"===K.type&&Xb.index===K.index&&\"\"===Y){ma+=G.slice(K.index,K.index+1);if(!Nc)throw K=Error(`0 width match regex (${z})`),\nK.languageName=z,K.badRule=Xb.rule,K;return 1}Xb=K;if(\"begin\"===K.type){a:{Y=K[0];A=K.rule;var Ea=new Mb(A);const wc=[A.__beforeBegin,A[\"on:begin\"]];for(var ra of wc)if(ra&&(ra(K,Ea),Ea.isMatchIgnored)){0===T.matcher.regexIndex?(ma+=Y[0],K=1):(jd=!0,K=0);break a}A.skip?ma+=Y:(A.excludeBegin&&(ma+=Y),cb(),A.returnBegin||A.excludeBegin||(ma=Y));Nd(A,K);K=A.returnBegin?0:Y.length}return K}if(\"illegal\"===K.type&&!V)throw K=Error('Illegal lexeme \"'+Y+'\" for mode \"'+(T.scope||\"\\x3cunnamed\\x3e\")+'\"'),K.mode=\nT,K;if(\"end\"===K.type){ra=K[0];A=G.substring(K.index);if(Ea=Qc(T,K,A)){A=T;T.endScope&&T.endScope._wrap?(cb(),db(ra,T.endScope._wrap)):T.endScope&&T.endScope._multi?(cb(),ye(T.endScope,K)):A.skip?ma+=ra:(A.returnEnd||A.excludeEnd||(ma+=ra),cb(),A.excludeEnd&&(ma=ra));do T.scope&&Ka.closeNode(),T.skip||T.subLanguage||(dc+=T.relevance),T=T.parent;while(T!==Ea.parent);Ea.starts&&Nd(Ea.starts,K);ra=A.returnEnd?0:ra.length}else ra=Ta;if(ra!==Ta)return ra}if(\"illegal\"===K.type&&\"\"===Y)return ma+=\"\\n\",1;\nif(1E5<vc&&vc>3*K.index)throw Error(\"potential infinite loop, way more iterations than matches\");ma+=Y;return Y.length}const Pd=Object.create(null);let Xb={};const eb=ka(z);if(!eb)throw console.error(\"Could not find the language '{}', did you forget to load/include a language module?\".replace(\"{}\",z)),Error('Unknown language: \"'+z+'\"');const Pf=ab(eb);let Qd=\"\",T=aa||Pf;const hd={},Ka=new Va.__emitter(Va);(function(){const A=[];for(let K=T;K!==eb;K=K.parent)K.scope&&A.unshift(K.scope);A.forEach(K=>\nKa.openNode(K))})();let ma=\"\",dc=0,vc=aa=0,jd=!1;try{if(eb.__emitTokens)eb.__emitTokens(G,Ka);else{for(T.matcher.considerAll();;){vc++;jd?jd=!1:T.matcher.considerAll();T.matcher.lastIndex=aa;const A=T.matcher.exec(G);if(!A)break;const K=G.substring(aa,A.index),Y=Od(K,A);aa=A.index+Y}Od(G.substring(aa))}Ka.finalize();Qd=Ka.toHTML();return{language:z,value:Qd,relevance:dc,illegal:!1,_emitter:Ka,_top:T}}catch(A){if(A.message&&A.message.includes(\"Illegal\"))return{language:z,value:O(G),illegal:!0,relevance:0,\n_illegalBy:{message:A.message,index:aa,context:G.slice(aa-100,aa+100),mode:A.mode,resultSoFar:Qd},_emitter:Ka};if(Nc)return{language:z,value:O(G),illegal:!1,relevance:0,errorRaised:A,_emitter:Ka,_top:T};throw A;}}function bb(z){const G={value:O(z),illegal:!1,relevance:0,_top:we,_emitter:new Va.__emitter(Va)};G._emitter.addText(z);return G}function Ia(z,G){G=G||Va.languages||Object.keys(bc);var V=bb(z);G=G.filter(ka).filter(Mc).map(cb=>X(cb,z,!1));G.unshift(V);V=G.sort((cb,db)=>{if(cb.relevance!==\ndb.relevance)return db.relevance-cb.relevance;if(cb.language&&db.language){if(ka(cb.language).supersetOf===db.language)return 1;if(ka(db.language).supersetOf===cb.language)return-1}return 0});const [aa,Oc]=V;aa.secondBest=Oc;return aa}function v(z){var G=B(z);if(!Va.noHighlightRe.test(G))if(gd(\"before:highlightElement\",{el:z,language:G}),z.dataset.highlighted)console.log(\"Element previously highlighted. To highlight again, first unset `dataset.highlighted`.\",z);else{if(0<z.children.length&&(Va.ignoreUnescapedHTML||\n(console.warn(\"One of your code blocks includes unescaped HTML. This is a potentially serious security risk.\"),console.warn(\"https://github.com/highlightjs/highlight.js/wiki/security\"),console.warn(\"The element with unescaped HTML:\"),console.warn(z)),Va.throwUnescapedHTML))throw new ia(\"One of your code blocks includes unescaped HTML.\",z.innerHTML);var V=z.textContent,aa=G?U(V,{language:G,ignoreIllegals:!0}):Ia(V);z.innerHTML=aa.value;z.dataset.highlighted=\"yes\";G=G&&Cb[G]||aa.language;z.classList.add(\"hljs\");\nz.classList.add(`language-${G}`);z.result={language:aa.language,re:aa.relevance,relevance:aa.relevance};aa.secondBest&&(z.secondBest={language:aa.secondBest.language,relevance:aa.secondBest.relevance});gd(\"after:highlightElement\",{el:z,result:aa,text:V})}}function I(){function z(){I()}\"loading\"===document.readyState?(Kd||window.addEventListener(\"DOMContentLoaded\",z,!1),Kd=!0):document.querySelectorAll(Va.cssSelector).forEach(v)}function ka(z){z=(z||\"\").toLowerCase();return bc[z]||bc[Cb[z]]}function xa(z,\n{languageName:G}){\"string\"===typeof z&&(z=[z]);z.forEach(V=>{Cb[V.toLowerCase()]=G})}function Mc(z){return(z=ka(z))&&!z.disableAutodetect}function ue(z){z[\"before:highlightBlock\"]&&!z[\"before:highlightElement\"]&&(z[\"before:highlightElement\"]=G=>{z[\"before:highlightBlock\"](Object.assign({block:G.el},G))});z[\"after:highlightBlock\"]&&!z[\"after:highlightElement\"]&&(z[\"after:highlightElement\"]=G=>{z[\"after:highlightBlock\"](Object.assign({block:G.el},G))})}function gd(z,G){cc.forEach(function(V){if(V[z])V[z](G)})}\nconst bc=Object.create(null),Cb=Object.create(null),cc=[];let Nc=!0;const we={disableAutodetect:!0,name:\"Plain text\",contains:[]};let Va={ignoreUnescapedHTML:!1,throwUnescapedHTML:!1,noHighlightRe:/^(no-?highlight)$/i,languageDetectRe:/\\blang(?:uage)?-([\\w-]+)\\b/i,classPrefix:\"hljs-\",cssSelector:\"pre code\",languages:null,__emitter:Bb},Kd=!1;Object.assign(h,{highlight:U,highlightAuto:Ia,highlightAll:I,highlightElement:v,highlightBlock:function(z){da(\"10.7.0\",\"highlightBlock will be removed entirely in v12.0\");\nda(\"10.7.0\",\"Please use highlightElement now.\");return v(z)},configure:function(z){Va=ba(Va,z)},initHighlighting:()=>{I();da(\"10.6.0\",\"initHighlighting() deprecated.  Use highlightAll() now.\")},initHighlightingOnLoad:function(){I();da(\"10.6.0\",\"initHighlightingOnLoad() deprecated.  Use highlightAll() now.\")},registerLanguage:function(z,G){let V=null;try{V=G(h)}catch(aa){console.error(\"Language definition for '{}' could not be registered.\".replace(\"{}\",z));if(Nc)console.error(aa);else throw aa;V=we}V.name||\n(V.name=z);bc[z]=V;V.rawDefinition=G.bind(null,h);V.aliases&&xa(V.aliases,{languageName:z})},unregisterLanguage:function(z){delete bc[z];for(const G of Object.keys(Cb))Cb[G]===z&&delete Cb[G]},listLanguages:function(){return Object.keys(bc)},getLanguage:ka,registerAliases:xa,autoDetection:Mc,inherit:ba,addPlugin:function(z){ue(z);cc.push(z)},removePlugin:function(z){z=cc.indexOf(z);-1!==z&&cc.splice(z,1)}});h.debugMode=function(){Nc=!1};h.safeMode=function(){Nc=!0};h.versionString=\"11.11.1\";h.regex=\n{concat:Aa,lookahead:Ya,either:Ba,optional:ja,anyNumberOfTimes:mb};for(const z in u)\"object\"===typeof u[z]&&t(u[z]);Object.assign(h,u);return h};ha=Id({});ha.newInstance=()=>Id({});va.exports=ha;ha.HighlightJS=ha;ha.default=ha}","~:source","shadow$provide[9] = function(global,require,module,exports) {\n/* eslint-disable no-multi-assign */\n\nfunction deepFreeze(obj) {\n  if (obj instanceof Map) {\n    obj.clear =\n      obj.delete =\n      obj.set =\n        function () {\n          throw new Error('map is read-only');\n        };\n  } else if (obj instanceof Set) {\n    obj.add =\n      obj.clear =\n      obj.delete =\n        function () {\n          throw new Error('set is read-only');\n        };\n  }\n\n  // Freeze self\n  Object.freeze(obj);\n\n  Object.getOwnPropertyNames(obj).forEach((name) => {\n    const prop = obj[name];\n    const type = typeof prop;\n\n    // Freeze prop if it is an object or function and also not already frozen\n    if ((type === 'object' || type === 'function') && !Object.isFrozen(prop)) {\n      deepFreeze(prop);\n    }\n  });\n\n  return obj;\n}\n\n/** @typedef {import('highlight.js').CallbackResponse} CallbackResponse */\n/** @typedef {import('highlight.js').CompiledMode} CompiledMode */\n/** @implements CallbackResponse */\n\nclass Response {\n  /**\n   * @param {CompiledMode} mode\n   */\n  constructor(mode) {\n    // eslint-disable-next-line no-undefined\n    if (mode.data === undefined) mode.data = {};\n\n    this.data = mode.data;\n    this.isMatchIgnored = false;\n  }\n\n  ignoreMatch() {\n    this.isMatchIgnored = true;\n  }\n}\n\n/**\n * @param {string} value\n * @returns {string}\n */\nfunction escapeHTML(value) {\n  return value\n    .replace(/&/g, '&amp;')\n    .replace(/</g, '&lt;')\n    .replace(/>/g, '&gt;')\n    .replace(/\"/g, '&quot;')\n    .replace(/'/g, '&#x27;');\n}\n\n/**\n * performs a shallow merge of multiple objects into one\n *\n * @template T\n * @param {T} original\n * @param {Record<string,any>[]} objects\n * @returns {T} a single new object\n */\nfunction inherit$1(original, ...objects) {\n  /** @type Record<string,any> */\n  const result = Object.create(null);\n\n  for (const key in original) {\n    result[key] = original[key];\n  }\n  objects.forEach(function(obj) {\n    for (const key in obj) {\n      result[key] = obj[key];\n    }\n  });\n  return /** @type {T} */ (result);\n}\n\n/**\n * @typedef {object} Renderer\n * @property {(text: string) => void} addText\n * @property {(node: Node) => void} openNode\n * @property {(node: Node) => void} closeNode\n * @property {() => string} value\n */\n\n/** @typedef {{scope?: string, language?: string, sublanguage?: boolean}} Node */\n/** @typedef {{walk: (r: Renderer) => void}} Tree */\n/** */\n\nconst SPAN_CLOSE = '</span>';\n\n/**\n * Determines if a node needs to be wrapped in <span>\n *\n * @param {Node} node */\nconst emitsWrappingTags = (node) => {\n  // rarely we can have a sublanguage where language is undefined\n  // TODO: track down why\n  return !!node.scope;\n};\n\n/**\n *\n * @param {string} name\n * @param {{prefix:string}} options\n */\nconst scopeToCSSClass = (name, { prefix }) => {\n  // sub-language\n  if (name.startsWith(\"language:\")) {\n    return name.replace(\"language:\", \"language-\");\n  }\n  // tiered scope: comment.line\n  if (name.includes(\".\")) {\n    const pieces = name.split(\".\");\n    return [\n      `${prefix}${pieces.shift()}`,\n      ...(pieces.map((x, i) => `${x}${\"_\".repeat(i + 1)}`))\n    ].join(\" \");\n  }\n  // simple scope\n  return `${prefix}${name}`;\n};\n\n/** @type {Renderer} */\nclass HTMLRenderer {\n  /**\n   * Creates a new HTMLRenderer\n   *\n   * @param {Tree} parseTree - the parse tree (must support `walk` API)\n   * @param {{classPrefix: string}} options\n   */\n  constructor(parseTree, options) {\n    this.buffer = \"\";\n    this.classPrefix = options.classPrefix;\n    parseTree.walk(this);\n  }\n\n  /**\n   * Adds texts to the output stream\n   *\n   * @param {string} text */\n  addText(text) {\n    this.buffer += escapeHTML(text);\n  }\n\n  /**\n   * Adds a node open to the output stream (if needed)\n   *\n   * @param {Node} node */\n  openNode(node) {\n    if (!emitsWrappingTags(node)) return;\n\n    const className = scopeToCSSClass(node.scope,\n      { prefix: this.classPrefix });\n    this.span(className);\n  }\n\n  /**\n   * Adds a node close to the output stream (if needed)\n   *\n   * @param {Node} node */\n  closeNode(node) {\n    if (!emitsWrappingTags(node)) return;\n\n    this.buffer += SPAN_CLOSE;\n  }\n\n  /**\n   * returns the accumulated buffer\n  */\n  value() {\n    return this.buffer;\n  }\n\n  // helpers\n\n  /**\n   * Builds a span element\n   *\n   * @param {string} className */\n  span(className) {\n    this.buffer += `<span class=\"${className}\">`;\n  }\n}\n\n/** @typedef {{scope?: string, language?: string, children: Node[]} | string} Node */\n/** @typedef {{scope?: string, language?: string, children: Node[]} } DataNode */\n/** @typedef {import('highlight.js').Emitter} Emitter */\n/**  */\n\n/** @returns {DataNode} */\nconst newNode = (opts = {}) => {\n  /** @type DataNode */\n  const result = { children: [] };\n  Object.assign(result, opts);\n  return result;\n};\n\nclass TokenTree {\n  constructor() {\n    /** @type DataNode */\n    this.rootNode = newNode();\n    this.stack = [this.rootNode];\n  }\n\n  get top() {\n    return this.stack[this.stack.length - 1];\n  }\n\n  get root() { return this.rootNode; }\n\n  /** @param {Node} node */\n  add(node) {\n    this.top.children.push(node);\n  }\n\n  /** @param {string} scope */\n  openNode(scope) {\n    /** @type Node */\n    const node = newNode({ scope });\n    this.add(node);\n    this.stack.push(node);\n  }\n\n  closeNode() {\n    if (this.stack.length > 1) {\n      return this.stack.pop();\n    }\n    // eslint-disable-next-line no-undefined\n    return undefined;\n  }\n\n  closeAllNodes() {\n    while (this.closeNode());\n  }\n\n  toJSON() {\n    return JSON.stringify(this.rootNode, null, 4);\n  }\n\n  /**\n   * @typedef { import(\"./html_renderer\").Renderer } Renderer\n   * @param {Renderer} builder\n   */\n  walk(builder) {\n    // this does not\n    return this.constructor._walk(builder, this.rootNode);\n    // this works\n    // return TokenTree._walk(builder, this.rootNode);\n  }\n\n  /**\n   * @param {Renderer} builder\n   * @param {Node} node\n   */\n  static _walk(builder, node) {\n    if (typeof node === \"string\") {\n      builder.addText(node);\n    } else if (node.children) {\n      builder.openNode(node);\n      node.children.forEach((child) => this._walk(builder, child));\n      builder.closeNode(node);\n    }\n    return builder;\n  }\n\n  /**\n   * @param {Node} node\n   */\n  static _collapse(node) {\n    if (typeof node === \"string\") return;\n    if (!node.children) return;\n\n    if (node.children.every(el => typeof el === \"string\")) {\n      // node.text = node.children.join(\"\");\n      // delete node.children;\n      node.children = [node.children.join(\"\")];\n    } else {\n      node.children.forEach((child) => {\n        TokenTree._collapse(child);\n      });\n    }\n  }\n}\n\n/**\n  Currently this is all private API, but this is the minimal API necessary\n  that an Emitter must implement to fully support the parser.\n\n  Minimal interface:\n\n  - addText(text)\n  - __addSublanguage(emitter, subLanguageName)\n  - startScope(scope)\n  - endScope()\n  - finalize()\n  - toHTML()\n\n*/\n\n/**\n * @implements {Emitter}\n */\nclass TokenTreeEmitter extends TokenTree {\n  /**\n   * @param {*} options\n   */\n  constructor(options) {\n    super();\n    this.options = options;\n  }\n\n  /**\n   * @param {string} text\n   */\n  addText(text) {\n    if (text === \"\") { return; }\n\n    this.add(text);\n  }\n\n  /** @param {string} scope */\n  startScope(scope) {\n    this.openNode(scope);\n  }\n\n  endScope() {\n    this.closeNode();\n  }\n\n  /**\n   * @param {Emitter & {root: DataNode}} emitter\n   * @param {string} name\n   */\n  __addSublanguage(emitter, name) {\n    /** @type DataNode */\n    const node = emitter.root;\n    if (name) node.scope = `language:${name}`;\n\n    this.add(node);\n  }\n\n  toHTML() {\n    const renderer = new HTMLRenderer(this, this.options);\n    return renderer.value();\n  }\n\n  finalize() {\n    this.closeAllNodes();\n    return true;\n  }\n}\n\n/**\n * @param {string} value\n * @returns {RegExp}\n * */\n\n/**\n * @param {RegExp | string } re\n * @returns {string}\n */\nfunction source(re) {\n  if (!re) return null;\n  if (typeof re === \"string\") return re;\n\n  return re.source;\n}\n\n/**\n * @param {RegExp | string } re\n * @returns {string}\n */\nfunction lookahead(re) {\n  return concat('(?=', re, ')');\n}\n\n/**\n * @param {RegExp | string } re\n * @returns {string}\n */\nfunction anyNumberOfTimes(re) {\n  return concat('(?:', re, ')*');\n}\n\n/**\n * @param {RegExp | string } re\n * @returns {string}\n */\nfunction optional(re) {\n  return concat('(?:', re, ')?');\n}\n\n/**\n * @param {...(RegExp | string) } args\n * @returns {string}\n */\nfunction concat(...args) {\n  const joined = args.map((x) => source(x)).join(\"\");\n  return joined;\n}\n\n/**\n * @param { Array<string | RegExp | Object> } args\n * @returns {object}\n */\nfunction stripOptionsFromArgs(args) {\n  const opts = args[args.length - 1];\n\n  if (typeof opts === 'object' && opts.constructor === Object) {\n    args.splice(args.length - 1, 1);\n    return opts;\n  } else {\n    return {};\n  }\n}\n\n/** @typedef { {capture?: boolean} } RegexEitherOptions */\n\n/**\n * Any of the passed expresssions may match\n *\n * Creates a huge this | this | that | that match\n * @param {(RegExp | string)[] | [...(RegExp | string)[], RegexEitherOptions]} args\n * @returns {string}\n */\nfunction either(...args) {\n  /** @type { object & {capture?: boolean} }  */\n  const opts = stripOptionsFromArgs(args);\n  const joined = '('\n    + (opts.capture ? \"\" : \"?:\")\n    + args.map((x) => source(x)).join(\"|\") + \")\";\n  return joined;\n}\n\n/**\n * @param {RegExp | string} re\n * @returns {number}\n */\nfunction countMatchGroups(re) {\n  return (new RegExp(re.toString() + '|')).exec('').length - 1;\n}\n\n/**\n * Does lexeme start with a regular expression match at the beginning\n * @param {RegExp} re\n * @param {string} lexeme\n */\nfunction startsWith(re, lexeme) {\n  const match = re && re.exec(lexeme);\n  return match && match.index === 0;\n}\n\n// BACKREF_RE matches an open parenthesis or backreference. To avoid\n// an incorrect parse, it additionally matches the following:\n// - [...] elements, where the meaning of parentheses and escapes change\n// - other escape sequences, so we do not misparse escape sequences as\n//   interesting elements\n// - non-matching or lookahead parentheses, which do not capture. These\n//   follow the '(' with a '?'.\nconst BACKREF_RE = /\\[(?:[^\\\\\\]]|\\\\.)*\\]|\\(\\??|\\\\([1-9][0-9]*)|\\\\./;\n\n// **INTERNAL** Not intended for outside usage\n// join logically computes regexps.join(separator), but fixes the\n// backreferences so they continue to match.\n// it also places each individual regular expression into it's own\n// match group, keeping track of the sequencing of those match groups\n// is currently an exercise for the caller. :-)\n/**\n * @param {(string | RegExp)[]} regexps\n * @param {{joinWith: string}} opts\n * @returns {string}\n */\nfunction _rewriteBackreferences(regexps, { joinWith }) {\n  let numCaptures = 0;\n\n  return regexps.map((regex) => {\n    numCaptures += 1;\n    const offset = numCaptures;\n    let re = source(regex);\n    let out = '';\n\n    while (re.length > 0) {\n      const match = BACKREF_RE.exec(re);\n      if (!match) {\n        out += re;\n        break;\n      }\n      out += re.substring(0, match.index);\n      re = re.substring(match.index + match[0].length);\n      if (match[0][0] === '\\\\' && match[1]) {\n        // Adjust the backreference.\n        out += '\\\\' + String(Number(match[1]) + offset);\n      } else {\n        out += match[0];\n        if (match[0] === '(') {\n          numCaptures++;\n        }\n      }\n    }\n    return out;\n  }).map(re => `(${re})`).join(joinWith);\n}\n\n/** @typedef {import('highlight.js').Mode} Mode */\n/** @typedef {import('highlight.js').ModeCallback} ModeCallback */\n\n// Common regexps\nconst MATCH_NOTHING_RE = /\\b\\B/;\nconst IDENT_RE = '[a-zA-Z]\\\\w*';\nconst UNDERSCORE_IDENT_RE = '[a-zA-Z_]\\\\w*';\nconst NUMBER_RE = '\\\\b\\\\d+(\\\\.\\\\d+)?';\nconst C_NUMBER_RE = '(-?)(\\\\b0[xX][a-fA-F0-9]+|(\\\\b\\\\d+(\\\\.\\\\d*)?|\\\\.\\\\d+)([eE][-+]?\\\\d+)?)'; // 0x..., 0..., decimal, float\nconst BINARY_NUMBER_RE = '\\\\b(0b[01]+)'; // 0b...\nconst RE_STARTERS_RE = '!|!=|!==|%|%=|&|&&|&=|\\\\*|\\\\*=|\\\\+|\\\\+=|,|-|-=|/=|/|:|;|<<|<<=|<=|<|===|==|=|>>>=|>>=|>=|>>>|>>|>|\\\\?|\\\\[|\\\\{|\\\\(|\\\\^|\\\\^=|\\\\||\\\\|=|\\\\|\\\\||~';\n\n/**\n* @param { Partial<Mode> & {binary?: string | RegExp} } opts\n*/\nconst SHEBANG = (opts = {}) => {\n  const beginShebang = /^#![ ]*\\//;\n  if (opts.binary) {\n    opts.begin = concat(\n      beginShebang,\n      /.*\\b/,\n      opts.binary,\n      /\\b.*/);\n  }\n  return inherit$1({\n    scope: 'meta',\n    begin: beginShebang,\n    end: /$/,\n    relevance: 0,\n    /** @type {ModeCallback} */\n    \"on:begin\": (m, resp) => {\n      if (m.index !== 0) resp.ignoreMatch();\n    }\n  }, opts);\n};\n\n// Common modes\nconst BACKSLASH_ESCAPE = {\n  begin: '\\\\\\\\[\\\\s\\\\S]', relevance: 0\n};\nconst APOS_STRING_MODE = {\n  scope: 'string',\n  begin: '\\'',\n  end: '\\'',\n  illegal: '\\\\n',\n  contains: [BACKSLASH_ESCAPE]\n};\nconst QUOTE_STRING_MODE = {\n  scope: 'string',\n  begin: '\"',\n  end: '\"',\n  illegal: '\\\\n',\n  contains: [BACKSLASH_ESCAPE]\n};\nconst PHRASAL_WORDS_MODE = {\n  begin: /\\b(a|an|the|are|I'm|isn't|don't|doesn't|won't|but|just|should|pretty|simply|enough|gonna|going|wtf|so|such|will|you|your|they|like|more)\\b/\n};\n/**\n * Creates a comment mode\n *\n * @param {string | RegExp} begin\n * @param {string | RegExp} end\n * @param {Mode | {}} [modeOptions]\n * @returns {Partial<Mode>}\n */\nconst COMMENT = function(begin, end, modeOptions = {}) {\n  const mode = inherit$1(\n    {\n      scope: 'comment',\n      begin,\n      end,\n      contains: []\n    },\n    modeOptions\n  );\n  mode.contains.push({\n    scope: 'doctag',\n    // hack to avoid the space from being included. the space is necessary to\n    // match here to prevent the plain text rule below from gobbling up doctags\n    begin: '[ ]*(?=(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):)',\n    end: /(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):/,\n    excludeBegin: true,\n    relevance: 0\n  });\n  const ENGLISH_WORD = either(\n    // list of common 1 and 2 letter words in English\n    \"I\",\n    \"a\",\n    \"is\",\n    \"so\",\n    \"us\",\n    \"to\",\n    \"at\",\n    \"if\",\n    \"in\",\n    \"it\",\n    \"on\",\n    // note: this is not an exhaustive list of contractions, just popular ones\n    /[A-Za-z]+['](d|ve|re|ll|t|s|n)/, // contractions - can't we'd they're let's, etc\n    /[A-Za-z]+[-][a-z]+/, // `no-way`, etc.\n    /[A-Za-z][a-z]{2,}/ // allow capitalized words at beginning of sentences\n  );\n  // looking like plain text, more likely to be a comment\n  mode.contains.push(\n    {\n      // TODO: how to include \", (, ) without breaking grammars that use these for\n      // comment delimiters?\n      // begin: /[ ]+([()\"]?([A-Za-z'-]{3,}|is|a|I|so|us|[tT][oO]|at|if|in|it|on)[.]?[()\":]?([.][ ]|[ ]|\\))){3}/\n      // ---\n\n      // this tries to find sequences of 3 english words in a row (without any\n      // \"programming\" type syntax) this gives us a strong signal that we've\n      // TRULY found a comment - vs perhaps scanning with the wrong language.\n      // It's possible to find something that LOOKS like the start of the\n      // comment - but then if there is no readable text - good chance it is a\n      // false match and not a comment.\n      //\n      // for a visual example please see:\n      // https://github.com/highlightjs/highlight.js/issues/2827\n\n      begin: concat(\n        /[ ]+/, // necessary to prevent us gobbling up doctags like /* @author Bob Mcgill */\n        '(',\n        ENGLISH_WORD,\n        /[.]?[:]?([.][ ]|[ ])/,\n        '){3}') // look for 3 words in a row\n    }\n  );\n  return mode;\n};\nconst C_LINE_COMMENT_MODE = COMMENT('//', '$');\nconst C_BLOCK_COMMENT_MODE = COMMENT('/\\\\*', '\\\\*/');\nconst HASH_COMMENT_MODE = COMMENT('#', '$');\nconst NUMBER_MODE = {\n  scope: 'number',\n  begin: NUMBER_RE,\n  relevance: 0\n};\nconst C_NUMBER_MODE = {\n  scope: 'number',\n  begin: C_NUMBER_RE,\n  relevance: 0\n};\nconst BINARY_NUMBER_MODE = {\n  scope: 'number',\n  begin: BINARY_NUMBER_RE,\n  relevance: 0\n};\nconst REGEXP_MODE = {\n  scope: \"regexp\",\n  begin: /\\/(?=[^/\\n]*\\/)/,\n  end: /\\/[gimuy]*/,\n  contains: [\n    BACKSLASH_ESCAPE,\n    {\n      begin: /\\[/,\n      end: /\\]/,\n      relevance: 0,\n      contains: [BACKSLASH_ESCAPE]\n    }\n  ]\n};\nconst TITLE_MODE = {\n  scope: 'title',\n  begin: IDENT_RE,\n  relevance: 0\n};\nconst UNDERSCORE_TITLE_MODE = {\n  scope: 'title',\n  begin: UNDERSCORE_IDENT_RE,\n  relevance: 0\n};\nconst METHOD_GUARD = {\n  // excludes method names from keyword processing\n  begin: '\\\\.\\\\s*' + UNDERSCORE_IDENT_RE,\n  relevance: 0\n};\n\n/**\n * Adds end same as begin mechanics to a mode\n *\n * Your mode must include at least a single () match group as that first match\n * group is what is used for comparison\n * @param {Partial<Mode>} mode\n */\nconst END_SAME_AS_BEGIN = function(mode) {\n  return Object.assign(mode,\n    {\n      /** @type {ModeCallback} */\n      'on:begin': (m, resp) => { resp.data._beginMatch = m[1]; },\n      /** @type {ModeCallback} */\n      'on:end': (m, resp) => { if (resp.data._beginMatch !== m[1]) resp.ignoreMatch(); }\n    });\n};\n\nvar MODES = /*#__PURE__*/Object.freeze({\n  __proto__: null,\n  APOS_STRING_MODE: APOS_STRING_MODE,\n  BACKSLASH_ESCAPE: BACKSLASH_ESCAPE,\n  BINARY_NUMBER_MODE: BINARY_NUMBER_MODE,\n  BINARY_NUMBER_RE: BINARY_NUMBER_RE,\n  COMMENT: COMMENT,\n  C_BLOCK_COMMENT_MODE: C_BLOCK_COMMENT_MODE,\n  C_LINE_COMMENT_MODE: C_LINE_COMMENT_MODE,\n  C_NUMBER_MODE: C_NUMBER_MODE,\n  C_NUMBER_RE: C_NUMBER_RE,\n  END_SAME_AS_BEGIN: END_SAME_AS_BEGIN,\n  HASH_COMMENT_MODE: HASH_COMMENT_MODE,\n  IDENT_RE: IDENT_RE,\n  MATCH_NOTHING_RE: MATCH_NOTHING_RE,\n  METHOD_GUARD: METHOD_GUARD,\n  NUMBER_MODE: NUMBER_MODE,\n  NUMBER_RE: NUMBER_RE,\n  PHRASAL_WORDS_MODE: PHRASAL_WORDS_MODE,\n  QUOTE_STRING_MODE: QUOTE_STRING_MODE,\n  REGEXP_MODE: REGEXP_MODE,\n  RE_STARTERS_RE: RE_STARTERS_RE,\n  SHEBANG: SHEBANG,\n  TITLE_MODE: TITLE_MODE,\n  UNDERSCORE_IDENT_RE: UNDERSCORE_IDENT_RE,\n  UNDERSCORE_TITLE_MODE: UNDERSCORE_TITLE_MODE\n});\n\n/**\n@typedef {import('highlight.js').CallbackResponse} CallbackResponse\n@typedef {import('highlight.js').CompilerExt} CompilerExt\n*/\n\n// Grammar extensions / plugins\n// See: https://github.com/highlightjs/highlight.js/issues/2833\n\n// Grammar extensions allow \"syntactic sugar\" to be added to the grammar modes\n// without requiring any underlying changes to the compiler internals.\n\n// `compileMatch` being the perfect small example of now allowing a grammar\n// author to write `match` when they desire to match a single expression rather\n// than being forced to use `begin`.  The extension then just moves `match` into\n// `begin` when it runs.  Ie, no features have been added, but we've just made\n// the experience of writing (and reading grammars) a little bit nicer.\n\n// ------\n\n// TODO: We need negative look-behind support to do this properly\n/**\n * Skip a match if it has a preceding dot\n *\n * This is used for `beginKeywords` to prevent matching expressions such as\n * `bob.keyword.do()`. The mode compiler automatically wires this up as a\n * special _internal_ 'on:begin' callback for modes with `beginKeywords`\n * @param {RegExpMatchArray} match\n * @param {CallbackResponse} response\n */\nfunction skipIfHasPrecedingDot(match, response) {\n  const before = match.input[match.index - 1];\n  if (before === \".\") {\n    response.ignoreMatch();\n  }\n}\n\n/**\n *\n * @type {CompilerExt}\n */\nfunction scopeClassName(mode, _parent) {\n  // eslint-disable-next-line no-undefined\n  if (mode.className !== undefined) {\n    mode.scope = mode.className;\n    delete mode.className;\n  }\n}\n\n/**\n * `beginKeywords` syntactic sugar\n * @type {CompilerExt}\n */\nfunction beginKeywords(mode, parent) {\n  if (!parent) return;\n  if (!mode.beginKeywords) return;\n\n  // for languages with keywords that include non-word characters checking for\n  // a word boundary is not sufficient, so instead we check for a word boundary\n  // or whitespace - this does no harm in any case since our keyword engine\n  // doesn't allow spaces in keywords anyways and we still check for the boundary\n  // first\n  mode.begin = '\\\\b(' + mode.beginKeywords.split(' ').join('|') + ')(?!\\\\.)(?=\\\\b|\\\\s)';\n  mode.__beforeBegin = skipIfHasPrecedingDot;\n  mode.keywords = mode.keywords || mode.beginKeywords;\n  delete mode.beginKeywords;\n\n  // prevents double relevance, the keywords themselves provide\n  // relevance, the mode doesn't need to double it\n  // eslint-disable-next-line no-undefined\n  if (mode.relevance === undefined) mode.relevance = 0;\n}\n\n/**\n * Allow `illegal` to contain an array of illegal values\n * @type {CompilerExt}\n */\nfunction compileIllegal(mode, _parent) {\n  if (!Array.isArray(mode.illegal)) return;\n\n  mode.illegal = either(...mode.illegal);\n}\n\n/**\n * `match` to match a single expression for readability\n * @type {CompilerExt}\n */\nfunction compileMatch(mode, _parent) {\n  if (!mode.match) return;\n  if (mode.begin || mode.end) throw new Error(\"begin & end are not supported with match\");\n\n  mode.begin = mode.match;\n  delete mode.match;\n}\n\n/**\n * provides the default 1 relevance to all modes\n * @type {CompilerExt}\n */\nfunction compileRelevance(mode, _parent) {\n  // eslint-disable-next-line no-undefined\n  if (mode.relevance === undefined) mode.relevance = 1;\n}\n\n// allow beforeMatch to act as a \"qualifier\" for the match\n// the full match begin must be [beforeMatch][begin]\nconst beforeMatchExt = (mode, parent) => {\n  if (!mode.beforeMatch) return;\n  // starts conflicts with endsParent which we need to make sure the child\n  // rule is not matched multiple times\n  if (mode.starts) throw new Error(\"beforeMatch cannot be used with starts\");\n\n  const originalMode = Object.assign({}, mode);\n  Object.keys(mode).forEach((key) => { delete mode[key]; });\n\n  mode.keywords = originalMode.keywords;\n  mode.begin = concat(originalMode.beforeMatch, lookahead(originalMode.begin));\n  mode.starts = {\n    relevance: 0,\n    contains: [\n      Object.assign(originalMode, { endsParent: true })\n    ]\n  };\n  mode.relevance = 0;\n\n  delete originalMode.beforeMatch;\n};\n\n// keywords that should have no default relevance value\nconst COMMON_KEYWORDS = [\n  'of',\n  'and',\n  'for',\n  'in',\n  'not',\n  'or',\n  'if',\n  'then',\n  'parent', // common variable name\n  'list', // common variable name\n  'value' // common variable name\n];\n\nconst DEFAULT_KEYWORD_SCOPE = \"keyword\";\n\n/**\n * Given raw keywords from a language definition, compile them.\n *\n * @param {string | Record<string,string|string[]> | Array<string>} rawKeywords\n * @param {boolean} caseInsensitive\n */\nfunction compileKeywords(rawKeywords, caseInsensitive, scopeName = DEFAULT_KEYWORD_SCOPE) {\n  /** @type {import(\"highlight.js/private\").KeywordDict} */\n  const compiledKeywords = Object.create(null);\n\n  // input can be a string of keywords, an array of keywords, or a object with\n  // named keys representing scopeName (which can then point to a string or array)\n  if (typeof rawKeywords === 'string') {\n    compileList(scopeName, rawKeywords.split(\" \"));\n  } else if (Array.isArray(rawKeywords)) {\n    compileList(scopeName, rawKeywords);\n  } else {\n    Object.keys(rawKeywords).forEach(function(scopeName) {\n      // collapse all our objects back into the parent object\n      Object.assign(\n        compiledKeywords,\n        compileKeywords(rawKeywords[scopeName], caseInsensitive, scopeName)\n      );\n    });\n  }\n  return compiledKeywords;\n\n  // ---\n\n  /**\n   * Compiles an individual list of keywords\n   *\n   * Ex: \"for if when while|5\"\n   *\n   * @param {string} scopeName\n   * @param {Array<string>} keywordList\n   */\n  function compileList(scopeName, keywordList) {\n    if (caseInsensitive) {\n      keywordList = keywordList.map(x => x.toLowerCase());\n    }\n    keywordList.forEach(function(keyword) {\n      const pair = keyword.split('|');\n      compiledKeywords[pair[0]] = [scopeName, scoreForKeyword(pair[0], pair[1])];\n    });\n  }\n}\n\n/**\n * Returns the proper score for a given keyword\n *\n * Also takes into account comment keywords, which will be scored 0 UNLESS\n * another score has been manually assigned.\n * @param {string} keyword\n * @param {string} [providedScore]\n */\nfunction scoreForKeyword(keyword, providedScore) {\n  // manual scores always win over common keywords\n  // so you can force a score of 1 if you really insist\n  if (providedScore) {\n    return Number(providedScore);\n  }\n\n  return commonKeyword(keyword) ? 0 : 1;\n}\n\n/**\n * Determines if a given keyword is common or not\n *\n * @param {string} keyword */\nfunction commonKeyword(keyword) {\n  return COMMON_KEYWORDS.includes(keyword.toLowerCase());\n}\n\n/*\n\nFor the reasoning behind this please see:\nhttps://github.com/highlightjs/highlight.js/issues/2880#issuecomment-747275419\n\n*/\n\n/**\n * @type {Record<string, boolean>}\n */\nconst seenDeprecations = {};\n\n/**\n * @param {string} message\n */\nconst error = (message) => {\n  console.error(message);\n};\n\n/**\n * @param {string} message\n * @param {any} args\n */\nconst warn = (message, ...args) => {\n  console.log(`WARN: ${message}`, ...args);\n};\n\n/**\n * @param {string} version\n * @param {string} message\n */\nconst deprecated = (version, message) => {\n  if (seenDeprecations[`${version}/${message}`]) return;\n\n  console.log(`Deprecated as of ${version}. ${message}`);\n  seenDeprecations[`${version}/${message}`] = true;\n};\n\n/* eslint-disable no-throw-literal */\n\n/**\n@typedef {import('highlight.js').CompiledMode} CompiledMode\n*/\n\nconst MultiClassError = new Error();\n\n/**\n * Renumbers labeled scope names to account for additional inner match\n * groups that otherwise would break everything.\n *\n * Lets say we 3 match scopes:\n *\n *   { 1 => ..., 2 => ..., 3 => ... }\n *\n * So what we need is a clean match like this:\n *\n *   (a)(b)(c) => [ \"a\", \"b\", \"c\" ]\n *\n * But this falls apart with inner match groups:\n *\n * (a)(((b)))(c) => [\"a\", \"b\", \"b\", \"b\", \"c\" ]\n *\n * Our scopes are now \"out of alignment\" and we're repeating `b` 3 times.\n * What needs to happen is the numbers are remapped:\n *\n *   { 1 => ..., 2 => ..., 5 => ... }\n *\n * We also need to know that the ONLY groups that should be output\n * are 1, 2, and 5.  This function handles this behavior.\n *\n * @param {CompiledMode} mode\n * @param {Array<RegExp | string>} regexes\n * @param {{key: \"beginScope\"|\"endScope\"}} opts\n */\nfunction remapScopeNames(mode, regexes, { key }) {\n  let offset = 0;\n  const scopeNames = mode[key];\n  /** @type Record<number,boolean> */\n  const emit = {};\n  /** @type Record<number,string> */\n  const positions = {};\n\n  for (let i = 1; i <= regexes.length; i++) {\n    positions[i + offset] = scopeNames[i];\n    emit[i + offset] = true;\n    offset += countMatchGroups(regexes[i - 1]);\n  }\n  // we use _emit to keep track of which match groups are \"top-level\" to avoid double\n  // output from inside match groups\n  mode[key] = positions;\n  mode[key]._emit = emit;\n  mode[key]._multi = true;\n}\n\n/**\n * @param {CompiledMode} mode\n */\nfunction beginMultiClass(mode) {\n  if (!Array.isArray(mode.begin)) return;\n\n  if (mode.skip || mode.excludeBegin || mode.returnBegin) {\n    error(\"skip, excludeBegin, returnBegin not compatible with beginScope: {}\");\n    throw MultiClassError;\n  }\n\n  if (typeof mode.beginScope !== \"object\" || mode.beginScope === null) {\n    error(\"beginScope must be object\");\n    throw MultiClassError;\n  }\n\n  remapScopeNames(mode, mode.begin, { key: \"beginScope\" });\n  mode.begin = _rewriteBackreferences(mode.begin, { joinWith: \"\" });\n}\n\n/**\n * @param {CompiledMode} mode\n */\nfunction endMultiClass(mode) {\n  if (!Array.isArray(mode.end)) return;\n\n  if (mode.skip || mode.excludeEnd || mode.returnEnd) {\n    error(\"skip, excludeEnd, returnEnd not compatible with endScope: {}\");\n    throw MultiClassError;\n  }\n\n  if (typeof mode.endScope !== \"object\" || mode.endScope === null) {\n    error(\"endScope must be object\");\n    throw MultiClassError;\n  }\n\n  remapScopeNames(mode, mode.end, { key: \"endScope\" });\n  mode.end = _rewriteBackreferences(mode.end, { joinWith: \"\" });\n}\n\n/**\n * this exists only to allow `scope: {}` to be used beside `match:`\n * Otherwise `beginScope` would necessary and that would look weird\n\n  {\n    match: [ /def/, /\\w+/ ]\n    scope: { 1: \"keyword\" , 2: \"title\" }\n  }\n\n * @param {CompiledMode} mode\n */\nfunction scopeSugar(mode) {\n  if (mode.scope && typeof mode.scope === \"object\" && mode.scope !== null) {\n    mode.beginScope = mode.scope;\n    delete mode.scope;\n  }\n}\n\n/**\n * @param {CompiledMode} mode\n */\nfunction MultiClass(mode) {\n  scopeSugar(mode);\n\n  if (typeof mode.beginScope === \"string\") {\n    mode.beginScope = { _wrap: mode.beginScope };\n  }\n  if (typeof mode.endScope === \"string\") {\n    mode.endScope = { _wrap: mode.endScope };\n  }\n\n  beginMultiClass(mode);\n  endMultiClass(mode);\n}\n\n/**\n@typedef {import('highlight.js').Mode} Mode\n@typedef {import('highlight.js').CompiledMode} CompiledMode\n@typedef {import('highlight.js').Language} Language\n@typedef {import('highlight.js').HLJSPlugin} HLJSPlugin\n@typedef {import('highlight.js').CompiledLanguage} CompiledLanguage\n*/\n\n// compilation\n\n/**\n * Compiles a language definition result\n *\n * Given the raw result of a language definition (Language), compiles this so\n * that it is ready for highlighting code.\n * @param {Language} language\n * @returns {CompiledLanguage}\n */\nfunction compileLanguage(language) {\n  /**\n   * Builds a regex with the case sensitivity of the current language\n   *\n   * @param {RegExp | string} value\n   * @param {boolean} [global]\n   */\n  function langRe(value, global) {\n    return new RegExp(\n      source(value),\n      'm'\n      + (language.case_insensitive ? 'i' : '')\n      + (language.unicodeRegex ? 'u' : '')\n      + (global ? 'g' : '')\n    );\n  }\n\n  /**\n    Stores multiple regular expressions and allows you to quickly search for\n    them all in a string simultaneously - returning the first match.  It does\n    this by creating a huge (a|b|c) regex - each individual item wrapped with ()\n    and joined by `|` - using match groups to track position.  When a match is\n    found checking which position in the array has content allows us to figure\n    out which of the original regexes / match groups triggered the match.\n\n    The match object itself (the result of `Regex.exec`) is returned but also\n    enhanced by merging in any meta-data that was registered with the regex.\n    This is how we keep track of which mode matched, and what type of rule\n    (`illegal`, `begin`, end, etc).\n  */\n  class MultiRegex {\n    constructor() {\n      this.matchIndexes = {};\n      // @ts-ignore\n      this.regexes = [];\n      this.matchAt = 1;\n      this.position = 0;\n    }\n\n    // @ts-ignore\n    addRule(re, opts) {\n      opts.position = this.position++;\n      // @ts-ignore\n      this.matchIndexes[this.matchAt] = opts;\n      this.regexes.push([opts, re]);\n      this.matchAt += countMatchGroups(re) + 1;\n    }\n\n    compile() {\n      if (this.regexes.length === 0) {\n        // avoids the need to check length every time exec is called\n        // @ts-ignore\n        this.exec = () => null;\n      }\n      const terminators = this.regexes.map(el => el[1]);\n      this.matcherRe = langRe(_rewriteBackreferences(terminators, { joinWith: '|' }), true);\n      this.lastIndex = 0;\n    }\n\n    /** @param {string} s */\n    exec(s) {\n      this.matcherRe.lastIndex = this.lastIndex;\n      const match = this.matcherRe.exec(s);\n      if (!match) { return null; }\n\n      // eslint-disable-next-line no-undefined\n      const i = match.findIndex((el, i) => i > 0 && el !== undefined);\n      // @ts-ignore\n      const matchData = this.matchIndexes[i];\n      // trim off any earlier non-relevant match groups (ie, the other regex\n      // match groups that make up the multi-matcher)\n      match.splice(0, i);\n\n      return Object.assign(match, matchData);\n    }\n  }\n\n  /*\n    Created to solve the key deficiently with MultiRegex - there is no way to\n    test for multiple matches at a single location.  Why would we need to do\n    that?  In the future a more dynamic engine will allow certain matches to be\n    ignored.  An example: if we matched say the 3rd regex in a large group but\n    decided to ignore it - we'd need to started testing again at the 4th\n    regex... but MultiRegex itself gives us no real way to do that.\n\n    So what this class creates MultiRegexs on the fly for whatever search\n    position they are needed.\n\n    NOTE: These additional MultiRegex objects are created dynamically.  For most\n    grammars most of the time we will never actually need anything more than the\n    first MultiRegex - so this shouldn't have too much overhead.\n\n    Say this is our search group, and we match regex3, but wish to ignore it.\n\n      regex1 | regex2 | regex3 | regex4 | regex5    ' ie, startAt = 0\n\n    What we need is a new MultiRegex that only includes the remaining\n    possibilities:\n\n      regex4 | regex5                               ' ie, startAt = 3\n\n    This class wraps all that complexity up in a simple API... `startAt` decides\n    where in the array of expressions to start doing the matching. It\n    auto-increments, so if a match is found at position 2, then startAt will be\n    set to 3.  If the end is reached startAt will return to 0.\n\n    MOST of the time the parser will be setting startAt manually to 0.\n  */\n  class ResumableMultiRegex {\n    constructor() {\n      // @ts-ignore\n      this.rules = [];\n      // @ts-ignore\n      this.multiRegexes = [];\n      this.count = 0;\n\n      this.lastIndex = 0;\n      this.regexIndex = 0;\n    }\n\n    // @ts-ignore\n    getMatcher(index) {\n      if (this.multiRegexes[index]) return this.multiRegexes[index];\n\n      const matcher = new MultiRegex();\n      this.rules.slice(index).forEach(([re, opts]) => matcher.addRule(re, opts));\n      matcher.compile();\n      this.multiRegexes[index] = matcher;\n      return matcher;\n    }\n\n    resumingScanAtSamePosition() {\n      return this.regexIndex !== 0;\n    }\n\n    considerAll() {\n      this.regexIndex = 0;\n    }\n\n    // @ts-ignore\n    addRule(re, opts) {\n      this.rules.push([re, opts]);\n      if (opts.type === \"begin\") this.count++;\n    }\n\n    /** @param {string} s */\n    exec(s) {\n      const m = this.getMatcher(this.regexIndex);\n      m.lastIndex = this.lastIndex;\n      let result = m.exec(s);\n\n      // The following is because we have no easy way to say \"resume scanning at the\n      // existing position but also skip the current rule ONLY\". What happens is\n      // all prior rules are also skipped which can result in matching the wrong\n      // thing. Example of matching \"booger\":\n\n      // our matcher is [string, \"booger\", number]\n      //\n      // ....booger....\n\n      // if \"booger\" is ignored then we'd really need a regex to scan from the\n      // SAME position for only: [string, number] but ignoring \"booger\" (if it\n      // was the first match), a simple resume would scan ahead who knows how\n      // far looking only for \"number\", ignoring potential string matches (or\n      // future \"booger\" matches that might be valid.)\n\n      // So what we do: We execute two matchers, one resuming at the same\n      // position, but the second full matcher starting at the position after:\n\n      //     /--- resume first regex match here (for [number])\n      //     |/---- full match here for [string, \"booger\", number]\n      //     vv\n      // ....booger....\n\n      // Which ever results in a match first is then used. So this 3-4 step\n      // process essentially allows us to say \"match at this position, excluding\n      // a prior rule that was ignored\".\n      //\n      // 1. Match \"booger\" first, ignore. Also proves that [string] does non match.\n      // 2. Resume matching for [number]\n      // 3. Match at index + 1 for [string, \"booger\", number]\n      // 4. If #2 and #3 result in matches, which came first?\n      if (this.resumingScanAtSamePosition()) {\n        if (result && result.index === this.lastIndex) ; else { // use the second matcher result\n          const m2 = this.getMatcher(0);\n          m2.lastIndex = this.lastIndex + 1;\n          result = m2.exec(s);\n        }\n      }\n\n      if (result) {\n        this.regexIndex += result.position + 1;\n        if (this.regexIndex === this.count) {\n          // wrap-around to considering all matches again\n          this.considerAll();\n        }\n      }\n\n      return result;\n    }\n  }\n\n  /**\n   * Given a mode, builds a huge ResumableMultiRegex that can be used to walk\n   * the content and find matches.\n   *\n   * @param {CompiledMode} mode\n   * @returns {ResumableMultiRegex}\n   */\n  function buildModeRegex(mode) {\n    const mm = new ResumableMultiRegex();\n\n    mode.contains.forEach(term => mm.addRule(term.begin, { rule: term, type: \"begin\" }));\n\n    if (mode.terminatorEnd) {\n      mm.addRule(mode.terminatorEnd, { type: \"end\" });\n    }\n    if (mode.illegal) {\n      mm.addRule(mode.illegal, { type: \"illegal\" });\n    }\n\n    return mm;\n  }\n\n  /** skip vs abort vs ignore\n   *\n   * @skip   - The mode is still entered and exited normally (and contains rules apply),\n   *           but all content is held and added to the parent buffer rather than being\n   *           output when the mode ends.  Mostly used with `sublanguage` to build up\n   *           a single large buffer than can be parsed by sublanguage.\n   *\n   *             - The mode begin ands ends normally.\n   *             - Content matched is added to the parent mode buffer.\n   *             - The parser cursor is moved forward normally.\n   *\n   * @abort  - A hack placeholder until we have ignore.  Aborts the mode (as if it\n   *           never matched) but DOES NOT continue to match subsequent `contains`\n   *           modes.  Abort is bad/suboptimal because it can result in modes\n   *           farther down not getting applied because an earlier rule eats the\n   *           content but then aborts.\n   *\n   *             - The mode does not begin.\n   *             - Content matched by `begin` is added to the mode buffer.\n   *             - The parser cursor is moved forward accordingly.\n   *\n   * @ignore - Ignores the mode (as if it never matched) and continues to match any\n   *           subsequent `contains` modes.  Ignore isn't technically possible with\n   *           the current parser implementation.\n   *\n   *             - The mode does not begin.\n   *             - Content matched by `begin` is ignored.\n   *             - The parser cursor is not moved forward.\n   */\n\n  /**\n   * Compiles an individual mode\n   *\n   * This can raise an error if the mode contains certain detectable known logic\n   * issues.\n   * @param {Mode} mode\n   * @param {CompiledMode | null} [parent]\n   * @returns {CompiledMode | never}\n   */\n  function compileMode(mode, parent) {\n    const cmode = /** @type CompiledMode */ (mode);\n    if (mode.isCompiled) return cmode;\n\n    [\n      scopeClassName,\n      // do this early so compiler extensions generally don't have to worry about\n      // the distinction between match/begin\n      compileMatch,\n      MultiClass,\n      beforeMatchExt\n    ].forEach(ext => ext(mode, parent));\n\n    language.compilerExtensions.forEach(ext => ext(mode, parent));\n\n    // __beforeBegin is considered private API, internal use only\n    mode.__beforeBegin = null;\n\n    [\n      beginKeywords,\n      // do this later so compiler extensions that come earlier have access to the\n      // raw array if they wanted to perhaps manipulate it, etc.\n      compileIllegal,\n      // default to 1 relevance if not specified\n      compileRelevance\n    ].forEach(ext => ext(mode, parent));\n\n    mode.isCompiled = true;\n\n    let keywordPattern = null;\n    if (typeof mode.keywords === \"object\" && mode.keywords.$pattern) {\n      // we need a copy because keywords might be compiled multiple times\n      // so we can't go deleting $pattern from the original on the first\n      // pass\n      mode.keywords = Object.assign({}, mode.keywords);\n      keywordPattern = mode.keywords.$pattern;\n      delete mode.keywords.$pattern;\n    }\n    keywordPattern = keywordPattern || /\\w+/;\n\n    if (mode.keywords) {\n      mode.keywords = compileKeywords(mode.keywords, language.case_insensitive);\n    }\n\n    cmode.keywordPatternRe = langRe(keywordPattern, true);\n\n    if (parent) {\n      if (!mode.begin) mode.begin = /\\B|\\b/;\n      cmode.beginRe = langRe(cmode.begin);\n      if (!mode.end && !mode.endsWithParent) mode.end = /\\B|\\b/;\n      if (mode.end) cmode.endRe = langRe(cmode.end);\n      cmode.terminatorEnd = source(cmode.end) || '';\n      if (mode.endsWithParent && parent.terminatorEnd) {\n        cmode.terminatorEnd += (mode.end ? '|' : '') + parent.terminatorEnd;\n      }\n    }\n    if (mode.illegal) cmode.illegalRe = langRe(/** @type {RegExp | string} */ (mode.illegal));\n    if (!mode.contains) mode.contains = [];\n\n    mode.contains = [].concat(...mode.contains.map(function(c) {\n      return expandOrCloneMode(c === 'self' ? mode : c);\n    }));\n    mode.contains.forEach(function(c) { compileMode(/** @type Mode */ (c), cmode); });\n\n    if (mode.starts) {\n      compileMode(mode.starts, parent);\n    }\n\n    cmode.matcher = buildModeRegex(cmode);\n    return cmode;\n  }\n\n  if (!language.compilerExtensions) language.compilerExtensions = [];\n\n  // self is not valid at the top-level\n  if (language.contains && language.contains.includes('self')) {\n    throw new Error(\"ERR: contains `self` is not supported at the top-level of a language.  See documentation.\");\n  }\n\n  // we need a null object, which inherit will guarantee\n  language.classNameAliases = inherit$1(language.classNameAliases || {});\n\n  return compileMode(/** @type Mode */ (language));\n}\n\n/**\n * Determines if a mode has a dependency on it's parent or not\n *\n * If a mode does have a parent dependency then often we need to clone it if\n * it's used in multiple places so that each copy points to the correct parent,\n * where-as modes without a parent can often safely be re-used at the bottom of\n * a mode chain.\n *\n * @param {Mode | null} mode\n * @returns {boolean} - is there a dependency on the parent?\n * */\nfunction dependencyOnParent(mode) {\n  if (!mode) return false;\n\n  return mode.endsWithParent || dependencyOnParent(mode.starts);\n}\n\n/**\n * Expands a mode or clones it if necessary\n *\n * This is necessary for modes with parental dependenceis (see notes on\n * `dependencyOnParent`) and for nodes that have `variants` - which must then be\n * exploded into their own individual modes at compile time.\n *\n * @param {Mode} mode\n * @returns {Mode | Mode[]}\n * */\nfunction expandOrCloneMode(mode) {\n  if (mode.variants && !mode.cachedVariants) {\n    mode.cachedVariants = mode.variants.map(function(variant) {\n      return inherit$1(mode, { variants: null }, variant);\n    });\n  }\n\n  // EXPAND\n  // if we have variants then essentially \"replace\" the mode with the variants\n  // this happens in compileMode, where this function is called from\n  if (mode.cachedVariants) {\n    return mode.cachedVariants;\n  }\n\n  // CLONE\n  // if we have dependencies on parents then we need a unique\n  // instance of ourselves, so we can be reused with many\n  // different parents without issue\n  if (dependencyOnParent(mode)) {\n    return inherit$1(mode, { starts: mode.starts ? inherit$1(mode.starts) : null });\n  }\n\n  if (Object.isFrozen(mode)) {\n    return inherit$1(mode);\n  }\n\n  // no special dependency issues, just return ourselves\n  return mode;\n}\n\nvar version = \"11.11.1\";\n\nclass HTMLInjectionError extends Error {\n  constructor(reason, html) {\n    super(reason);\n    this.name = \"HTMLInjectionError\";\n    this.html = html;\n  }\n}\n\n/*\nSyntax highlighting with language autodetection.\nhttps://highlightjs.org/\n*/\n\n\n\n/**\n@typedef {import('highlight.js').Mode} Mode\n@typedef {import('highlight.js').CompiledMode} CompiledMode\n@typedef {import('highlight.js').CompiledScope} CompiledScope\n@typedef {import('highlight.js').Language} Language\n@typedef {import('highlight.js').HLJSApi} HLJSApi\n@typedef {import('highlight.js').HLJSPlugin} HLJSPlugin\n@typedef {import('highlight.js').PluginEvent} PluginEvent\n@typedef {import('highlight.js').HLJSOptions} HLJSOptions\n@typedef {import('highlight.js').LanguageFn} LanguageFn\n@typedef {import('highlight.js').HighlightedHTMLElement} HighlightedHTMLElement\n@typedef {import('highlight.js').BeforeHighlightContext} BeforeHighlightContext\n@typedef {import('highlight.js/private').MatchType} MatchType\n@typedef {import('highlight.js/private').KeywordData} KeywordData\n@typedef {import('highlight.js/private').EnhancedMatch} EnhancedMatch\n@typedef {import('highlight.js/private').AnnotatedError} AnnotatedError\n@typedef {import('highlight.js').AutoHighlightResult} AutoHighlightResult\n@typedef {import('highlight.js').HighlightOptions} HighlightOptions\n@typedef {import('highlight.js').HighlightResult} HighlightResult\n*/\n\n\nconst escape = escapeHTML;\nconst inherit = inherit$1;\nconst NO_MATCH = Symbol(\"nomatch\");\nconst MAX_KEYWORD_HITS = 7;\n\n/**\n * @param {any} hljs - object that is extended (legacy)\n * @returns {HLJSApi}\n */\nconst HLJS = function(hljs) {\n  // Global internal variables used within the highlight.js library.\n  /** @type {Record<string, Language>} */\n  const languages = Object.create(null);\n  /** @type {Record<string, string>} */\n  const aliases = Object.create(null);\n  /** @type {HLJSPlugin[]} */\n  const plugins = [];\n\n  // safe/production mode - swallows more errors, tries to keep running\n  // even if a single syntax or parse hits a fatal error\n  let SAFE_MODE = true;\n  const LANGUAGE_NOT_FOUND = \"Could not find the language '{}', did you forget to load/include a language module?\";\n  /** @type {Language} */\n  const PLAINTEXT_LANGUAGE = { disableAutodetect: true, name: 'Plain text', contains: [] };\n\n  // Global options used when within external APIs. This is modified when\n  // calling the `hljs.configure` function.\n  /** @type HLJSOptions */\n  let options = {\n    ignoreUnescapedHTML: false,\n    throwUnescapedHTML: false,\n    noHighlightRe: /^(no-?highlight)$/i,\n    languageDetectRe: /\\blang(?:uage)?-([\\w-]+)\\b/i,\n    classPrefix: 'hljs-',\n    cssSelector: 'pre code',\n    languages: null,\n    // beta configuration options, subject to change, welcome to discuss\n    // https://github.com/highlightjs/highlight.js/issues/1086\n    __emitter: TokenTreeEmitter\n  };\n\n  /* Utility functions */\n\n  /**\n   * Tests a language name to see if highlighting should be skipped\n   * @param {string} languageName\n   */\n  function shouldNotHighlight(languageName) {\n    return options.noHighlightRe.test(languageName);\n  }\n\n  /**\n   * @param {HighlightedHTMLElement} block - the HTML element to determine language for\n   */\n  function blockLanguage(block) {\n    let classes = block.className + ' ';\n\n    classes += block.parentNode ? block.parentNode.className : '';\n\n    // language-* takes precedence over non-prefixed class names.\n    const match = options.languageDetectRe.exec(classes);\n    if (match) {\n      const language = getLanguage(match[1]);\n      if (!language) {\n        warn(LANGUAGE_NOT_FOUND.replace(\"{}\", match[1]));\n        warn(\"Falling back to no-highlight mode for this block.\", block);\n      }\n      return language ? match[1] : 'no-highlight';\n    }\n\n    return classes\n      .split(/\\s+/)\n      .find((_class) => shouldNotHighlight(_class) || getLanguage(_class));\n  }\n\n  /**\n   * Core highlighting function.\n   *\n   * OLD API\n   * highlight(lang, code, ignoreIllegals, continuation)\n   *\n   * NEW API\n   * highlight(code, {lang, ignoreIllegals})\n   *\n   * @param {string} codeOrLanguageName - the language to use for highlighting\n   * @param {string | HighlightOptions} optionsOrCode - the code to highlight\n   * @param {boolean} [ignoreIllegals] - whether to ignore illegal matches, default is to bail\n   *\n   * @returns {HighlightResult} Result - an object that represents the result\n   * @property {string} language - the language name\n   * @property {number} relevance - the relevance score\n   * @property {string} value - the highlighted HTML code\n   * @property {string} code - the original raw code\n   * @property {CompiledMode} top - top of the current mode stack\n   * @property {boolean} illegal - indicates whether any illegal matches were found\n  */\n  function highlight(codeOrLanguageName, optionsOrCode, ignoreIllegals) {\n    let code = \"\";\n    let languageName = \"\";\n    if (typeof optionsOrCode === \"object\") {\n      code = codeOrLanguageName;\n      ignoreIllegals = optionsOrCode.ignoreIllegals;\n      languageName = optionsOrCode.language;\n    } else {\n      // old API\n      deprecated(\"10.7.0\", \"highlight(lang, code, ...args) has been deprecated.\");\n      deprecated(\"10.7.0\", \"Please use highlight(code, options) instead.\\nhttps://github.com/highlightjs/highlight.js/issues/2277\");\n      languageName = codeOrLanguageName;\n      code = optionsOrCode;\n    }\n\n    // https://github.com/highlightjs/highlight.js/issues/3149\n    // eslint-disable-next-line no-undefined\n    if (ignoreIllegals === undefined) { ignoreIllegals = true; }\n\n    /** @type {BeforeHighlightContext} */\n    const context = {\n      code,\n      language: languageName\n    };\n    // the plugin can change the desired language or the code to be highlighted\n    // just be changing the object it was passed\n    fire(\"before:highlight\", context);\n\n    // a before plugin can usurp the result completely by providing it's own\n    // in which case we don't even need to call highlight\n    const result = context.result\n      ? context.result\n      : _highlight(context.language, context.code, ignoreIllegals);\n\n    result.code = context.code;\n    // the plugin can change anything in result to suite it\n    fire(\"after:highlight\", result);\n\n    return result;\n  }\n\n  /**\n   * private highlight that's used internally and does not fire callbacks\n   *\n   * @param {string} languageName - the language to use for highlighting\n   * @param {string} codeToHighlight - the code to highlight\n   * @param {boolean?} [ignoreIllegals] - whether to ignore illegal matches, default is to bail\n   * @param {CompiledMode?} [continuation] - current continuation mode, if any\n   * @returns {HighlightResult} - result of the highlight operation\n  */\n  function _highlight(languageName, codeToHighlight, ignoreIllegals, continuation) {\n    const keywordHits = Object.create(null);\n\n    /**\n     * Return keyword data if a match is a keyword\n     * @param {CompiledMode} mode - current mode\n     * @param {string} matchText - the textual match\n     * @returns {KeywordData | false}\n     */\n    function keywordData(mode, matchText) {\n      return mode.keywords[matchText];\n    }\n\n    function processKeywords() {\n      if (!top.keywords) {\n        emitter.addText(modeBuffer);\n        return;\n      }\n\n      let lastIndex = 0;\n      top.keywordPatternRe.lastIndex = 0;\n      let match = top.keywordPatternRe.exec(modeBuffer);\n      let buf = \"\";\n\n      while (match) {\n        buf += modeBuffer.substring(lastIndex, match.index);\n        const word = language.case_insensitive ? match[0].toLowerCase() : match[0];\n        const data = keywordData(top, word);\n        if (data) {\n          const [kind, keywordRelevance] = data;\n          emitter.addText(buf);\n          buf = \"\";\n\n          keywordHits[word] = (keywordHits[word] || 0) + 1;\n          if (keywordHits[word] <= MAX_KEYWORD_HITS) relevance += keywordRelevance;\n          if (kind.startsWith(\"_\")) {\n            // _ implied for relevance only, do not highlight\n            // by applying a class name\n            buf += match[0];\n          } else {\n            const cssClass = language.classNameAliases[kind] || kind;\n            emitKeyword(match[0], cssClass);\n          }\n        } else {\n          buf += match[0];\n        }\n        lastIndex = top.keywordPatternRe.lastIndex;\n        match = top.keywordPatternRe.exec(modeBuffer);\n      }\n      buf += modeBuffer.substring(lastIndex);\n      emitter.addText(buf);\n    }\n\n    function processSubLanguage() {\n      if (modeBuffer === \"\") return;\n      /** @type HighlightResult */\n      let result = null;\n\n      if (typeof top.subLanguage === 'string') {\n        if (!languages[top.subLanguage]) {\n          emitter.addText(modeBuffer);\n          return;\n        }\n        result = _highlight(top.subLanguage, modeBuffer, true, continuations[top.subLanguage]);\n        continuations[top.subLanguage] = /** @type {CompiledMode} */ (result._top);\n      } else {\n        result = highlightAuto(modeBuffer, top.subLanguage.length ? top.subLanguage : null);\n      }\n\n      // Counting embedded language score towards the host language may be disabled\n      // with zeroing the containing mode relevance. Use case in point is Markdown that\n      // allows XML everywhere and makes every XML snippet to have a much larger Markdown\n      // score.\n      if (top.relevance > 0) {\n        relevance += result.relevance;\n      }\n      emitter.__addSublanguage(result._emitter, result.language);\n    }\n\n    function processBuffer() {\n      if (top.subLanguage != null) {\n        processSubLanguage();\n      } else {\n        processKeywords();\n      }\n      modeBuffer = '';\n    }\n\n    /**\n     * @param {string} text\n     * @param {string} scope\n     */\n    function emitKeyword(keyword, scope) {\n      if (keyword === \"\") return;\n\n      emitter.startScope(scope);\n      emitter.addText(keyword);\n      emitter.endScope();\n    }\n\n    /**\n     * @param {CompiledScope} scope\n     * @param {RegExpMatchArray} match\n     */\n    function emitMultiClass(scope, match) {\n      let i = 1;\n      const max = match.length - 1;\n      while (i <= max) {\n        if (!scope._emit[i]) { i++; continue; }\n        const klass = language.classNameAliases[scope[i]] || scope[i];\n        const text = match[i];\n        if (klass) {\n          emitKeyword(text, klass);\n        } else {\n          modeBuffer = text;\n          processKeywords();\n          modeBuffer = \"\";\n        }\n        i++;\n      }\n    }\n\n    /**\n     * @param {CompiledMode} mode - new mode to start\n     * @param {RegExpMatchArray} match\n     */\n    function startNewMode(mode, match) {\n      if (mode.scope && typeof mode.scope === \"string\") {\n        emitter.openNode(language.classNameAliases[mode.scope] || mode.scope);\n      }\n      if (mode.beginScope) {\n        // beginScope just wraps the begin match itself in a scope\n        if (mode.beginScope._wrap) {\n          emitKeyword(modeBuffer, language.classNameAliases[mode.beginScope._wrap] || mode.beginScope._wrap);\n          modeBuffer = \"\";\n        } else if (mode.beginScope._multi) {\n          // at this point modeBuffer should just be the match\n          emitMultiClass(mode.beginScope, match);\n          modeBuffer = \"\";\n        }\n      }\n\n      top = Object.create(mode, { parent: { value: top } });\n      return top;\n    }\n\n    /**\n     * @param {CompiledMode } mode - the mode to potentially end\n     * @param {RegExpMatchArray} match - the latest match\n     * @param {string} matchPlusRemainder - match plus remainder of content\n     * @returns {CompiledMode | void} - the next mode, or if void continue on in current mode\n     */\n    function endOfMode(mode, match, matchPlusRemainder) {\n      let matched = startsWith(mode.endRe, matchPlusRemainder);\n\n      if (matched) {\n        if (mode[\"on:end\"]) {\n          const resp = new Response(mode);\n          mode[\"on:end\"](match, resp);\n          if (resp.isMatchIgnored) matched = false;\n        }\n\n        if (matched) {\n          while (mode.endsParent && mode.parent) {\n            mode = mode.parent;\n          }\n          return mode;\n        }\n      }\n      // even if on:end fires an `ignore` it's still possible\n      // that we might trigger the end node because of a parent mode\n      if (mode.endsWithParent) {\n        return endOfMode(mode.parent, match, matchPlusRemainder);\n      }\n    }\n\n    /**\n     * Handle matching but then ignoring a sequence of text\n     *\n     * @param {string} lexeme - string containing full match text\n     */\n    function doIgnore(lexeme) {\n      if (top.matcher.regexIndex === 0) {\n        // no more regexes to potentially match here, so we move the cursor forward one\n        // space\n        modeBuffer += lexeme[0];\n        return 1;\n      } else {\n        // no need to move the cursor, we still have additional regexes to try and\n        // match at this very spot\n        resumeScanAtSamePosition = true;\n        return 0;\n      }\n    }\n\n    /**\n     * Handle the start of a new potential mode match\n     *\n     * @param {EnhancedMatch} match - the current match\n     * @returns {number} how far to advance the parse cursor\n     */\n    function doBeginMatch(match) {\n      const lexeme = match[0];\n      const newMode = match.rule;\n\n      const resp = new Response(newMode);\n      // first internal before callbacks, then the public ones\n      const beforeCallbacks = [newMode.__beforeBegin, newMode[\"on:begin\"]];\n      for (const cb of beforeCallbacks) {\n        if (!cb) continue;\n        cb(match, resp);\n        if (resp.isMatchIgnored) return doIgnore(lexeme);\n      }\n\n      if (newMode.skip) {\n        modeBuffer += lexeme;\n      } else {\n        if (newMode.excludeBegin) {\n          modeBuffer += lexeme;\n        }\n        processBuffer();\n        if (!newMode.returnBegin && !newMode.excludeBegin) {\n          modeBuffer = lexeme;\n        }\n      }\n      startNewMode(newMode, match);\n      return newMode.returnBegin ? 0 : lexeme.length;\n    }\n\n    /**\n     * Handle the potential end of mode\n     *\n     * @param {RegExpMatchArray} match - the current match\n     */\n    function doEndMatch(match) {\n      const lexeme = match[0];\n      const matchPlusRemainder = codeToHighlight.substring(match.index);\n\n      const endMode = endOfMode(top, match, matchPlusRemainder);\n      if (!endMode) { return NO_MATCH; }\n\n      const origin = top;\n      if (top.endScope && top.endScope._wrap) {\n        processBuffer();\n        emitKeyword(lexeme, top.endScope._wrap);\n      } else if (top.endScope && top.endScope._multi) {\n        processBuffer();\n        emitMultiClass(top.endScope, match);\n      } else if (origin.skip) {\n        modeBuffer += lexeme;\n      } else {\n        if (!(origin.returnEnd || origin.excludeEnd)) {\n          modeBuffer += lexeme;\n        }\n        processBuffer();\n        if (origin.excludeEnd) {\n          modeBuffer = lexeme;\n        }\n      }\n      do {\n        if (top.scope) {\n          emitter.closeNode();\n        }\n        if (!top.skip && !top.subLanguage) {\n          relevance += top.relevance;\n        }\n        top = top.parent;\n      } while (top !== endMode.parent);\n      if (endMode.starts) {\n        startNewMode(endMode.starts, match);\n      }\n      return origin.returnEnd ? 0 : lexeme.length;\n    }\n\n    function processContinuations() {\n      const list = [];\n      for (let current = top; current !== language; current = current.parent) {\n        if (current.scope) {\n          list.unshift(current.scope);\n        }\n      }\n      list.forEach(item => emitter.openNode(item));\n    }\n\n    /** @type {{type?: MatchType, index?: number, rule?: Mode}}} */\n    let lastMatch = {};\n\n    /**\n     *  Process an individual match\n     *\n     * @param {string} textBeforeMatch - text preceding the match (since the last match)\n     * @param {EnhancedMatch} [match] - the match itself\n     */\n    function processLexeme(textBeforeMatch, match) {\n      const lexeme = match && match[0];\n\n      // add non-matched text to the current mode buffer\n      modeBuffer += textBeforeMatch;\n\n      if (lexeme == null) {\n        processBuffer();\n        return 0;\n      }\n\n      // we've found a 0 width match and we're stuck, so we need to advance\n      // this happens when we have badly behaved rules that have optional matchers to the degree that\n      // sometimes they can end up matching nothing at all\n      // Ref: https://github.com/highlightjs/highlight.js/issues/2140\n      if (lastMatch.type === \"begin\" && match.type === \"end\" && lastMatch.index === match.index && lexeme === \"\") {\n        // spit the \"skipped\" character that our regex choked on back into the output sequence\n        modeBuffer += codeToHighlight.slice(match.index, match.index + 1);\n        if (!SAFE_MODE) {\n          /** @type {AnnotatedError} */\n          const err = new Error(`0 width match regex (${languageName})`);\n          err.languageName = languageName;\n          err.badRule = lastMatch.rule;\n          throw err;\n        }\n        return 1;\n      }\n      lastMatch = match;\n\n      if (match.type === \"begin\") {\n        return doBeginMatch(match);\n      } else if (match.type === \"illegal\" && !ignoreIllegals) {\n        // illegal match, we do not continue processing\n        /** @type {AnnotatedError} */\n        const err = new Error('Illegal lexeme \"' + lexeme + '\" for mode \"' + (top.scope || '<unnamed>') + '\"');\n        err.mode = top;\n        throw err;\n      } else if (match.type === \"end\") {\n        const processed = doEndMatch(match);\n        if (processed !== NO_MATCH) {\n          return processed;\n        }\n      }\n\n      // edge case for when illegal matches $ (end of line) which is technically\n      // a 0 width match but not a begin/end match so it's not caught by the\n      // first handler (when ignoreIllegals is true)\n      if (match.type === \"illegal\" && lexeme === \"\") {\n        // advance so we aren't stuck in an infinite loop\n        modeBuffer += \"\\n\";\n        return 1;\n      }\n\n      // infinite loops are BAD, this is a last ditch catch all. if we have a\n      // decent number of iterations yet our index (cursor position in our\n      // parsing) still 3x behind our index then something is very wrong\n      // so we bail\n      if (iterations > 100000 && iterations > match.index * 3) {\n        const err = new Error('potential infinite loop, way more iterations than matches');\n        throw err;\n      }\n\n      /*\n      Why might be find ourselves here?  An potential end match that was\n      triggered but could not be completed.  IE, `doEndMatch` returned NO_MATCH.\n      (this could be because a callback requests the match be ignored, etc)\n\n      This causes no real harm other than stopping a few times too many.\n      */\n\n      modeBuffer += lexeme;\n      return lexeme.length;\n    }\n\n    const language = getLanguage(languageName);\n    if (!language) {\n      error(LANGUAGE_NOT_FOUND.replace(\"{}\", languageName));\n      throw new Error('Unknown language: \"' + languageName + '\"');\n    }\n\n    const md = compileLanguage(language);\n    let result = '';\n    /** @type {CompiledMode} */\n    let top = continuation || md;\n    /** @type Record<string,CompiledMode> */\n    const continuations = {}; // keep continuations for sub-languages\n    const emitter = new options.__emitter(options);\n    processContinuations();\n    let modeBuffer = '';\n    let relevance = 0;\n    let index = 0;\n    let iterations = 0;\n    let resumeScanAtSamePosition = false;\n\n    try {\n      if (!language.__emitTokens) {\n        top.matcher.considerAll();\n\n        for (;;) {\n          iterations++;\n          if (resumeScanAtSamePosition) {\n            // only regexes not matched previously will now be\n            // considered for a potential match\n            resumeScanAtSamePosition = false;\n          } else {\n            top.matcher.considerAll();\n          }\n          top.matcher.lastIndex = index;\n\n          const match = top.matcher.exec(codeToHighlight);\n          // console.log(\"match\", match[0], match.rule && match.rule.begin)\n\n          if (!match) break;\n\n          const beforeMatch = codeToHighlight.substring(index, match.index);\n          const processedCount = processLexeme(beforeMatch, match);\n          index = match.index + processedCount;\n        }\n        processLexeme(codeToHighlight.substring(index));\n      } else {\n        language.__emitTokens(codeToHighlight, emitter);\n      }\n\n      emitter.finalize();\n      result = emitter.toHTML();\n\n      return {\n        language: languageName,\n        value: result,\n        relevance,\n        illegal: false,\n        _emitter: emitter,\n        _top: top\n      };\n    } catch (err) {\n      if (err.message && err.message.includes('Illegal')) {\n        return {\n          language: languageName,\n          value: escape(codeToHighlight),\n          illegal: true,\n          relevance: 0,\n          _illegalBy: {\n            message: err.message,\n            index,\n            context: codeToHighlight.slice(index - 100, index + 100),\n            mode: err.mode,\n            resultSoFar: result\n          },\n          _emitter: emitter\n        };\n      } else if (SAFE_MODE) {\n        return {\n          language: languageName,\n          value: escape(codeToHighlight),\n          illegal: false,\n          relevance: 0,\n          errorRaised: err,\n          _emitter: emitter,\n          _top: top\n        };\n      } else {\n        throw err;\n      }\n    }\n  }\n\n  /**\n   * returns a valid highlight result, without actually doing any actual work,\n   * auto highlight starts with this and it's possible for small snippets that\n   * auto-detection may not find a better match\n   * @param {string} code\n   * @returns {HighlightResult}\n   */\n  function justTextHighlightResult(code) {\n    const result = {\n      value: escape(code),\n      illegal: false,\n      relevance: 0,\n      _top: PLAINTEXT_LANGUAGE,\n      _emitter: new options.__emitter(options)\n    };\n    result._emitter.addText(code);\n    return result;\n  }\n\n  /**\n  Highlighting with language detection. Accepts a string with the code to\n  highlight. Returns an object with the following properties:\n\n  - language (detected language)\n  - relevance (int)\n  - value (an HTML string with highlighting markup)\n  - secondBest (object with the same structure for second-best heuristically\n    detected language, may be absent)\n\n    @param {string} code\n    @param {Array<string>} [languageSubset]\n    @returns {AutoHighlightResult}\n  */\n  function highlightAuto(code, languageSubset) {\n    languageSubset = languageSubset || options.languages || Object.keys(languages);\n    const plaintext = justTextHighlightResult(code);\n\n    const results = languageSubset.filter(getLanguage).filter(autoDetection).map(name =>\n      _highlight(name, code, false)\n    );\n    results.unshift(plaintext); // plaintext is always an option\n\n    const sorted = results.sort((a, b) => {\n      // sort base on relevance\n      if (a.relevance !== b.relevance) return b.relevance - a.relevance;\n\n      // always award the tie to the base language\n      // ie if C++ and Arduino are tied, it's more likely to be C++\n      if (a.language && b.language) {\n        if (getLanguage(a.language).supersetOf === b.language) {\n          return 1;\n        } else if (getLanguage(b.language).supersetOf === a.language) {\n          return -1;\n        }\n      }\n\n      // otherwise say they are equal, which has the effect of sorting on\n      // relevance while preserving the original ordering - which is how ties\n      // have historically been settled, ie the language that comes first always\n      // wins in the case of a tie\n      return 0;\n    });\n\n    const [best, secondBest] = sorted;\n\n    /** @type {AutoHighlightResult} */\n    const result = best;\n    result.secondBest = secondBest;\n\n    return result;\n  }\n\n  /**\n   * Builds new class name for block given the language name\n   *\n   * @param {HTMLElement} element\n   * @param {string} [currentLang]\n   * @param {string} [resultLang]\n   */\n  function updateClassName(element, currentLang, resultLang) {\n    const language = (currentLang && aliases[currentLang]) || resultLang;\n\n    element.classList.add(\"hljs\");\n    element.classList.add(`language-${language}`);\n  }\n\n  /**\n   * Applies highlighting to a DOM node containing code.\n   *\n   * @param {HighlightedHTMLElement} element - the HTML element to highlight\n  */\n  function highlightElement(element) {\n    /** @type HTMLElement */\n    let node = null;\n    const language = blockLanguage(element);\n\n    if (shouldNotHighlight(language)) return;\n\n    fire(\"before:highlightElement\",\n      { el: element, language });\n\n    if (element.dataset.highlighted) {\n      console.log(\"Element previously highlighted. To highlight again, first unset `dataset.highlighted`.\", element);\n      return;\n    }\n\n    // we should be all text, no child nodes (unescaped HTML) - this is possibly\n    // an HTML injection attack - it's likely too late if this is already in\n    // production (the code has likely already done its damage by the time\n    // we're seeing it)... but we yell loudly about this so that hopefully it's\n    // more likely to be caught in development before making it to production\n    if (element.children.length > 0) {\n      if (!options.ignoreUnescapedHTML) {\n        console.warn(\"One of your code blocks includes unescaped HTML. This is a potentially serious security risk.\");\n        console.warn(\"https://github.com/highlightjs/highlight.js/wiki/security\");\n        console.warn(\"The element with unescaped HTML:\");\n        console.warn(element);\n      }\n      if (options.throwUnescapedHTML) {\n        const err = new HTMLInjectionError(\n          \"One of your code blocks includes unescaped HTML.\",\n          element.innerHTML\n        );\n        throw err;\n      }\n    }\n\n    node = element;\n    const text = node.textContent;\n    const result = language ? highlight(text, { language, ignoreIllegals: true }) : highlightAuto(text);\n\n    element.innerHTML = result.value;\n    element.dataset.highlighted = \"yes\";\n    updateClassName(element, language, result.language);\n    element.result = {\n      language: result.language,\n      // TODO: remove with version 11.0\n      re: result.relevance,\n      relevance: result.relevance\n    };\n    if (result.secondBest) {\n      element.secondBest = {\n        language: result.secondBest.language,\n        relevance: result.secondBest.relevance\n      };\n    }\n\n    fire(\"after:highlightElement\", { el: element, result, text });\n  }\n\n  /**\n   * Updates highlight.js global options with the passed options\n   *\n   * @param {Partial<HLJSOptions>} userOptions\n   */\n  function configure(userOptions) {\n    options = inherit(options, userOptions);\n  }\n\n  // TODO: remove v12, deprecated\n  const initHighlighting = () => {\n    highlightAll();\n    deprecated(\"10.6.0\", \"initHighlighting() deprecated.  Use highlightAll() now.\");\n  };\n\n  // TODO: remove v12, deprecated\n  function initHighlightingOnLoad() {\n    highlightAll();\n    deprecated(\"10.6.0\", \"initHighlightingOnLoad() deprecated.  Use highlightAll() now.\");\n  }\n\n  let wantsHighlight = false;\n\n  /**\n   * auto-highlights all pre>code elements on the page\n   */\n  function highlightAll() {\n    function boot() {\n      // if a highlight was requested before DOM was loaded, do now\n      highlightAll();\n    }\n\n    // if we are called too early in the loading process\n    if (document.readyState === \"loading\") {\n      // make sure the event listener is only added once\n      if (!wantsHighlight) {\n        window.addEventListener('DOMContentLoaded', boot, false);\n      }\n      wantsHighlight = true;\n      return;\n    }\n\n    const blocks = document.querySelectorAll(options.cssSelector);\n    blocks.forEach(highlightElement);\n  }\n\n  /**\n   * Register a language grammar module\n   *\n   * @param {string} languageName\n   * @param {LanguageFn} languageDefinition\n   */\n  function registerLanguage(languageName, languageDefinition) {\n    let lang = null;\n    try {\n      lang = languageDefinition(hljs);\n    } catch (error$1) {\n      error(\"Language definition for '{}' could not be registered.\".replace(\"{}\", languageName));\n      // hard or soft error\n      if (!SAFE_MODE) { throw error$1; } else { error(error$1); }\n      // languages that have serious errors are replaced with essentially a\n      // \"plaintext\" stand-in so that the code blocks will still get normal\n      // css classes applied to them - and one bad language won't break the\n      // entire highlighter\n      lang = PLAINTEXT_LANGUAGE;\n    }\n    // give it a temporary name if it doesn't have one in the meta-data\n    if (!lang.name) lang.name = languageName;\n    languages[languageName] = lang;\n    lang.rawDefinition = languageDefinition.bind(null, hljs);\n\n    if (lang.aliases) {\n      registerAliases(lang.aliases, { languageName });\n    }\n  }\n\n  /**\n   * Remove a language grammar module\n   *\n   * @param {string} languageName\n   */\n  function unregisterLanguage(languageName) {\n    delete languages[languageName];\n    for (const alias of Object.keys(aliases)) {\n      if (aliases[alias] === languageName) {\n        delete aliases[alias];\n      }\n    }\n  }\n\n  /**\n   * @returns {string[]} List of language internal names\n   */\n  function listLanguages() {\n    return Object.keys(languages);\n  }\n\n  /**\n   * @param {string} name - name of the language to retrieve\n   * @returns {Language | undefined}\n   */\n  function getLanguage(name) {\n    name = (name || '').toLowerCase();\n    return languages[name] || languages[aliases[name]];\n  }\n\n  /**\n   *\n   * @param {string|string[]} aliasList - single alias or list of aliases\n   * @param {{languageName: string}} opts\n   */\n  function registerAliases(aliasList, { languageName }) {\n    if (typeof aliasList === 'string') {\n      aliasList = [aliasList];\n    }\n    aliasList.forEach(alias => { aliases[alias.toLowerCase()] = languageName; });\n  }\n\n  /**\n   * Determines if a given language has auto-detection enabled\n   * @param {string} name - name of the language\n   */\n  function autoDetection(name) {\n    const lang = getLanguage(name);\n    return lang && !lang.disableAutodetect;\n  }\n\n  /**\n   * Upgrades the old highlightBlock plugins to the new\n   * highlightElement API\n   * @param {HLJSPlugin} plugin\n   */\n  function upgradePluginAPI(plugin) {\n    // TODO: remove with v12\n    if (plugin[\"before:highlightBlock\"] && !plugin[\"before:highlightElement\"]) {\n      plugin[\"before:highlightElement\"] = (data) => {\n        plugin[\"before:highlightBlock\"](\n          Object.assign({ block: data.el }, data)\n        );\n      };\n    }\n    if (plugin[\"after:highlightBlock\"] && !plugin[\"after:highlightElement\"]) {\n      plugin[\"after:highlightElement\"] = (data) => {\n        plugin[\"after:highlightBlock\"](\n          Object.assign({ block: data.el }, data)\n        );\n      };\n    }\n  }\n\n  /**\n   * @param {HLJSPlugin} plugin\n   */\n  function addPlugin(plugin) {\n    upgradePluginAPI(plugin);\n    plugins.push(plugin);\n  }\n\n  /**\n   * @param {HLJSPlugin} plugin\n   */\n  function removePlugin(plugin) {\n    const index = plugins.indexOf(plugin);\n    if (index !== -1) {\n      plugins.splice(index, 1);\n    }\n  }\n\n  /**\n   *\n   * @param {PluginEvent} event\n   * @param {any} args\n   */\n  function fire(event, args) {\n    const cb = event;\n    plugins.forEach(function(plugin) {\n      if (plugin[cb]) {\n        plugin[cb](args);\n      }\n    });\n  }\n\n  /**\n   * DEPRECATED\n   * @param {HighlightedHTMLElement} el\n   */\n  function deprecateHighlightBlock(el) {\n    deprecated(\"10.7.0\", \"highlightBlock will be removed entirely in v12.0\");\n    deprecated(\"10.7.0\", \"Please use highlightElement now.\");\n\n    return highlightElement(el);\n  }\n\n  /* Interface definition */\n  Object.assign(hljs, {\n    highlight,\n    highlightAuto,\n    highlightAll,\n    highlightElement,\n    // TODO: Remove with v12 API\n    highlightBlock: deprecateHighlightBlock,\n    configure,\n    initHighlighting,\n    initHighlightingOnLoad,\n    registerLanguage,\n    unregisterLanguage,\n    listLanguages,\n    getLanguage,\n    registerAliases,\n    autoDetection,\n    inherit,\n    addPlugin,\n    removePlugin\n  });\n\n  hljs.debugMode = function() { SAFE_MODE = false; };\n  hljs.safeMode = function() { SAFE_MODE = true; };\n  hljs.versionString = version;\n\n  hljs.regex = {\n    concat: concat,\n    lookahead: lookahead,\n    either: either,\n    optional: optional,\n    anyNumberOfTimes: anyNumberOfTimes\n  };\n\n  for (const key in MODES) {\n    // @ts-ignore\n    if (typeof MODES[key] === \"object\") {\n      // @ts-ignore\n      deepFreeze(MODES[key]);\n    }\n  }\n\n  // merge all the modes/regexes into our main object\n  Object.assign(hljs, MODES);\n\n  return hljs;\n};\n\n// Other names for the variable may break build script\nconst highlight = HLJS({});\n\n// returns a new instance of the highlighter to be used for extensions\n// check https://github.com/wooorm/lowlight/issues/47\nhighlight.newInstance = () => HLJS({});\n\nmodule.exports = highlight;\nhighlight.HighlightJS = highlight;\nhighlight.default = highlight;\n\n};","~:removed-requires",["~#set",[]],"~:actual-requires",["^5",["~$shadow.js"]],"~:properties",["^5",["initHighlighting","compilerExtensions","versionString","matchIndexes","endRe","message","BINARY_NUMBER_MODE","variants","RE_STARTERS_RE","excludeBegin","resultSoFar","begin","highlightAll","cssSelector","rule","highlightBlock","languageName","NUMBER_MODE","optional","newInstance","UNDERSCORE_TITLE_MODE","index","languageDetectRe","endScope","_emit","innerHTML","prefix","getLanguage","delete","UNDERSCORE_IDENT_RE","__emitter","__beforeBegin","el","matchAt","BACKSLASH_ESCAPE","contains","_beginMatch","key","parent","children","registerLanguage","addPlugin","throwUnescapedHTML","either","classPrefix","C_NUMBER_RE","highlighted","regex","lastIndex","relevance","configure","badRule","SHEBANG","C_NUMBER_MODE","lookahead","initHighlightingOnLoad","_top","block","unregisterLanguage","count","exec","classNameAliases","_illegalBy","highlightElement","autoDetection","mode","buffer","position","name","endsParent","value","isCompiled","ignoreUnescapedHTML","rootNode","rules","context","highlightAuto","illegalRe","text","languages","scope","inherit","safeMode","regexes","concat","_multi","END_SAME_AS_BEGIN","secondBest","debugMode","rawDefinition","matcherRe","ignoreIllegals","keywords","registerAliases","MATCH_NOTHING_RE","IDENT_RE","listLanguages","beginScope","C_BLOCK_COMMENT_MODE","type","isMatchIgnored","BINARY_NUMBER_RE","__proto__","PHRASAL_WORDS_MODE","COMMENT","terminatorEnd","removePlugin","noHighlightRe","HASH_COMMENT_MODE","add","QUOTE_STRING_MODE","set","C_LINE_COMMENT_MODE","starts","re","NUMBER_RE","cachedVariants","REGEXP_MODE","highlight","clear","html","regexIndex","_wrap","language","options","illegal","multiRegexes","data","default","code","beginRe","HighlightJS","end","_emitter","disableAutodetect","keywordPatternRe","TITLE_MODE","METHOD_GUARD","anyNumberOfTimes","stack","matcher","APOS_STRING_MODE","result","joinWith","errorRaised"]],"~:compiled-at",1776822624869,"~:source-map-json","{\n\"version\":3,\n\"file\":\"module$node_modules$highlight_DOT_js$lib$core.js\",\n\"lineCount\":41,\n\"mappings\":\"AAAAA,cAAA,CAAe,CAAf,CAAA,CAAoB,QAAQ,CAACC,EAAD,CAAQC,EAAR,CAAgBC,EAAhB,CAAuBC,CAAvB,CAAgC,CAG5DC,QAASA,EAAU,CAACC,CAAD,CAAM,CACnBA,CAAJ,WAAmBC,IAAnB,CACED,CAAIE,CAAAA,KADN,CAEIF,CAAIG,CAAAA,MAFR,CAGIH,CAAII,CAAAA,GAHR,CAIMC,QAAS,EAAG,CACV,KAAUC,MAAJ,CAAU,kBAAV,CAAN,CADU,CAJlB,CAOWN,CAPX,WAO0BO,IAP1B,GAQEP,CAAIQ,CAAAA,GARN,CASIR,CAAIE,CAAAA,KATR,CAUIF,CAAIG,CAAAA,MAVR,CAWMM,QAAS,EAAG,CACV,KAAUH,MAAJ,CAAU,kBAAV,CAAN,CADU,CAXlB,CAiBAI,OAAOC,CAAAA,MAAP,CAAcX,CAAd,CAEAU,OAAOE,CAAAA,mBAAP,CAA2BZ,CAA3B,CAAgCa,CAAAA,OAAhC,CAAyCC,CAAD,EAAU,CAC1CC,CAAAA,CAAOf,CAAA,CAAIc,CAAJ,CACb,OAAME,EAAO,MAAOD,EAGN,SAAd,GAAKC,CAAL,EAAmC,UAAnC,GAA0BA,CAA1B,EAAmDN,MAAOO,CAAAA,QAAP,CAAgBF,CAAhB,CAAnD,EACEhB,CAAA,CAAWgB,CAAX,CAN8C,CAAlD,CAUA,OAAOf,EA9BgB,CA0DzBkB,QAASA,EAAU,CAACC,CAAD,CAAQ,CACzB,MAAOA,EACJC,CAAAA,OADI,CACI,IADJ,CACU,UADV,CAEJA,CAAAA,OAFI,CAEI,IAFJ,CAEU,SAFV,CAGJA,CAAAA,OAHI,CAGI,IAHJ,CAGU,SAHV,CAIJA,CAAAA,OAJI,CAII,IAJJ,CAIU,WAJV,CAKJA,CAAAA,OALI,CAKI,IALJ;AAKU,WALV,CADkB,CAiB3BC,QAASA,GAAS,CAACC,CAAD,CAAW,GAAGC,CAAd,CAAuB,CAEvC,MAAMC,EAASd,MAAOe,CAAAA,MAAP,CAAc,IAAd,CAEf,KAAK,MAAMC,CAAX,GAAkBJ,EAAlB,CACEE,CAAA,CAAOE,CAAP,CAAA,CAAcJ,CAAA,CAASI,CAAT,CAEhBH,EAAQV,CAAAA,OAAR,CAAgB,QAAQ,CAACb,CAAD,CAAM,CAC5B,IAAK,MAAM0B,EAAX,GAAkB1B,EAAlB,CACEwB,CAAA,CAAOE,EAAP,CAAA,CAAc1B,CAAA,CAAI0B,EAAJ,CAFY,CAA9B,CAKA,OAAyBF,EAZc,CA4SzCG,QAASA,GAAM,CAACC,CAAD,CAAK,CAClB,MAAKA,EAAL,CACkB,QAAlB,GAAI,MAAOA,EAAX,CAAmCA,CAAnC,CAEOA,CAAGD,CAAAA,MAHV,CAAgB,IADE,CAWpBE,QAASA,GAAS,CAACD,CAAD,CAAK,CACrB,MAAOE,GAAA,CAAO,QAAP,CAAcF,CAAd,CAAkB,GAAlB,CADc,CAQvBG,QAASA,GAAgB,CAACH,CAAD,CAAK,CAC5B,MAAOE,GAAA,CAAO,KAAP,CAAcF,CAAd,CAAkB,IAAlB,CADqB,CAQ9BI,QAASA,GAAQ,CAACJ,CAAD,CAAK,CACpB,MAAOE,GAAA,CAAO,KAAP,CAAcF,CAAd,CAAkB,IAAlB,CADa,CAQtBE,QAASA,GAAM,CAAC,GAAGG,CAAJ,CAAU,CAEvB,MADeA,EAAKC,CAAAA,GAAL,CAAUC,CAAD,EAAOR,EAAA,CAAOQ,CAAP,CAAhB,CAA2BC,CAAAA,IAA3BC,CAAgC,EAAhCA,CADQ,CASzBC,QAASA,GAAoB,CAACL,CAAD,CAAO,CAClC,MAAMM,EAAON,CAAA,CAAKA,CAAKO,CAAAA,MAAV,CAAmB,CAAnB,CAEb,OAAoB,QAApB,GAAI,MAAOD,EAAX,EAAgCA,CAAKE,CAAAA,WAArC,GAAqD/B,MAArD,EACEuB,CAAKS,CAAAA,MAAL,CAAYT,CAAKO,CAAAA,MAAjB,CAA0B,CAA1B,CAA6B,CAA7B,CACOD,CAAAA,CAFT,EAIS,EAPyB,CAoBpCI,QAASA,GAAM,CAAC,GAAGV,CAAJ,CAAU,CAMvB,MAHe,GAGf;CAJaK,EAAAC,CAAqBN,CAArBM,CAEHK,CAAAA,OAAL,CAAe,EAAf,CAAoB,IAEzB,EADIX,CAAKC,CAAAA,GAAL,CAAUC,CAAD,EAAOR,EAAA,CAAOQ,CAAP,CAAhB,CAA2BC,CAAAA,IAA3B,CAAgC,GAAhC,CACJ,CAD2C,GALpB,CA+CzBS,QAASA,GAAsB,CAACC,CAAD,CAAU,CAAE,SAAAC,CAAF,CAAV,CAAwB,CACrD,IAAIC,EAAc,CAElB,OAAOF,EAAQZ,CAAAA,GAAR,CAAae,CAAD,EAAW,CAE5B,MAAMC,GADNF,CACME,EADS,CAEXtB,EAAAA,CAAKD,EAAA,CAAOsB,CAAP,CACT,KAAIE,GAAM,EAEV,KAAA,CAAmB,CAAnB,CAAOvB,CAAGY,CAAAA,MAAV,CAAA,CAAsB,CACpB,MAAMY,EAAQC,EAAWC,CAAAA,IAAX,CAAgB1B,CAAhB,CACd,IAAI,CAACwB,CAAL,CAAY,CACVD,EAAA,EAAOvB,CACP,MAFU,CAIZuB,EAAA,EAAOvB,CAAG2B,CAAAA,SAAH,CAAa,CAAb,CAAgBH,CAAMI,CAAAA,KAAtB,CACP5B,EAAA,CAAKA,CAAG2B,CAAAA,SAAH,CAAaH,CAAMI,CAAAA,KAAnB,CAA2BJ,CAAA,CAAM,CAAN,CAASZ,CAAAA,MAApC,CACe,KAApB,GAAIY,CAAA,CAAM,CAAN,CAAA,CAAS,CAAT,CAAJ,EAA4BA,CAAA,CAAM,CAAN,CAA5B,CAEED,EAFF,EAES,IAFT,CAEgBM,MAAA,CAAOC,MAAA,CAAON,CAAA,CAAM,CAAN,CAAP,CAAP,CAA0BF,EAA1B,CAFhB,EAIEC,EACA,EADOC,CAAA,CAAM,CAAN,CACP,CAAiB,GAAjB,GAAIA,CAAA,CAAM,CAAN,CAAJ,EACEJ,CAAA,EANJ,CARoB,CAkBtB,MAAOG,GAxBqB,CAAvB,CAyBJjB,CAAAA,GAzBI,CAyBAN,CAAA,EAAO,IAAGA,CAAH,GAzBP,CAyBiBQ,CAAAA,IAzBjB,CAyBsBW,CAzBtB,CAH8C,CA2RvDY,QAASA,GAAqB,CAACP,CAAD,CAAQQ,CAAR,CAAkB,CAE/B,GAAf,GADeR,CAAMS,CAAAA,KAANC,CAAYV,CAAMI,CAAAA,KAAlBM,CAA0B,CAA1BA,CACf,EACEF,CAASG,CAAAA,WAAT,EAH4C,CAWhDC,QAASA,GAAc,CAACC,CAAD,CAAOC,CAAP,CAAgB,CAEdC,IAAAA,EAAvB,GAAIF,CAAKG,CAAAA,SAAT,GACEH,CAAKI,CAAAA,KACL,CADaJ,CAAKG,CAAAA,SAClB,CAAA,OAAOH,CAAKG,CAAAA,SAFd,CAFqC;AAYvCE,QAASA,GAAa,CAACL,CAAD,CAAOM,CAAP,CAAe,CAC9BA,CAAL,EACKN,CAAKK,CAAAA,aADV,GAQAL,CAAKO,CAAAA,KAQL,CARa,MAQb,CARsBP,CAAKK,CAAAA,aAAcG,CAAAA,KAAnB,CAAyB,GAAzB,CAA8BrC,CAAAA,IAA9B,CAAmC,GAAnC,CAQtB,CARgE,wBAQhE,CAPA6B,CAAKS,CAAAA,aAOL,CAPqBf,EAOrB,CANAM,CAAKU,CAAAA,QAML,CANgBV,CAAKU,CAAAA,QAMrB,EANiCV,CAAKK,CAAAA,aAMtC,CALA,OAAOL,CAAKK,CAAAA,aAKZ,CAAuBH,IAAAA,EAAvB,GAAIF,CAAKW,CAAAA,SAAT,GAAkCX,CAAKW,CAAAA,SAAvC,CAAmD,CAAnD,CAhBA,CADmC,CAwBrCC,QAASA,GAAc,CAACZ,CAAD,CAAOC,CAAP,CAAgB,CAChCY,KAAMC,CAAAA,OAAN,CAAcd,CAAKe,CAAAA,OAAnB,CAAL,GAEAf,CAAKe,CAAAA,OAFL,CAEerC,EAAA,CAAO,GAAGsB,CAAKe,CAAAA,OAAf,CAFf,CADqC,CAUvCC,QAASA,GAAY,CAAChB,CAAD,CAAOC,CAAP,CAAgB,CACnC,GAAKD,CAAKb,CAAAA,KAAV,CAAA,CACA,GAAIa,CAAKO,CAAAA,KAAT,EAAkBP,CAAKiB,CAAAA,GAAvB,CAA4B,KAAU5E,MAAJ,CAAU,6CAAV,CAAN,CAE5B2D,CAAKO,CAAAA,KAAL,CAAaP,CAAKb,CAAAA,KAClB,QAAOa,CAAKb,CAAAA,KAJZ,CADmC,CAYrC+B,QAASA,GAAgB,CAAClB,CAAD,CAAOC,CAAP,CAAgB,CAEhBC,IAAAA,EAAvB,GAAIF,CAAKW,CAAAA,SAAT,GAAkCX,CAAKW,CAAAA,SAAvC,CAAmD,CAAnD,CAFuC,CAoDzCQ,QAASA,GAAe,CAACC,CAAD;AAAcC,CAAd,CAA+BC,CAAA,CARzBC,SAQN,CAAkE,CA+BxFC,QAASA,EAAW,CAACF,EAAD,CAAYG,CAAZ,CAAyB,CACvCJ,CAAJ,GACEI,CADF,CACgBA,CAAYxD,CAAAA,GAAZ,CAAgBC,CAAA,EAAKA,CAAEwD,CAAAA,WAAF,EAArB,CADhB,CAGAD,EAAY7E,CAAAA,OAAZ,CAAoB,QAAQ,CAAC+E,CAAD,CAAU,CACpC,IAAMC,GAAOD,CAAQnB,CAAAA,KAAR,CAAc,GAAd,CACI,EAAA,CAAAoB,EAAA,CAAK,CAAL,CAAuC,KAAA,GAAAA,EAAA,CAAK,CAAL,CAiB1D,GAAA,CADF,CAhBqE,EAgBrE,CAhBqEA,EAAA,CAAK,CAAL,CAgBrE,EACSnC,MAAA,CAAOoC,EAAP,CADT,CAYOC,CAAgBC,CAAAA,QAAhB,CARcJ,EAQmBD,CAAAA,WAAR,EAAzB,CARA,CAAyB,CAAzB,CAA6B,CApBhCM,GAAA,CAAiB,CAAjB,CAAA,CAA4B,CAACV,EAAD,CAAY,EAAZ,CAFQ,CAAtC,CAJ2C,CA7B7C,MAAMU,GAAmBvF,MAAOe,CAAAA,MAAP,CAAc,IAAd,CAIE,SAA3B,GAAI,MAAO4D,EAAX,CACEI,CAAA,CAAYF,CAAZ,CAAuBF,CAAYZ,CAAAA,KAAZ,CAAkB,GAAlB,CAAvB,CADF,CAEWK,KAAMC,CAAAA,OAAN,CAAcM,CAAd,CAAJ,CACLI,CAAA,CAAYF,CAAZ,CAAuBF,CAAvB,CADK,CAGL3E,MAAOwF,CAAAA,IAAP,CAAYb,CAAZ,CAAyBxE,CAAAA,OAAzB,CAAiC,QAAQ,CAAC0E,EAAD,CAAY,CAEnD7E,MAAOyF,CAAAA,MAAP,CACEF,EADF,CAEEb,EAAA,CAAgBC,CAAA,CAAYE,EAAZ,CAAhB,CAAwCD,CAAxC,CAAyDC,EAAzD,CAFF,CAFmD,CAArD,CAQF,OAAOU,GAnBiF,CA8I1FG,QAASA,GAAe,CAACnC,CAAD,CAAOoC,CAAP,CAAgB,CAAE,IAAA3E,CAAF,CAAhB,CAAyB,CAC/C,IAAIwB,EAAS,CACb,OAAMoD,GAAarC,CAAA,CAAKvC,CAAL,CAAnB,CAEM6E,GAAO,EAFb,CAIMC,EAAY,EAElB,KAAK,IAAIC,EAAI,CAAb,CAAgBA,CAAhB,EAAqBJ,CAAQ7D,CAAAA,MAA7B,CAAqCiE,CAAA,EAArC,CACED,CAAA,CAAUC,CAAV,CAAcvD,CAAd,CAEA,CAFwBoD,EAAA,CAAWG,CAAX,CAExB,CADAF,EAAA,CAAKE,CAAL,CAASvD,CAAT,CACA,CADmB,CAAA,CACnB,CAAAA,CAAA,EA9kBuCI,CAAjC,IAAIoD,MAAJ,CA8kBqBL,CAAAzE,CAAQ6E,CAAR7E;AAAY,CAAZA,CA9kBP+E,CAAAA,QAAH,EAAX,CAA2B,GAA3B,CAAiCrD,EAAAA,IAAlC,CAAuC,EAAvC,CAA2Cd,CAAAA,MA8kBhD,CA9kByD,CAklB3DyB,EAAA,CAAKvC,CAAL,CAAA,CAAY8E,CACZvC,EAAA,CAAKvC,CAAL,CAAUkF,CAAAA,KAAV,CAAkBL,EAClBtC,EAAA,CAAKvC,CAAL,CAAUmF,CAAAA,MAAV,CAAmB,CAAA,CAjB4B,CAiFjDC,QAASA,GAAU,CAAC7C,CAAD,CAAO,CACbA,CAVFI,CAAAA,KAAT,EAAwC,QAAxC,GAAkB,MAUPJ,EAVmBI,CAAAA,KAA9B,EAAmE,IAAnE,GAUWJ,CAV8CI,CAAAA,KAAzD,GAUWJ,CATJ8C,CAAAA,UACL,CAQS9C,CATcI,CAAAA,KACvB,CAAA,OAQSJ,CARGI,CAAAA,KAFd,CAY+B,SAA/B,GAAI,MAAOJ,EAAK8C,CAAAA,UAAhB,GACE9C,CAAK8C,CAAAA,UADP,CACoB,CAAEC,MAAO/C,CAAK8C,CAAAA,UAAd,CADpB,CAG6B,SAA7B,GAAI,MAAO9C,EAAKgD,CAAAA,QAAhB,GACEhD,CAAKgD,CAAAA,QADP,CACkB,CAAED,MAAO/C,CAAKgD,CAAAA,QAAd,CADlB,CA/DA,IAAKnC,KAAMC,CAAAA,OAAN,CAmEWd,CAnEQO,CAAAA,KAAnB,CAAL,CAAA,CAEA,GAiEgBP,CAjEPiD,CAAAA,IAAT,EAiEgBjD,CAjEMkD,CAAAA,YAAtB,EAiEgBlD,CAjE2BmD,CAAAA,WAA3C,CAEE,KAtFFC,QAAQC,CAAAA,KAAR,CAqFQC,oEArFR,CAsFQC,CAAAA,CAAN,CAGF,GAA+B,QAA/B,GAAI,MA4DYvD,EA5DA8C,CAAAA,UAAhB;AAA+D,IAA/D,GA4DgB9C,CA5DgC8C,CAAAA,UAAhD,CAEE,KA3FFM,QAAQC,CAAAA,KAAR,CA0FQC,2BA1FR,CA2FQC,CAAAA,CAAN,CAGFpB,EAAA,CAuDgBnC,CAvDhB,CAuDgBA,CAvDWO,CAAAA,KAA3B,CAAkC,CAAE9C,IAAK,YAAP,CAAlC,CAuDgBuC,EAtDXO,CAAAA,KAAL,CAAa3B,EAAA,CAsDGoB,CAtDyBO,CAAAA,KAA5B,CAAmC,CAAEzB,SAAU,EAAZ,CAAnC,CAbb,CAoBA,GAAK+B,KAAMC,CAAAA,OAAN,CAgDSd,CAhDUiB,CAAAA,GAAnB,CAAL,CAAA,CAEA,GA8CcjB,CA9CLiD,CAAAA,IAAT,EA8CcjD,CA9CQwD,CAAAA,UAAtB,EA8CcxD,CA9C2ByD,CAAAA,SAAzC,CAEE,KA1GFL,QAAQC,CAAAA,KAAR,CAyGQC,8DAzGR,CA0GQC,CAAAA,CAAN,CAGF,GAA6B,QAA7B,GAAI,MAyCUvD,EAzCEgD,CAAAA,QAAhB,EAA2D,IAA3D,GAyCchD,CAzCgCgD,CAAAA,QAA9C,CAEE,KA/GFI,QAAQC,CAAAA,KAAR,CA8GQC,yBA9GR,CA+GQC,CAAAA,CAAN,CAGFpB,EAAA,CAoCcnC,CApCd,CAoCcA,CApCaiB,CAAAA,GAA3B,CAAgC,CAAExD,IAAK,UAAP,CAAhC,CAoCcuC,EAnCTiB,CAAAA,GAAL,CAAWrC,EAAA,CAmCGoB,CAnCyBiB,CAAAA,GAA5B,CAAiC,CAAEnC,SAAU,EAAZ,CAAjC,CAbX,CAqCwB,CAgC1B4E,QAASA,GAAe,CAACC,CAAD,CAAW,CAOjCC,QAASA,EAAM,CAAC1G,CAAD,CAAQxB,CAAR,CAAgB,CAC7B,MAAO,KAAI+G,MAAJ,CACL/E,EAAA,CAAOR,CAAP,CADK;AAEL,GAFK,EAGFyG,CAASE,CAAAA,gBAAT,CAA4B,GAA5B,CAAkC,EAHhC,GAIFF,CAASG,CAAAA,YAAT,CAAwB,GAAxB,CAA8B,EAJ5B,GAKFpI,CAAA,CAAS,GAAT,CAAe,EALb,EADsB,CA0M/BqI,QAASA,EAAc,CAAC/D,CAAD,CAAO,CAC5B,MAAMgE,EAAK,IAAIC,EAEfjE,EAAKkE,CAAAA,QAAStH,CAAAA,OAAd,CAAsBuH,EAAA,EAAQH,CAAGI,CAAAA,OAAH,CAAWD,EAAK5D,CAAAA,KAAhB,CAAuB,CAAE8D,KAAMF,EAAR,CAAcpH,KAAM,OAApB,CAAvB,CAA9B,CAEIiD,EAAKsE,CAAAA,aAAT,EACEN,CAAGI,CAAAA,OAAH,CAAWpE,CAAKsE,CAAAA,aAAhB,CAA+B,CAAEvH,KAAM,KAAR,CAA/B,CAEEiD,EAAKe,CAAAA,OAAT,EACEiD,CAAGI,CAAAA,OAAH,CAAWpE,CAAKe,CAAAA,OAAhB,CAAyB,CAAEhE,KAAM,SAAR,CAAzB,CAGF,OAAOiH,EAZqB,CAsD9BO,QAASA,EAAW,CAACvE,CAAD,CAAOM,CAAP,CAAe,CAEjC,GAAIN,CAAKwE,CAAAA,UAAT,CAAqB,MADoBxE,EAGzC,EACED,EADF,CAIEiB,EAJF,CAKE6B,EALF,CAME4B,CANF,CAOE7H,CAAAA,OAPF,CAOU8H,EAAA,EAAOA,EAAA,CAAI1E,CAAJ,CAAUM,CAAV,CAPjB,CASAqD,EAASgB,CAAAA,kBAAmB/H,CAAAA,OAA5B,CAAoC8H,EAAA,EAAOA,EAAA,CAAI1E,CAAJ,CAAUM,CAAV,CAA3C,CAGAN,EAAKS,CAAAA,aAAL,CAAqB,IAErB,EACEJ,EADF,CAIEO,EAJF,CAMEM,EANF,CAOEtE,CAAAA,OAPF,CAOU8H,EAAA,EAAOA,EAAA,CAAI1E,CAAJ,CAAUM,CAAV,CAPjB,CASAN,EAAKwE,CAAAA,UAAL,CAAkB,CAAA,CAElB,KAAII,GAAiB,IACQ,SAA7B,GAAI,MAAO5E,EAAKU,CAAAA,QAAhB;AAAyCV,CAAKU,CAAAA,QAASmE,CAAAA,QAAvD,GAIE7E,CAAKU,CAAAA,QAEL,CAFgBjE,MAAOyF,CAAAA,MAAP,CAAc,EAAd,CAAkBlC,CAAKU,CAAAA,QAAvB,CAEhB,CADAkE,EACA,CADiB5E,CAAKU,CAAAA,QAASmE,CAAAA,QAC/B,CAAA,OAAO7E,CAAKU,CAAAA,QAASmE,CAAAA,QANvB,CAQAD,GAAA,CAAiBA,EAAjB,EAAmC,KAE/B5E,EAAKU,CAAAA,QAAT,GACEV,CAAKU,CAAAA,QADP,CACkBS,EAAA,CAAgBnB,CAAKU,CAAAA,QAArB,CAA+BiD,CAASE,CAAAA,gBAAxC,CADlB,CAvCyC7D,EA2CnC8E,CAAAA,gBAAN,CAAyBlB,CAAA,CAAOgB,EAAP,CAAuB,CAAA,CAAvB,CAErBtE,EAAJ,GACON,CAAKO,CAAAA,KAKV,GALiBP,CAAKO,CAAAA,KAKtB,CAL8B,OAK9B,EAnDuCP,CA+CjC+E,CAAAA,OAIN,CAJgBnB,CAAA,CA/CuB5D,CA+CVO,CAAAA,KAAb,CAIhB,CAHKP,CAAKiB,CAAAA,GAGV,EAHkBjB,CAAKgF,CAAAA,cAGvB,GAHuChF,CAAKiB,CAAAA,GAG5C,CAHkD,OAGlD,EAFIjB,CAAKiB,CAAAA,GAET,GAnDuCjB,CAiDnBiF,CAAAA,KAEpB,CAF4BrB,CAAA,CAjDW5D,CAiDEiB,CAAAA,GAAb,CAE5B,EAnDuCjB,CAkDjCsE,CAAAA,aACN,CADsB5G,EAAA,CAlDiBsC,CAkDJiB,CAAAA,GAAb,CACtB,EAD2C,EAC3C,CAAIjB,CAAKgF,CAAAA,cAAT,EAA2B1E,CAAOgE,CAAAA,aAAlC,GAnDuCtE,CAoD/BsE,CAAAA,aADR,GAC0BtE,CAAKiB,CAAAA,GAAL,CAAW,GAAX,CAAiB,EAD3C,EACiDX,CAAOgE,CAAAA,aADxD,CANF,CAUItE,EAAKe,CAAAA,OAAT,GAvDyCf,CAuDjBkF,CAAAA,SAAxB,CAAoCtB,CAAA,CAAuC5D,CAAKe,CAAAA,OAA5C,CAApC,CACKf,EAAKkE,CAAAA,QAAV,GAAoBlE,CAAKkE,CAAAA,QAAzB;AAAoC,EAApC,CAEAlE,EAAKkE,CAAAA,QAAL,CAAgB,EAAGrG,CAAAA,MAAH,CAAU,GAAGmC,CAAKkE,CAAAA,QAASjG,CAAAA,GAAd,CAAkB,QAAQ,CAACkH,EAAD,CAAI,CACzD,MAAOC,GAAA,CAAwB,MAAN,GAAAD,EAAA,CAAenF,CAAf,CAAsBmF,EAAxC,CADkD,CAA9B,CAAb,CAGhBnF,EAAKkE,CAAAA,QAAStH,CAAAA,OAAd,CAAsB,QAAQ,CAACuI,EAAD,CAAI,CAAEZ,CAAA,CAA+BY,EAA/B,CA7DKnF,CA6DL,CAAF,CAAlC,CAEIA,EAAKqF,CAAAA,MAAT,EACEd,CAAA,CAAYvE,CAAKqF,CAAAA,MAAjB,CAAyB/E,CAAzB,CAhEuCN,EAmEnCsF,CAAAA,OAAN,CAAgBvB,CAAA,CAnEyB/D,CAmEzB,CAChB,OApEyCA,EADR,CAzOnC,KAAMuF,GAAN,CACE/G,WAAW,EAAG,CACZ,IAAKgH,CAAAA,YAAL,CAAoB,EAEpB,KAAKpD,CAAAA,OAAL,CAAe,EACf,KAAKqD,CAAAA,OAAL,CAAe,CACf,KAAKC,CAAAA,QAAL,CAAgB,CALJ,CASdtB,OAAO,CAACzG,CAAD,CAAKW,CAAL,CAAW,CAChBA,CAAKoH,CAAAA,QAAL,CAAgB,IAAKA,CAAAA,QAAL,EAEhB,KAAKF,CAAAA,YAAL,CAAkB,IAAKC,CAAAA,OAAvB,CAAA,CAAkCnH,CAClC,KAAK8D,CAAAA,OAAQuD,CAAAA,IAAb,CAAkB,CAACrH,CAAD,CAAOX,CAAP,CAAlB,CACA,KAAK8H,CAAAA,OAAL,EAjuBqCpG,CAAjC,IAAIoD,MAAJ,CAiuB6B9E,CAjuBf+E,CAAAA,QAAH,EAAX,CAA2B,GAA3B,CAAiCrD,EAAAA,IAAlC,CAAuC,EAAvC,CAA2Cd,CAAAA,MAiuB9C,CAjuBuD,CAiuBvD,CAAuC,CALvB,CAQlBqH,OAAO,EAAG,CACoB,CAA5B,GAAI,IAAKxD,CAAAA,OAAQ7D,CAAAA,MAAjB,GAGE,IAAKc,CAAAA,IAHP,CAGc,EAAAwG,EAAM,IAHpB,CAKA,OAAMC,EAAc,IAAK1D,CAAAA,OAAQnE,CAAAA,GAAb,CAAiB8H,CAAA;AAAMA,CAAA,CAAG,CAAH,CAAvB,CACpB,KAAKC,CAAAA,SAAL,CAAiBpC,CAAA,CAAOhF,EAAA,CAAuBkH,CAAvB,CAAoC,CAAEhH,SAAU,GAAZ,CAApC,CAAP,CAA+D,CAAA,CAA/D,CACjB,KAAKmH,CAAAA,SAAL,CAAiB,CART,CAYV5G,IAAI,CAAC6G,CAAD,CAAI,CACN,IAAKF,CAAAA,SAAUC,CAAAA,SAAf,CAA2B,IAAKA,CAAAA,SAC1B9G,EAAAA,CAAQ,IAAK6G,CAAAA,SAAU3G,CAAAA,IAAf,CAAoB6G,CAApB,CACd,IAAI,CAAC/G,CAAL,CAAc,MAAO,KAGrB,OAAMqD,EAAIrD,CAAMgH,CAAAA,SAAN,CAAgB,CAACJ,EAAD,CAAKvD,EAAL,CAAA,EAAe,CAAf,CAAWA,EAAX,EAA2BtC,IAAAA,EAA3B,GAAoB6F,EAApC,CAAV,CAEMK,GAAY,IAAKZ,CAAAA,YAAL,CAAkBhD,CAAlB,CAGlBrD,EAAMV,CAAAA,MAAN,CAAa,CAAb,CAAgB+D,CAAhB,CAEA,OAAO/F,OAAOyF,CAAAA,MAAP,CAAc/C,CAAd,CAAqBiH,EAArB,CAbD,CA9BV,CA8EA,KAAMnC,GAAN,CACEzF,WAAW,EAAG,CAEZ,IAAK6H,CAAAA,KAAL,CAAa,EAEb,KAAKC,CAAAA,YAAL,CAAoB,EAIpB,KAAKC,CAAAA,UAAL,CADA,IAAKN,CAAAA,SACL,CAHA,IAAKO,CAAAA,KAGL,CAHa,CALD,CAYdC,UAAU,CAAClH,CAAD,CAAQ,CAChB,GAAI,IAAK+G,CAAAA,YAAL,CAAkB/G,CAAlB,CAAJ,CAA8B,MAAO,KAAK+G,CAAAA,YAAL,CAAkB/G,CAAlB,CAErC,OAAM+F,EAAU,IAAIC,EACpB,KAAKc,CAAAA,KAAMK,CAAAA,KAAX,CAAiBnH,CAAjB,CAAwB3C,CAAAA,OAAxB,CAAgC,CAAC,CAACe,EAAD,CAAKW,EAAL,CAAD,CAAA,EAAgBgH,CAAQlB,CAAAA,OAAR,CAAgBzG,EAAhB;AAAoBW,EAApB,CAAhD,CACAgH,EAAQM,CAAAA,OAAR,EAEA,OADA,KAAKU,CAAAA,YAAL,CAAkB/G,CAAlB,CACA,CAD2B+F,CANX,CAUlBqB,0BAA0B,EAAG,CAC3B,MAA2B,EAA3B,GAAO,IAAKJ,CAAAA,UADe,CAI7BK,WAAW,EAAG,CACZ,IAAKL,CAAAA,UAAL,CAAkB,CADN,CAKdnC,OAAO,CAACzG,CAAD,CAAKW,CAAL,CAAW,CAChB,IAAK+H,CAAAA,KAAMV,CAAAA,IAAX,CAAgB,CAAChI,CAAD,CAAKW,CAAL,CAAhB,CACkB,QAAlB,GAAIA,CAAKvB,CAAAA,IAAT,EAA2B,IAAKyJ,CAAAA,KAAL,EAFX,CAMlBnH,IAAI,CAAC6G,CAAD,CAAI,CACN,IAAMW,EAAI,IAAKJ,CAAAA,UAAL,CAAgB,IAAKF,CAAAA,UAArB,CACVM,EAAEZ,CAAAA,SAAF,CAAc,IAAKA,CAAAA,SACf1I,EAAAA,CAASsJ,CAAExH,CAAAA,IAAF,CAAO6G,CAAP,CAiCT,EAAA,IAAKS,CAAAA,0BAAL,EAAJ,EACMpJ,CADN,EACgBA,CAAOgC,CAAAA,KADvB,GACiC,IAAK0G,CAAAA,SADtC,GAEUa,CAEN,CAFW,IAAKL,CAAAA,UAAL,CAAgB,CAAhB,CAEX,CADAK,CAAGb,CAAAA,SACH,CADe,IAAKA,CAAAA,SACpB,CADgC,CAChC,CAAA1I,CAAA,CAASuJ,CAAGzH,CAAAA,IAAH,CAAQ6G,CAAR,CAJb,CAQI3I,EAAJ,GACE,IAAKgJ,CAAAA,UACL,EADmBhJ,CAAOmI,CAAAA,QAC1B,CADqC,CACrC,CAAI,IAAKa,CAAAA,UAAT,GAAwB,IAAKC,CAAAA,KAA7B,EAEE,IAAKI,CAAAA,WAAL,EAJJ,CAQA;MAAOrJ,EApDD,CAtCV,CAmOKoG,CAASgB,CAAAA,kBAAd,GAAkChB,CAASgB,CAAAA,kBAA3C,CAAgE,EAAhE,CAGA,IAAIhB,CAASO,CAAAA,QAAb,EAAyBP,CAASO,CAAAA,QAASnC,CAAAA,QAAlB,CAA2B,MAA3B,CAAzB,CACE,KAAU1F,MAAJ,CAAU,2FAAV,CAAN,CAIFsH,CAASoD,CAAAA,gBAAT,CAA4B3J,EAAA,CAAUuG,CAASoD,CAAAA,gBAAnB,EAAuC,EAAvC,CAE5B,OAAOxC,EAAA,CAA+BZ,CAA/B,CAzV0B,CAuWnCqD,QAASA,GAAkB,CAAChH,CAAD,CAAO,CAChC,MAAKA,EAAL,CAEOA,CAAKgF,CAAAA,cAFZ,EAE8BgC,EAAA,CAAmBhH,CAAKqF,CAAAA,MAAxB,CAF9B,CAAkB,CAAA,CADc,CAgBlCD,QAASA,GAAiB,CAACpF,CAAD,CAAO,CAC3BA,CAAKiH,CAAAA,QAAT,EAAqB,CAACjH,CAAKkH,CAAAA,cAA3B,GACElH,CAAKkH,CAAAA,cADP,CACwBlH,CAAKiH,CAAAA,QAAShJ,CAAAA,GAAd,CAAkB,QAAQ,CAACkJ,CAAD,CAAU,CACxD,MAAO/J,GAAA,CAAU4C,CAAV,CAAgB,CAAEiH,SAAU,IAAZ,CAAhB,CAAoCE,CAApC,CADiD,CAApC,CADxB,CASA,OAAInH,EAAKkH,CAAAA,cAAT,CACSlH,CAAKkH,CAAAA,cADd,CAQIF,EAAA,CAAmBhH,CAAnB,CAAJ,CACS5C,EAAA,CAAU4C,CAAV,CAAgB,CAAEqF,OAAQrF,CAAKqF,CAAAA,MAAL;AAAcjI,EAAA,CAAU4C,CAAKqF,CAAAA,MAAf,CAAd,CAAuC,IAAjD,CAAhB,CADT,CAII5I,MAAOO,CAAAA,QAAP,CAAgBgD,CAAhB,CAAJ,CACS5C,EAAA,CAAU4C,CAAV,CADT,CAKOA,CA3BwB,CA38CjC,KAAMoH,GAAN,CAIE5I,WAAW,CAACwB,CAAD,CAAO,CAEEE,IAAAA,EAAlB,GAAIF,CAAKqH,CAAAA,IAAT,GAA6BrH,CAAKqH,CAAAA,IAAlC,CAAyC,EAAzC,CAEA,KAAKA,CAAAA,IAAL,CAAYrH,CAAKqH,CAAAA,IACjB,KAAKC,CAAAA,cAAL,CAAsB,CAAA,CALN,CAQlBxH,WAAW,EAAG,CACZ,IAAKwH,CAAAA,cAAL,CAAsB,CAAA,CADV,CAZhB,CAkFA,MAAMC,GAAkB,CAAC1K,CAAD,CAAO,CAAE,OAAA2K,CAAF,CAAP,CAAAD,EAElB1K,CAAK4K,CAAAA,UAAL,CAAgB,WAAhB,CAAJ,CACS5K,CAAKM,CAAAA,OAAL,CAAa,WAAb,CAA0B,WAA1B,CADT,CAIIN,CAAKkF,CAAAA,QAAL,CAAc,GAAd,CAAJ,EACQ2F,CACC,CADQ7K,CAAK2D,CAAAA,KAAL,CAAW,GAAX,CACR,CAAA,CACJ,GAAEgH,CAAF,GAAWE,CAAOC,CAAAA,KAAP,EAAX,EADI,CAEL,GAAID,CAAOzJ,CAAAA,GAAP,CAAW,CAACC,CAAD,CAAIsE,CAAJ,CAAA,EAAW,GAAEtE,CAAF,GAAM,GAAI0J,CAAAA,MAAJ,CAAWpF,CAAX,CAAe,CAAf,CAAN,EAAtB,CAFC,CAGLrE,CAAAA,IAHK,CAGA,GAHA,CAFT,EAQQ,GAAEqJ,CAAF,GAAW3K,CAAX,EAIV,MAAMgL,GAAN,CAOErJ,WAAW,CAACsJ,CAAD,CAAYC,CAAZ,CAAqB,CAC9B,IAAKC,CAAAA,MAAL,CAAc,EACd,KAAKC,CAAAA,WAAL,CAAmBF,CAAQE,CAAAA,WAC3BH,EAAUI,CAAAA,IAAV,CAAe,IAAf,CAH8B,CAUhCC,OAAO,CAACC,CAAD,CAAO,CACZ,IAAKJ,CAAAA,MAAL,EAAe/K,CAAA,CAAWmL,CAAX,CADH,CAQdC,QAAQ,CAACC,CAAD,CAAO,CACUA,CApDXlI,CAAAA,KAoDZ;CAEMD,CAEN,CAFkBoH,EAAA,CAAgBe,CAAKlI,CAAAA,KAArB,CAChB,CAAEoH,OAAQ,IAAKS,CAAAA,WAAf,CADgB,CAElB,CAAA,IAAKM,CAAAA,IAAL,CAAUpI,CAAV,CAJA,CADa,CAYfqI,SAAS,CAACF,CAAD,CAAO,CACSA,CAhEXlI,CAAAA,KAgEZ,GAEA,IAAK4H,CAAAA,MAFL,EAzEeS,eAyEf,CADc,CAShBvL,KAAK,EAAG,CACN,MAAO,KAAK8K,CAAAA,MADN,CAURO,IAAI,CAACpI,CAAD,CAAY,CACd,IAAK6H,CAAAA,MAAL,EAAgB,gBAAe7H,CAAf,IADF,CAxDlB,CAmEA,MAAMuI,GAAU,CAACpK,CAAA,CAAO,EAAR,CAAAoK,EAAe,CAE7B,MAAMnL,EAAS,CAAEoL,SAAU,EAAZ,CACflM,OAAOyF,CAAAA,MAAP,CAAc3E,CAAd,CAAsBe,CAAtB,CACA,OAAOf,EAJsB,CAO/B,MAAMqL,GAAN,CACEpK,WAAW,EAAG,CAEZ,IAAKqK,CAAAA,QAAL,CAAgBH,EAAA,EAChB,KAAKI,CAAAA,KAAL,CAAa,CAAC,IAAKD,CAAAA,QAAN,CAHD,CAMV,OAAM,EAAA,CACR,MAAO,KAAKC,CAAAA,KAAL,CAAW,IAAKA,CAAAA,KAAMvK,CAAAA,MAAtB,CAA+B,CAA/B,CADC,CAIN,QAAO,EAAA,CAAE,MAAO,KAAKsK,CAAAA,QAAd,CAGXtM,GAAG,CAAC+L,CAAD,CAAO,CACR,IAAKS,CAAAA,GAAIJ,CAAAA,QAAShD,CAAAA,IAAlB,CAAuB2C,CAAvB,CADQ,CAKVD,QAAQ,CAACjI,CAAD,CAAQ,CAERkI,CAAAA,CAAOI,EAAA,CAAQ,CAAEtI,MAAAA,CAAF,CAAR,CACb,KAAK7D,CAAAA,GAAL,CAAS+L,CAAT,CACA,KAAKQ,CAAAA,KAAMnD,CAAAA,IAAX,CAAgB2C,CAAhB,CAJc,CAOhBE,SAAS,EAAG,CACV,GAAwB,CAAxB;AAAI,IAAKM,CAAAA,KAAMvK,CAAAA,MAAf,CACE,MAAO,KAAKuK,CAAAA,KAAME,CAAAA,GAAX,EAFC,CAQZC,aAAa,EAAG,CACd,IAAA,CAAO,IAAKT,CAAAA,SAAL,EAAP,CAAA,EADc,CAIhBU,MAAM,EAAG,CACP,MAAOC,KAAKC,CAAAA,SAAL,CAAe,IAAKP,CAAAA,QAApB,CAA8B,IAA9B,CAAoC,CAApC,CADA,CAQTX,IAAI,CAACmB,CAAD,CAAU,CAEZ,MAAO,KAAK7K,CAAAA,WAAY8K,CAAAA,KAAjB,CAAuBD,CAAvB,CAAgC,IAAKR,CAAAA,QAArC,CAFK,CAWPS,YAAK,CAACD,CAAD,CAAUf,CAAV,CAAgB,CACN,QAApB,GAAI,MAAOA,EAAX,CACEe,CAAQlB,CAAAA,OAAR,CAAgBG,CAAhB,CADF,CAEWA,CAAKK,CAAAA,QAFhB,GAGEU,CAAQhB,CAAAA,QAAR,CAAiBC,CAAjB,CAEA,CADAA,CAAKK,CAAAA,QAAS/L,CAAAA,OAAd,CAAuB2M,CAAD,EAAW,IAAKD,CAAAA,KAAL,CAAWD,CAAX,CAAoBE,CAApB,CAAjC,CACA,CAAAF,CAAQb,CAAAA,SAAR,CAAkBF,CAAlB,CALF,CAOA,OAAOe,EARmB,CAcrBG,gBAAS,CAAClB,CAAD,CAAO,CACD,QAApB,GAAI,MAAOA,EAAX,EACKA,CAAKK,CAAAA,QADV,GAGIL,CAAKK,CAAAA,QAASc,CAAAA,KAAd,CAAoB1D,CAAA,EAAoB,QAApB,GAAM,MAAOA,EAAjC,CAAJ,CAGEuC,CAAKK,CAAAA,QAHP,CAGkB,CAACL,CAAKK,CAAAA,QAASxK,CAAAA,IAAd,CAAmB,EAAnB,CAAD,CAHlB,CAKEmK,CAAKK,CAAAA,QAAS/L,CAAAA,OAAd,CAAuB2M,CAAD,EAAW,CAC/BX,EAAUY,CAAAA,SAAV,CAAoBD,CAApB,CAD+B,CAAjC,CARF,CADqB,CAvEzB;AAyGA,KAAMG,GAAN,QAA+Bd,GAA/B,CAIEpK,WAAW,CAACuJ,CAAD,CAAU,CACnB,KAAA,EACA,KAAKA,CAAAA,OAAL,CAAeA,CAFI,CAQrBI,OAAO,CAACC,CAAD,CAAO,CACC,EAAb,GAAIA,CAAJ,EAEA,IAAK7L,CAAAA,GAAL,CAAS6L,CAAT,CAHY,CAOduB,UAAU,CAACvJ,CAAD,CAAQ,CAChB,IAAKiI,CAAAA,QAAL,CAAcjI,CAAd,CADgB,CAIlB4C,QAAQ,EAAG,CACT,IAAKwF,CAAAA,SAAL,EADS,CAQXoB,gBAAgB,CAACC,CAAD,CAAUhN,CAAV,CAAgB,CAExByL,CAAAA,CAAOuB,CAAQC,CAAAA,IACjBjN,EAAJ,GAAUyL,CAAKlI,CAAAA,KAAf,CAAwB,YAAWvD,CAAX,EAAxB,CAEA,KAAKN,CAAAA,GAAL,CAAS+L,CAAT,CAL8B,CAQhCyB,MAAM,EAAG,CAEP,MAAgB7M,CADC8M,IAAInC,EAAJmC,CAAiB,IAAjBA,CAAuB,IAAKjC,CAAAA,OAA5BiC,CACD9M,EAAAA,KAAT,EAFA,CAKT+M,QAAQ,EAAG,CACT,IAAKhB,CAAAA,aAAL,EACA,OAAO,CAAA,CAFE,CA5Cb,CA6JA,MAAM7J,GAAa,gDAiFb8K,GAAAA,CAAmB,CACvB3J,MAAO,cADgB,CACAI,UAAW,CADX,CAGnBwJ,GAAAA,CAAmB,CACvB/J,MAAO,QADgB,CAEvBG,MAAO,GAFgB,CAGvBU,IAAK,GAHkB,CAIvBF,QAAS,KAJc,CAKvBmD,SAAU,CAACgG,EAAD,CALa,CAOnBE,EAAAA,CAAoB,CACxBhK,MAAO,QADiB,CAExBG,MAAO,GAFiB;AAGxBU,IAAK,GAHmB,CAIxBF,QAAS,KAJe,CAKxBmD,SAAU,CAACgG,EAAD,CALc,CAkB1B,OAAMG,GAAUA,QAAQ,CAAC9J,CAAD,CAAQU,CAAR,CAAaqJ,CAAA,CAAc,EAA3B,CAA+B,CAC/CtK,CAAAA,CAAO5C,EAAA,CACX,CACEgD,MAAO,SADT,CAEEG,MAAAA,CAFF,CAGEU,IAAAA,CAHF,CAIEiD,SAAU,EAJZ,CADW,CAOXoG,CAPW,CASbtK,EAAKkE,CAAAA,QAASyB,CAAAA,IAAd,CAAmB,CACjBvF,MAAO,QADU,CAIjBG,MAAO,qDAJU,CAKjBU,IAAK,0CALY,CAMjBiC,aAAc,CAAA,CANG,CAOjBvC,UAAW,CAPM,CAAnB,CASM4J,EAAAA,CAAe7L,EAAA,CAEnB,GAFmB,CAGnB,GAHmB,CAInB,IAJmB,CAKnB,IALmB,CAMnB,IANmB,CAOnB,IAPmB,CAQnB,IARmB,CASnB,IATmB,CAUnB,IAVmB,CAWnB,IAXmB,CAYnB,IAZmB,CAcnB,gCAdmB,CAenB,oBAfmB,CAgBnB,mBAhBmB,CAmBrBsB,EAAKkE,CAAAA,QAASyB,CAAAA,IAAd,CACE,CAgBEpF,MAAO1C,EAAA,CACL,MADK,CAEL,GAFK,CAGL0M,CAHK,CAIL,sBAJK,CAKL,MALK,CAhBT,CADF,CAyBA,OAAOvK,EA/D8C,CAAvD;AAiEMwK,GAAsBH,EAAA,CAAQ,IAAR,CAAc,GAAd,CAjE5B,CAkEMI,EAAuBJ,EAAA,CAAQ,MAAR,CAAgB,MAAhB,CAlE7B,CAmEMK,EAAoBL,EAAA,CAAQ,GAAR,CAAa,GAAb,CA+D1B,KAAIM,EAAqBlO,MAAOC,CAAAA,MAAP,CAAc,CACrCkO,UAAW,IAD0B,CAErCT,iBAAkBA,EAFmB,CAGrCD,iBAAkBA,EAHmB,CAIrCW,mBAxDyBA,CACzBzK,MAAO,QADkByK,CAEzBtK,MAxIuBuK,cAsIED,CAGzBlK,UAAW,CAHckK,CAoDY,CAKrCC,iBA/LuBA,cA0Lc,CAMrCT,QAASA,EAN4B,CAOrCI,qBAAsBA,CAPe,CAQrCD,oBAAqBA,EARgB,CASrCO,cAlEoBA,CACpB3K,MAAO,QADa2K,CAEpBxK,MApIkByK,wEAkIED,CAGpBpK,UAAW,CAHSoK,CAyDiB,CAUrCC,YArMkBA,wEA2LmB,CAWrCC,kBArBwBA,QAAQ,CAACjL,CAAD,CAAO,CACvC,MAAOvD,OAAOyF,CAAAA,MAAP,CAAclC,CAAd;AACL,CAEE,WAAY,CAAC6G,CAAD,CAAIqE,CAAJ,CAAAC,EAAa,CAAED,CAAK7D,CAAAA,IAAK+D,CAAAA,WAAV,CAAwBvE,CAAA,CAAE,CAAF,CAA1B,CAF3B,CAIE,SAAU,CAACA,CAAD,CAAIqE,CAAJ,CAAAG,EAAa,CAAMH,CAAK7D,CAAAA,IAAK+D,CAAAA,WAAd,GAA8BvE,CAAA,CAAE,CAAF,CAA9B,EAAoCqE,CAAKpL,CAAAA,WAAL,EAAtC,CAJzB,CADK,CADgC,CAUF,CAYrC4K,kBAAmBA,CAZkB,CAarCY,SA3MeA,cA8LsB,CAcrCC,iBA7MuBA,MA+Lc,CAerCC,aAtCmBA,CAEnBjL,MAAO,sBAFYiL,CAGnB7K,UAAW,CAHQ6K,CAuBkB,CAgBrCC,YA9EkBA,CAClBrL,MAAO,QADWqL,CAElBlL,MAhIgBmL,mBA8HED,CAGlB9K,UAAW,CAHO8K,CA8DmB,CAiBrCC,UA7MgBA,mBA4LqB,CAkBrCC,mBA/JyBA,CACzBpL,MAAO,4IADkBoL,CA6IY,CAmBrCvB,kBAAmBA,CAnBkB;AAoBrCwB,YAnEkBA,CAClBxL,MAAO,QADWwL,CAElBrL,MAAO,iBAFWqL,CAGlB3K,IAAK,YAHa2K,CAIlB1H,SAAU,CACRgG,EADQ,CAER,CACE3J,MAAO,IADT,CAEEU,IAAK,IAFP,CAGEN,UAAW,CAHb,CAIEuD,SAAU,CAACgG,EAAD,CAJZ,CAFQ,CAJQ0B,CA+CmB,CAqBrCC,eA9MqBA,kRAyLgB,CAsBrCC,QA1Mc,CAACxN,CAAA,CAAO,EAAR,CAAAwN,EAAe,CAC7B,MAAMC,EAAe,WACjBzN,EAAK0N,CAAAA,MAAT,GACE1N,CAAKiC,CAAAA,KADP,CACe1C,EAAA,CACXkO,CADW,CAEX,MAFW,CAGXzN,CAAK0N,CAAAA,MAHM,CAIX,MAJW,CADf,CAOA;MAAO5O,GAAA,CAAU,CACfgD,MAAO,MADQ,CAEfG,MAAOwL,CAFQ,CAGf9K,IAAK,GAHU,CAIfN,UAAW,CAJI,CAMf,WAAY,CAACkG,CAAD,CAAIqE,CAAJ,CAAAC,EAAa,CACP,CAAhB,GAAItE,CAAEtH,CAAAA,KAAN,EAAmB2L,CAAKpL,CAAAA,WAAL,EADI,CANV,CAAV,CASJxB,CATI,CATsB,CAoLQ,CAuBrC2N,WAxDiBA,CACjB7L,MAAO,OADU6L,CAEjB1L,MA/Je+K,cA6JEW,CAGjBtL,UAAW,CAHMsL,CAiCoB,CAwBrCC,oBArN0BA,eA6LW,CAyBrCC,sBArD4BA,CAC5B/L,MAAO,OADqB+L,CAE5B5L,MAnK0B2L,eAiKEC,CAG5BxL,UAAW,CAHiBwL,CA4BS,CAAd,CAqIzB,OAAM1H,EAAiB,CAACzE,CAAD,CAAOM,CAAP,CAAAmE,EAAkB,CACvC,GAAKzE,CAAKoM,CAAAA,WAAV,CAAA,CAGA,GAAIpM,CAAKqF,CAAAA,MAAT,CAAiB,KAAUhJ,MAAJ,CAAU,wCAAV,CAAN,CAEXgQ,CAAAA,CAAe5P,MAAOyF,CAAAA,MAAP,CAAc,EAAd,CAAkBlC,CAAlB,CACrBvD,OAAOwF,CAAAA,IAAP,CAAYjC,CAAZ,CAAkBpD,CAAAA,OAAlB,CAA2Ba,CAAD,EAAS,CAAE,OAAOuC,CAAA,CAAKvC,CAAL,CAAT,CAAnC,CAEAuC,EAAKU,CAAAA,QAAL,CAAgB2L,CAAa3L,CAAAA,QAC7BV,EAAKO,CAAAA,KAAL,CAAa1C,EAAA,CAAOwO,CAAaD,CAAAA,WAApB,CAAiCxO,EAAA,CAAUyO,CAAa9L,CAAAA,KAAvB,CAAjC,CACbP,EAAKqF,CAAAA,MAAL;AAAc,CACZ1E,UAAW,CADC,CAEZuD,SAAU,CACRzH,MAAOyF,CAAAA,MAAP,CAAcmK,CAAd,CAA4B,CAAEC,WAAY,CAAA,CAAd,CAA5B,CADQ,CAFE,CAMdtM,EAAKW,CAAAA,SAAL,CAAiB,CAEjB,QAAO0L,CAAaD,CAAAA,WAlBpB,CADuC,CAAzC,CAuBMtK,EAAkB,gDAAA,CAAA,KAAA,CAAA,GAAA,CAvBxB,CA2HMyK,EAAmB,EA3HzB,CAwIMC,GAAO,CAAClJ,CAAD,CAAU,GAAGtF,CAAb,CAAAwO,EAAsB,CACjCpJ,OAAQqJ,CAAAA,GAAR,CAAa,SAAQnJ,CAAR,EAAb,CAAgC,GAAGtF,CAAnC,CADiC,CAxInC,CAgJM0O,GAAa,CAACC,CAAD,CAAUrJ,CAAV,CAAAoJ,EAAsB,CACnCH,CAAA,CAAkB,GAAEI,CAAF,IAAarJ,CAAb,EAAlB,CAAJ,GAEAF,OAAQqJ,CAAAA,GAAR,CAAa,oBAAmBE,CAAnB,KAA+BrJ,CAA/B,EAAb,CACA,CAAAiJ,CAAA,CAAkB,GAAEI,CAAF,IAAarJ,CAAb,EAAlB,CAAA,CAA4C,CAAA,CAH5C,CADuC,CAhJzC,CA6JMC,EAAsBlH,KAAJ,EAsiBxB,MAAMuQ,GAAN,QAAiCvQ,MAAjC,CACEmC,WAAW,CAACqO,CAAD,CAASC,CAAT,CAAe,CACxB,KAAA,CAAMD,CAAN,CACA,KAAKhQ,CAAAA,IAAL,CAAY,oBACZ,KAAKiQ,CAAAA,IAAL,CAAYA,CAHY,CAD5B,CAuCA,MAAMC,GAAWC,MAAA,CAAO,SAAP,CAAjB,CAOMC,GAAOA,QAAQ,CAACC,CAAD,CAAO,CA6C1BC,QAASA,EAAa,CAACC,CAAD,CAAQ,CAC5B,IAAIC,EAAUD,CAAMjN,CAAAA,SAAhBkN,CAA4B,GAEhCA,EAAA,EAAWD,CAAME,CAAAA,UAAN,CAAmBF,CAAME,CAAAA,UAAWnN,CAAAA,SAApC;AAAgD,EAG3D,OAAMhB,EAAQ4I,EAAQwF,CAAAA,gBAAiBlO,CAAAA,IAAzB,CAA8BgO,CAA9B,CACd,OAAIlO,EAAJ,EACQwE,CAKC,CALU6J,EAAA,CAAYrO,CAAA,CAAM,CAAN,CAAZ,CAKV,CAJFwE,CAIE,GAHL6I,EAAA,CA3CqBiB,qFA2CGtQ,CAAAA,OAAnB,CAA2B,IAA3B,CAAiCgC,CAAA,CAAM,CAAN,CAAjC,CAAL,CACA,CAAAqN,EAAA,CAAK,mDAAL,CAA0DY,CAA1D,CAEK,EAAAzJ,CAAA,CAAWxE,CAAA,CAAM,CAAN,CAAX,CAAsB,cAN/B,EASOkO,CACJ7M,CAAAA,KADI,CACE,KADF,CAEJkN,CAAAA,IAFI,CAEEC,EAAD,EAxBD5F,EAAQ6F,CAAAA,aAAcC,CAAAA,IAAtB,CAwBgCF,EAxBhC,CAwBC,EAA0CH,EAAA,CAAYG,EAAZ,CAF3C,CAhBqB,CA0C9BG,QAASA,EAAS,CAACC,CAAD,CAAqBC,CAArB,CAAoCC,CAApC,CAAoD,CAEpE,IAAIC,EACyB,SAA7B,GAAI,MAAOF,EAAX,EAEEC,CACA,CADiBD,CAAcC,CAAAA,cAC/B,CAAAC,EAAA,CAAeF,CAAcrK,CAAAA,QAH/B,GAME+I,EAAA,CAAW,QAAX,CAAqB,qDAArB,CAGA,CAFAA,EAAA,CAAW,QAAX,CAAqB,uGAArB,CAEA;AADAwB,EACA,CADeH,CACf,CAAAI,CAAA,CAAOH,CATT,CAcuB9N,KAAAA,EAAvB,GAAI+N,CAAJ,GAAoCA,CAApC,CAAqD,CAAA,CAArD,CAGMG,EAAAA,CAAU,CACdD,KAAAA,CADc,CAEdxK,SAAUuK,EAFI,CAMhBG,GAAA,CAAK,kBAAL,CAAyBD,CAAzB,CAIM7Q,EAAAA,CAAS6Q,CAAQ7Q,CAAAA,MAAR,CACX6Q,CAAQ7Q,CAAAA,MADG,CAEX+Q,CAAA,CAAWF,CAAQzK,CAAAA,QAAnB,CAA6ByK,CAAQD,CAAAA,IAArC,CAA2CF,CAA3C,CAEJ1Q,EAAO4Q,CAAAA,IAAP,CAAcC,CAAQD,CAAAA,IAEtBE,GAAA,CAAK,iBAAL,CAAwB9Q,CAAxB,CAEA,OAAOA,EAtC6D,CAkDtE+Q,QAASA,EAAU,CAACJ,CAAD,CAAeK,CAAf,CAAgCN,CAAhC,CAAgDO,EAAhD,CAA8D,CAa/EC,QAASA,GAAe,EAAG,CACzB,GAAK1F,CAAIrI,CAAAA,QAAT,CAAA,CAKA,IAAIuF,EAAY,CAChB8C,EAAIjE,CAAAA,gBAAiBmB,CAAAA,SAArB,CAAiC,CAIjC,KAHA,IAAI9G,EAAQ4J,CAAIjE,CAAAA,gBAAiBzF,CAAAA,IAArB,CAA0BqP,EAA1B,CAAZ,CACIC,EAAM,EAEV,CAAOxP,CAAP,CAAA,CAAc,CACZwP,CAAA,EAAOD,EAAWpP,CAAAA,SAAX,CAAqB2G,CAArB,CAAgC9G,CAAMI,CAAAA,KAAtC,CACDqP,EAAAA,CAAOjL,EAASE,CAAAA,gBAAT,CAA4B1E,CAAA,CAAM,CAAN,CAASuC,CAAAA,WAAT,EAA5B,CAAqDvC,CAAA,CAAM,CAAN,CAClE,OAAMkI,GAAmB0B,CAjBfrI,CAAAA,QAAL,CAiByBkO,CAjBzB,CAkBL,IAAIvH,EAAJ,CAAU,CACR,MAAM,CAACwH,EAAD,CAAOC,EAAP,CAAA,CAA2BzH,EACjCwC,GAAQ1B,CAAAA,OAAR,CAAgBwG,CAAhB,CACAA,EAAA,CAAM,EAENI,GAAA,CAAYH,CAAZ,CAAA,EAAqBG,EAAA,CAAYH,CAAZ,CAArB,EAA0C,CAA1C,EAA+C,CAhLhCI,EAiLf,EAAID,EAAA,CAAYH,CAAZ,CAAJ,GAA2CjO,EAA3C,EAAwDmO,EAAxD,CACID,GAAKpH,CAAAA,UAAL,CAAgB,GAAhB,CAAJ,CAGEkH,CAHF;AAGSxP,CAAA,CAAM,CAAN,CAHT,CAME8P,EAAA,CAAY9P,CAAA,CAAM,CAAN,CAAZ,CADiBwE,EAASoD,CAAAA,gBAAT,CAA0B8H,EAA1B,CACjB,EADoDA,EACpD,CAbM,CAAV,IAgBEF,EAAA,EAAOxP,CAAA,CAAM,CAAN,CAET8G,EAAA,CAAY8C,CAAIjE,CAAAA,gBAAiBmB,CAAAA,SACjC9G,EAAA,CAAQ4J,CAAIjE,CAAAA,gBAAiBzF,CAAAA,IAArB,CAA0BqP,EAA1B,CAvBI,CAyBdC,CAAA,EAAOD,EAAWpP,CAAAA,SAAX,CAAqB2G,CAArB,CACP4D,GAAQ1B,CAAAA,OAAR,CAAgBwG,CAAhB,CApCA,CAAA,IACE9E,GAAQ1B,CAAAA,OAAR,CAAgBuG,EAAhB,CAFuB,CAkE3BQ,QAASA,GAAa,EAAG,CACvB,GAAuB,IAAvB,EAAInG,CAAIoG,CAAAA,WAAR,CA3B4B,CAAA,CAC5B,IAAmB,EAAnB,GAAIT,EAAJ,CAAA,CAIA,GAA+B,QAA/B,GAAI,MAAO3F,EAAIoG,CAAAA,WAAf,CAAyC,CACvC,GAAI,CAACC,EAAA,CAAUrG,CAAIoG,CAAAA,WAAd,CAAL,CAAiC,CAC/BtF,EAAQ1B,CAAAA,OAAR,CAAgBuG,EAAhB,CACA,OAAA,CAF+B,CAIjC,IAAAnR,EAAS+Q,CAAA,CAAWvF,CAAIoG,CAAAA,WAAf,CAA4BT,EAA5B,CAAwC,CAAA,CAAxC,CAA8CW,EAAA,CAActG,CAAIoG,CAAAA,WAAlB,CAA9C,CACTE,GAAA,CAActG,CAAIoG,CAAAA,WAAlB,CAAA,CAA8D5R,CAAO+R,CAAAA,IAN9B,CAAzC,IAQE/R,EAAA,CAASgS,EAAA,CAAcb,EAAd,CAA0B3F,CAAIoG,CAAAA,WAAY5Q,CAAAA,MAAhB,CAAyBwK,CAAIoG,CAAAA,WAA7B,CAA2C,IAArE,CAOS,EAApB,CAAIpG,CAAIpI,CAAAA,SAAR,GACEA,EADF,EACepD,CAAOoD,CAAAA,SADtB,CAGAkJ,GAAQD,CAAAA,gBAAR,CAAyBrM,CAAOiS,CAAAA,QAAhC,CAA0CjS,CAAOoG,CAAAA,QAAjD,CAtBA,CAAA,CA0BA,IAGE8K,GAAA,EAEFC;EAAA,CAAa,EANU,CAazBO,QAASA,GAAW,CAACtN,CAAD,CAAUvB,CAAV,CAAiB,CACnB,EAAhB,GAAIuB,CAAJ,GAEAkI,EAAQF,CAAAA,UAAR,CAAmBvJ,CAAnB,CAEA,CADAyJ,EAAQ1B,CAAAA,OAAR,CAAgBxG,CAAhB,CACA,CAAAkI,EAAQ7G,CAAAA,QAAR,EAJA,CADmC,CAYrCyM,QAASA,GAAc,CAACrP,CAAD,CAAQjB,CAAR,CAAe,CACpC,IAAIqD,EAAI,CACR,OAAMkN,GAAMvQ,CAAMZ,CAAAA,MAAZmR,CAAqB,CAC3B,KAAA,CAAOlN,CAAP,EAAYkN,EAAZ,CAAA,CAAiB,CACf,GAAI,CAACtP,CAAMuC,CAAAA,KAAN,CAAYH,CAAZ,CAAL,CAAqB,CAAEA,CAAA,EAAK,SAAP,CACrB,MAAMmN,GAAQhM,EAASoD,CAAAA,gBAAT,CAA0B3G,CAAA,CAAMoC,CAAN,CAA1B,CAARmN,EAA+CvP,CAAA,CAAMoC,CAAN,CAArD,CACM4F,GAAOjJ,CAAA,CAAMqD,CAAN,CACTmN,GAAJ,CACEV,EAAA,CAAY7G,EAAZ,CAAkBuH,EAAlB,CADF,EAGEjB,EAEA,CAFatG,EAEb,CADAqG,EAAA,EACA,CAAAC,EAAA,CAAa,EALf,CAOAlM,EAAA,EAXe,CAHmB,CAsBtCoN,QAASA,GAAY,CAAC5P,CAAD,CAAOb,CAAP,CAAc,CAC7Ba,CAAKI,CAAAA,KAAT,EAAwC,QAAxC,GAAkB,MAAOJ,EAAKI,CAAAA,KAA9B,EACEyJ,EAAQxB,CAAAA,QAAR,CAAiB1E,EAASoD,CAAAA,gBAAT,CAA0B/G,CAAKI,CAAAA,KAA/B,CAAjB,EAA0DJ,CAAKI,CAAAA,KAA/D,CAEEJ,EAAK8C,CAAAA,UAAT,GAEM9C,CAAK8C,CAAAA,UAAWC,CAAAA,KAApB,EACEkM,EAAA,CAAYP,EAAZ,CAAwB/K,EAASoD,CAAAA,gBAAT,CAA0B/G,CAAK8C,CAAAA,UAAWC,CAAAA,KAA1C,CAAxB,EAA4E/C,CAAK8C,CAAAA,UAAWC,CAAAA,KAA5F,CACA,CAAA2L,EAAA,CAAa,EAFf,EAGW1O,CAAK8C,CAAAA,UAAWF,CAAAA,MAH3B,GAKE6M,EAAA,CAAezP,CAAK8C,CAAAA,UAApB;AAAgC3D,CAAhC,CACA,CAAAuP,EAAA,CAAa,EANf,CAFF,CAaA,OADA3F,EACA,CADMtM,MAAOe,CAAAA,MAAP,CAAcwC,CAAd,CAAoB,CAAEM,OAAQ,CAAEpD,MAAO6L,CAAT,CAAV,CAApB,CAhB2B,CA0BnC8G,QAASA,GAAS,CAAC7P,CAAD,CAAOb,CAAP,CAAc2Q,CAAd,CAAkC,CACpB7K,IAAAA,GAALjF,CAAKiF,CAAAA,KAE9B,IAFI8K,EAEJ,EAp5CE5Q,EAo5CF,CAp5CUxB,EAo5CV,EAp5CgBA,EAAG0B,CAAAA,IAAH,CAk5CqByQ,CAl5CrB,CAo5ChB,GAn5C4B,CAm5C5B,GAn5CY3Q,EAAMI,CAAAA,KAm5ClB,CAAa,CACX,GAAIS,CAAA,CAAK,QAAL,CAAJ,CAAoB,CAClB,MAAMkL,GAAO,IAAI9D,EAAJ,CAAapH,CAAb,CACbA,EAAA,CAAK,QAAL,CAAA,CAAeb,CAAf,CAAsB+L,EAAtB,CACIA,GAAK5D,CAAAA,cAAT,GAAyByI,EAAzB,CAAmC,CAAA,CAAnC,CAHkB,CAMpB,GAAIA,EAAJ,CAAa,CACX,IAAA,CAAO/P,CAAKsM,CAAAA,UAAZ,EAA0BtM,CAAKM,CAAAA,MAA/B,CAAA,CACEN,CAAA,CAAOA,CAAKM,CAAAA,MAEd,OAAON,EAJI,CAPF,CAgBb,GAAIA,CAAKgF,CAAAA,cAAT,CACE,MAAO6K,GAAA,CAAU7P,CAAKM,CAAAA,MAAf,CAAuBnB,CAAvB,CAA8B2Q,CAA9B,CApByC,CA6IpDE,QAASA,GAAa,CAACC,CAAD,CAAkB9Q,CAAlB,CAAyB,CAC7C,IAAM+Q,EAAS/Q,CAAT+Q,EAAkB/Q,CAAA,CAAM,CAAN,CAGxBuP,GAAA,EAAcuB,CAEd,IAAc,IAAd,EAAIC,CAAJ,CAEE,MADAhB,GAAA,EACO,CAAA,CAOT,IAAuB,OAAvB,GAAIiB,EAAUpT,CAAAA,IAAd,EAAiD,KAAjD,GAAkCoC,CAAMpC,CAAAA,IAAxC,EAA0DoT,EAAU5Q,CAAAA,KAApE,GAA8EJ,CAAMI,CAAAA,KAApF,EAAwG,EAAxG,GAA6F2Q,CAA7F,CAA4G,CAE1GxB,EAAA,EAAcH,CAAgB7H,CAAAA,KAAhB,CAAsBvH,CAAMI,CAAAA,KAA5B,CAAmCJ,CAAMI,CAAAA,KAAzC,CAAiD,CAAjD,CACd,IAAI,CAAC6Q,EAAL,CAKE,KAHMC,EAGAA,CAHUhU,KAAJ,CAAW,wBAAuB6R,CAAvB,GAAX,CAGNmC;AAFNA,CAAInC,CAAAA,YAEEmC,CAFanC,CAEbmC,CADNA,CAAIC,CAAAA,OACED,CADQF,EAAU9L,CAAAA,IAClBgM,CAAAA,CAAN,CAEF,MAAO,EAVmG,CAY5GF,EAAA,CAAYhR,CAEZ,IAAmB,OAAnB,GAAIA,CAAMpC,CAAAA,IAAV,CAA4B,CAzHD,CAAA,CAAA,CACrBmT,CAAAA,CAyHgB/Q,CAzHP,CAAM,CAAN,CACToR,EAAAA,CAwHgBpR,CAxHAkF,CAAAA,IAEtB,KAAM6G,GAAO,IAAI9D,EAAJ,CAAamJ,CAAb,CAEb,OAAMC,GAAkB,CAACD,CAAQ9P,CAAAA,aAAT,CAAwB8P,CAAA,CAAQ,UAAR,CAAxB,CACxB,KAAK,IAAME,EAAX,GAAiBD,GAAjB,CACE,GAAKC,EAAL,GACAA,EAAA,CAiHoBtR,CAjHpB,CAAU+L,EAAV,CACS5D,CAAL4D,EAAK5D,CAAAA,cAFT,EAEyB,CA7BI,CAA/B,GAAIyB,CAAIzD,CAAAA,OAAQiB,CAAAA,UAAhB,EAGEmI,EACA,EAyByCwB,CA1B3B,CAAO,CAAP,CACd,CAAA,CAAA,CAAO,CAJT,GAQEQ,EACA,CAD2B,CAAA,CAC3B,CAAA,CAAA,CAAO,CATT,CA6B2B,OAAA,CAAA,CAGvBH,CAAQtN,CAAAA,IAAZ,CACEyL,EADF,EACgBwB,CADhB,EAGMK,CAAQrN,CAAAA,YAIZ,GAHEwL,EAGF,EAHgBwB,CAGhB,EADAhB,EAAA,EACA,CAAKqB,CAAQpN,CAAAA,WAAb,EAA6BoN,CAAQrN,CAAAA,YAArC,GACEwL,EADF,CACewB,CADf,CAPF,CAWAN,GAAA,CAAaW,CAAb,CAkGsBpR,CAlGtB,CACA,EAAA,CAAOoR,CAAQpN,CAAAA,WAAR,CAAsB,CAAtB,CAA0B+M,CAAO3R,CAAAA,MAzBb,CA0HzB,MAAO,EADmB,CAErB,GAAmB,SAAnB,GAAIY,CAAMpC,CAAAA,IAAV,EAAgC,CAACkR,CAAjC,CAKL,KAFMoC,EAEAA,CAFUhU,KAAJ,CAAU,kBAAV,CAA+B6T,CAA/B,CAAwC,cAAxC,EAA0DnH,CAAI3I,CAAAA,KAA9D,EAAuE,iBAAvE,EAAsF,GAAtF,CAENiQ,CADNA,CAAIrQ,CAAAA,IACEqQ;AADKtH,CACLsH,CAAAA,CAAN,CACK,GAAmB,KAAnB,GAAIlR,CAAMpC,CAAAA,IAAV,CAA0B,CA/F3BmT,EAAAA,CAgGyB/Q,CAhGhB,CAAM,CAAN,CACT2Q,EAAAA,CAAqBvB,CAAgBjP,CAAAA,SAAhB,CA+FIH,CA/F4BI,CAAAA,KAAhC,CAG3B,IADMoR,EACN,CADgBd,EAAA,CAAU9G,CAAV,CA6Fe5J,CA7Ff,CAAsB2Q,CAAtB,CAChB,CAAA,CAEMc,CAAAA,CAAS7H,CACXA,EAAI/F,CAAAA,QAAR,EAAoB+F,CAAI/F,CAAAA,QAASD,CAAAA,KAAjC,EACEmM,EAAA,EACA,CAAAD,EAAA,CAAYiB,EAAZ,CAAoBnH,CAAI/F,CAAAA,QAASD,CAAAA,KAAjC,CAFF,EAGWgG,CAAI/F,CAAAA,QAAR,EAAoB+F,CAAI/F,CAAAA,QAASJ,CAAAA,MAAjC,EACLsM,EAAA,EACA,CAAAO,EAAA,CAAe1G,CAAI/F,CAAAA,QAAnB,CAoF6B7D,CApF7B,CAFK,EAGIyR,CAAO3N,CAAAA,IAAX,CACLyL,EADK,EACSwB,EADT,EAGCU,CAAOnN,CAAAA,SAIb,EAJ0BmN,CAAOpN,CAAAA,UAIjC,GAHEkL,EAGF,EAHgBwB,EAGhB,EADAhB,EAAA,EACA,CAAI0B,CAAOpN,CAAAA,UAAX,GACEkL,EADF,CACewB,EADf,CAPK,CAWP,GACMnH,EAAI3I,CAAAA,KAMR,EALEyJ,EAAQrB,CAAAA,SAAR,EAKF,CAHKO,CAAI9F,CAAAA,IAGT,EAHkB8F,CAAIoG,CAAAA,WAGtB,GAFExO,EAEF,EAFeoI,CAAIpI,CAAAA,SAEnB,EAAAoI,CAAA,CAAMA,CAAIzI,CAAAA,MAPZ,OAQSyI,CART,GAQiB4H,EAAQrQ,CAAAA,MARzB,CASIqQ,GAAQtL,CAAAA,MAAZ,EACEuK,EAAA,CAAae,EAAQtL,CAAAA,MAArB,CA8D6BlG,CA9D7B,CAEF,GAAA,CAAOyR,CAAOnN,CAAAA,SAAP,CAAmB,CAAnB,CAAuByM,EAAO3R,CAAAA,MAhCrC,CAAA,IAAgB,GAAA,CAAOwO,EA6FrB,IAAI8D,EAAJ,GAAkB9D,EAAlB,CACE,MAAO8D,GAHsB,CAUjC,GAAmB,SAAnB,GAAI1R,CAAMpC,CAAAA,IAAV,EAA2C,EAA3C,GAAgCmT,CAAhC,CAGE,MADAxB,GACO,EADO,IACP,CAAA,CAOT;GAAiB,GAAjB,CAAIoC,EAAJ,EAA2BA,EAA3B,CAAsD,CAAtD,CAAwC3R,CAAMI,CAAAA,KAA9C,CAEE,KADgBlD,MAAJgU,CAAU,2DAAVA,CACZ,CAWF3B,EAAA,EAAcwB,CACd,OAAOA,EAAO3R,CAAAA,MAvE+B,CApS/C,MAAMwQ,GAActS,MAAOe,CAAAA,MAAP,CAAc,IAAd,CA4RpB,KAAI2S,GAAY,EAkFhB,OAAMxM,GAAW6J,EAAA,CAAYU,CAAZ,CACjB,IAAI,CAACvK,EAAL,CAEE,KA3mCJP,QAAQC,CAAAA,KAAR,CA4nB2BoK,qFA8eEtQ,CAAAA,OAAnBmG,CAA2B,IAA3BA,CAAiC4K,CAAjC5K,CA1mCV,CA2mCU,CAAIjH,KAAJ,CAAU,qBAAV,CAAkC6R,CAAlC,CAAiD,GAAjD,CAAN,CAGF,MAAM6C,GAAKrN,EAAA,CAAgBC,EAAhB,CACX,KAAIpG,GAAS,EAAb,CAEIwL,EAAMyF,EAANzF,EAAsBgI,EAE1B,OAAM1B,GAAgB,EAAtB,CACMxF,GAAU,IAAI9B,EAAQiJ,CAAAA,SAAZ,CAAsBjJ,EAAtB,CAzGhBkJ,UAA6B,EAAG,CAC9B,MAAMC,EAAO,EACb,KAAK,IAAIC,EAAUpI,CAAnB,CAAwBoI,CAAxB,GAAoCxN,EAApC,CAA8CwN,CAA9C,CAAwDA,CAAQ7Q,CAAAA,MAAhE,CACM6Q,CAAQ/Q,CAAAA,KAAZ,EACE8Q,CAAKE,CAAAA,OAAL,CAAaD,CAAQ/Q,CAAAA,KAArB,CAGJ8Q,EAAKtU,CAAAA,OAAL,CAAayU,CAAA;AAAQxH,EAAQxB,CAAAA,QAAR,CAAiBgJ,CAAjB,CAArB,CAP8B,CAAhCJ,CA0GA,EACA,KAAIvC,GAAa,EAAjB,CACI/N,GAAY,CADhB,CAGImQ,GADAvR,EACAuR,CADQ,CAFZ,CAIIJ,GAA2B,CAAA,CAE/B,IAAI,CACF,GAAK/M,EAAS2N,CAAAA,YAAd,CAyBE3N,EAAS2N,CAAAA,YAAT,CAAsB/C,CAAtB,CAAuC1E,EAAvC,CAzBF,KAA4B,CAG1B,IAFAd,CAAIzD,CAAAA,OAAQsB,CAAAA,WAAZ,EAEA,CAAA,CAAA,CAAS,CACPkK,EAAA,EACIJ,GAAJ,CAGEA,EAHF,CAG6B,CAAA,CAH7B,CAKE3H,CAAIzD,CAAAA,OAAQsB,CAAAA,WAAZ,EAEFmC,EAAIzD,CAAAA,OAAQW,CAAAA,SAAZ,CAAwB1G,EAExB,OAAMJ,EAAQ4J,CAAIzD,CAAAA,OAAQjG,CAAAA,IAAZ,CAAiBkP,CAAjB,CAGd,IAAI,CAACpP,CAAL,CAAY,KAEZ,OAAMiN,EAAcmC,CAAgBjP,CAAAA,SAAhB,CAA0BC,EAA1B,CAAiCJ,CAAMI,CAAAA,KAAvC,CAApB,CACMgS,EAAiBvB,EAAA,CAAc5D,CAAd,CAA2BjN,CAA3B,CACvBI,GAAA,CAAQJ,CAAMI,CAAAA,KAAd,CAAsBgS,CAlBf,CAoBTvB,EAAA,CAAczB,CAAgBjP,CAAAA,SAAhB,CAA0BC,EAA1B,CAAd,CAvB0B,CA4B5BsK,EAAQI,CAAAA,QAAR,EACA1M,GAAA,CAASsM,EAAQE,CAAAA,MAAR,EAET,OAAO,CACLpG,SAAUuK,CADL,CAELhR,MAAOK,EAFF,CAGLoD,UAAAA,EAHK,CAILI,QAAS,CAAA,CAJJ,CAKLyO,SAAU3F,EALL,CAMLyF,KAAMvG,CAND,CAhCL,CAwCF,MAAOsH,CAAP,CAAY,CACZ,GAAIA,CAAI/M,CAAAA,OAAR,EAAmB+M,CAAI/M,CAAAA,OAAQvB,CAAAA,QAAZ,CAAqB,SAArB,CAAnB,CACE,MAAO,CACL4B,SAAUuK,CADL,CAELhR,MAjkBKD,CAikBE,CAAOsR,CAAP,CAFF,CAGLxN,QAAS,CAAA,CAHJ,CAILJ,UAAW,CAJN;AAKL6Q,WAAY,CACVlO,QAAS+M,CAAI/M,CAAAA,OADH,CAEV/D,MAAAA,EAFU,CAGV6O,QAASG,CAAgB7H,CAAAA,KAAhB,CAAsBnH,EAAtB,CAA8B,GAA9B,CAAmCA,EAAnC,CAA2C,GAA3C,CAHC,CAIVS,KAAMqQ,CAAIrQ,CAAAA,IAJA,CAKVyR,YAAalU,EALH,CALP,CAYLiS,SAAU3F,EAZL,CAcF,IAAIuG,EAAJ,CACL,MAAO,CACLzM,SAAUuK,CADL,CAELhR,MAhlBKD,CAglBE,CAAOsR,CAAP,CAFF,CAGLxN,QAAS,CAAA,CAHJ,CAILJ,UAAW,CAJN,CAKL+Q,YAAarB,CALR,CAMLb,SAAU3F,EANL,CAOLyF,KAAMvG,CAPD,CAUP,MAAMsH,EAAN,CA3BU,CA3aiE,CAkdjFsB,QAASA,GAAuB,CAACxD,CAAD,CAAO,CACrC,MAAM5Q,EAAS,CACbL,MAtmBSD,CAsmBF,CAAOkR,CAAP,CADM,CAEbpN,QAAS,CAAA,CAFI,CAGbJ,UAAW,CAHE,CAIb2O,KAAMsC,EAJO,CAKbpC,SAAU,IAAIzH,EAAQiJ,CAAAA,SAAZ,CAAsBjJ,EAAtB,CALG,CAOfxK,EAAOiS,CAAAA,QAASrH,CAAAA,OAAhB,CAAwBgG,CAAxB,CACA,OAAO5Q,EAT8B,CA0BvCgS,QAASA,GAAa,CAACpB,CAAD,CAAO0D,CAAP,CAAuB,CAC3CA,CAAA,CAAiBA,CAAjB,EAAmC9J,EAAQqH,CAAAA,SAA3C,EAAwD3S,MAAOwF,CAAAA,IAAP,CAAYmN,EAAZ,CACxD,KAAM0C,EAAYH,EAAA,CAAwBxD,CAAxB,CAEZ4D,EAAAA,CAAUF,CAAeG,CAAAA,MAAf,CAAsBxE,EAAtB,CAAmCwE,CAAAA,MAAnC,CAA0CC,EAA1C,CAAyDhU,CAAAA,GAAzD,CAA6DpB,EAAA,EAC3EyR,CAAA,CAAWzR,EAAX,CAAiBsR,CAAjB,CAAuB,CAAA,CAAvB,CADc,CAGhB4D,EAAQX,CAAAA,OAAR,CAAgBU,CAAhB,CAEMI,EAAAA,CAASH,CAAQI,CAAAA,IAAR,CAAa,CAACC,EAAD,CAAIC,EAAJ,CAAA,EAAU,CAEpC,GAAID,EAAEzR,CAAAA,SAAN;AAAoB0R,EAAE1R,CAAAA,SAAtB,CAAiC,MAAO0R,GAAE1R,CAAAA,SAAT,CAAqByR,EAAEzR,CAAAA,SAIxD,IAAIyR,EAAEzO,CAAAA,QAAN,EAAkB0O,EAAE1O,CAAAA,QAApB,CAA8B,CAC5B,GAAI6J,EAAA,CAAY4E,EAAEzO,CAAAA,QAAd,CAAwB2O,CAAAA,UAA5B,GAA2CD,EAAE1O,CAAAA,QAA7C,CACE,MAAO,EACF,IAAI6J,EAAA,CAAY6E,EAAE1O,CAAAA,QAAd,CAAwB2O,CAAAA,UAA5B,GAA2CF,EAAEzO,CAAAA,QAA7C,CACL,MAAO,CAAC,CAJkB,CAY9B,MAAO,EAlB6B,CAAvB,CAqBf,OAAM,CAAC4O,EAAD,CAAOC,EAAP,CAAA,CAAqBN,CAGZK,GACRC,CAAAA,UAAP,CAAoBA,EAEpB,OAHeD,GAjC4B,CA0D7CE,QAASA,EAAgB,CAACC,CAAD,CAAU,CAGjC,IAAM/O,EAAWwJ,CAAA,CAAcuF,CAAd,CAEjB,IA7oBO,CAAA3K,EAAQ6F,CAAAA,aAAcC,CAAAA,IAAtB,CA6oBgBlK,CA7oBhB,CA6oBP,CAKA,GAHA0K,EAAA,CAAK,yBAAL,CACE,CAAEtI,GAAI2M,CAAN,CAAe/O,SAAAA,CAAf,CADF,CAGoBgP,CAAhBD,CAAQE,CAAAA,OAAQD,CAAAA,WAApB,CACEvP,OAAQqJ,CAAAA,GAAR,CAAY,wFAAZ,CAAsGiG,CAAtG,CADF,KAAA,CAUA,GAA8B,CAA9B,CAAIA,CAAQ/J,CAAAA,QAASpK,CAAAA,MAArB,GACOwJ,EAAQ8K,CAAAA,mBAMDC;CALV1P,OAAQoJ,CAAAA,IAAR,CAAa,+FAAb,CAGA,CAFApJ,OAAQoJ,CAAAA,IAAR,CAAa,2DAAb,CAEA,CADApJ,OAAQoJ,CAAAA,IAAR,CAAa,kCAAb,CACA,CAAApJ,OAAQoJ,CAAAA,IAAR,CAAakG,CAAb,CAEUI,EAAR/K,EAAQ+K,CAAAA,kBAPd,EAYI,KAJYzC,KAAIzD,EAAJyD,CACV,kDADUA,CAEVqC,CAAQK,CAAAA,SAFE1C,CAIZ,CAKJ,IAAMjI,EADCsK,CACWM,CAAAA,WAAlB,CACMzV,GAASoG,CAAA,CAAWmK,CAAA,CAAU1F,CAAV,CAAgB,CAAEzE,SAAAA,CAAF,CAAYsK,eAAgB,CAAA,CAA5B,CAAhB,CAAX,CAAiEsB,EAAA,CAAcnH,CAAd,CAEhFsK,EAAQK,CAAAA,SAAR,CAAoBxV,EAAOL,CAAAA,KAC3BwV,EAAQE,CAAAA,OAAQD,CAAAA,WAAhB,CAA8B,KApDxBhP,EAAAA,CAqDmBA,CArDnBA,EAA2BsP,EAAA,CAqDRtP,CArDQ,CAA3BA,EAqD6BpG,EAAOoG,CAAAA,QAA1B+O,EAnDRQ,CAAAA,SAAU3W,CAAAA,GAAlB,CAAsB,MAAtB,CAmDgBmW;CAlDRQ,CAAAA,SAAU3W,CAAAA,GAAlB,CAAuB,YAAWoH,CAAX,EAAvB,CAmDA+O,EAAQnV,CAAAA,MAAR,CAAiB,CACfoG,SAAUpG,EAAOoG,CAAAA,QADF,CAGfhG,GAAIJ,EAAOoD,CAAAA,SAHI,CAIfA,UAAWpD,EAAOoD,CAAAA,SAJH,CAMbpD,GAAOiV,CAAAA,UAAX,GACEE,CAAQF,CAAAA,UADV,CACuB,CACnB7O,SAAUpG,EAAOiV,CAAAA,UAAW7O,CAAAA,QADT,CAEnBhD,UAAWpD,EAAOiV,CAAAA,UAAW7R,CAAAA,SAFV,CADvB,CAOA0N,GAAA,CAAK,wBAAL,CAA+B,CAAEtI,GAAI2M,CAAN,CAAenV,OAAAA,EAAf,CAAuB6K,KAAAA,CAAvB,CAA/B,CA9CA,CAViC,CAqFnC+K,QAASA,EAAY,EAAG,CACtBC,QAASA,EAAI,EAAG,CAEdD,CAAA,EAFc,CAMY,SAA5B,GAAIE,QAASC,CAAAA,UAAb,EAEOC,EAGL,EAFEC,MAAOC,CAAAA,gBAAP,CAAwB,kBAAxB,CAA4CL,CAA5C,CAAkD,CAAA,CAAlD,CAEF,CAAAG,EAAA,CAAiB,CAAA,CALnB,EASeF,QAASK,CAAAA,gBAATC,CAA0B5L,EAAQ6L,CAAAA,WAAlCD,CACR/W,CAAAA,OAAP,CAAe6V,CAAf,CAjBsB,CA2ExBjF,QAASA,GAAW,CAAC3Q,CAAD,CAAO,CACzBA,CAAA,CAAoB6E,CAAZ7E,CAAY6E,EAAJ,EAAIA,EAAAA,WAAb,EACP,OAAO0N,GAAA,CAAUvS,CAAV,CAAP,EAA0BuS,EAAA,CAAU6D,EAAA,CAAQpW,CAAR,CAAV,CAFD,CAU3BgX,QAASA,GAAe,CAACC,CAAD;AAAY,CAAE,aAAA5F,CAAF,CAAZ,CAA8B,CAC3B,QAAzB,GAAI,MAAO4F,EAAX,GACEA,CADF,CACc,CAACA,CAAD,CADd,CAGAA,EAAUlX,CAAAA,OAAV,CAAkBmX,CAAA,EAAS,CAAEd,EAAA,CAAQc,CAAMrS,CAAAA,WAAN,EAAR,CAAA,CAA+BwM,CAAjC,CAA3B,CAJoD,CAWtD+D,QAASA,GAAa,CAACpV,CAAD,CAAO,CAE3B,OADMmX,CACN,CADaxG,EAAA,CAAY3Q,CAAZ,CACb,GAAe,CAACmX,CAAKC,CAAAA,iBAFM,CAU7BC,QAASA,GAAgB,CAACC,CAAD,CAAS,CAE5BA,CAAA,CAAO,uBAAP,CAAJ,EAAuC,CAACA,CAAA,CAAO,yBAAP,CAAxC,GACEA,CAAA,CAAO,yBAAP,CADF,CACuC9M,CAAD,EAAU,CAC5C8M,CAAA,CAAO,uBAAP,CAAA,CACE1X,MAAOyF,CAAAA,MAAP,CAAc,CAAEkL,MAAO/F,CAAKtB,CAAAA,EAAd,CAAd,CAAkCsB,CAAlC,CADF,CAD4C,CADhD,CAOI8M,EAAA,CAAO,sBAAP,CAAJ,EAAsC,CAACA,CAAA,CAAO,wBAAP,CAAvC,GACEA,CAAA,CAAO,wBAAP,CADF,CACsC9M,CAAD,EAAU,CAC3C8M,CAAA,CAAO,sBAAP,CAAA,CACE1X,MAAOyF,CAAAA,MAAP,CAAc,CAAEkL,MAAO/F,CAAKtB,CAAAA,EAAd,CAAd,CAAkCsB,CAAlC,CADF,CAD2C,CAD/C,CATgC,CAyClCgH,QAASA,GAAI,CAAC+F,CAAD,CAAQpW,CAAR,CAAc,CAEzBqW,EAAQzX,CAAAA,OAAR,CAAgB,QAAQ,CAACuX,CAAD,CAAS,CAC/B,GAAIA,CAAA,CAFKC,CAEL,CAAJ,CACED,CAAA,CAHOC,CAGP,CAAA,CAAWpW,CAAX,CAF6B,CAAjC,CAFyB;AAp5B3B,MAAMoR,GAAY3S,MAAOe,CAAAA,MAAP,CAAc,IAAd,CAAlB,CAEMyV,GAAUxW,MAAOe,CAAAA,MAAP,CAAc,IAAd,CAFhB,CAIM6W,GAAU,EAIhB,KAAIjE,GAAY,CAAA,CAGhB,OAAMwB,GAAqB,CAAEqC,kBAAmB,CAAA,CAArB,CAA2BpX,KAAM,YAAjC,CAA+CqH,SAAU,EAAzD,CAK3B,KAAI6D,GAAU,CACZ8K,oBAAqB,CAAA,CADT,CAEZC,mBAAoB,CAAA,CAFR,CAGZlF,cAAe,oBAHH,CAIZL,iBAAkB,6BAJN,CAKZtF,YAAa,OALD,CAMZ2L,YAAa,UAND,CAOZxE,UAAW,IAPC,CAUZ4B,UAAWtH,EAVC,CAAd,CA4uBI6J,GAAiB,CAAA,CA6KrB9W,OAAOyF,CAAAA,MAAP,CAAcgL,CAAd,CAAoB,CAClBY,UAAAA,CADkB,CAElByB,cAAAA,EAFkB,CAGlB4D,aAAAA,CAHkB,CAIlBV,iBAAAA,CAJkB,CAMlB6B,eAdFC,QAAgC,CAACxO,CAAD,CAAK,CACnC2G,EAAA,CAAW,QAAX,CAAqB,kDAArB,CACAA;EAAA,CAAW,QAAX,CAAqB,kCAArB,CAEA,OAAO+F,EAAA,CAAiB1M,CAAjB,CAJ4B,CAQjB,CAOlByO,UApMFA,QAAkB,CAACC,CAAD,CAAc,CAC9B1M,EAAA,CAxvBY3K,EAwvBF,CAAQ2K,EAAR,CAAiB0M,CAAjB,CADoB,CA6LZ,CAQlBC,iBAhMuB,EAAAA,EAAM,CAC7BvB,CAAA,EACAzG,GAAA,CAAW,QAAX,CAAqB,yDAArB,CAF6B,CAwLX,CASlBiI,uBA3LFA,QAA+B,EAAG,CAChCxB,CAAA,EACAzG,GAAA,CAAW,QAAX,CAAqB,+DAArB,CAFgC,CAkLd,CAUlBkI,iBAxJFA,QAAyB,CAAC1G,CAAD,CAAe2G,CAAf,CAAmC,CAC1D,IAAIb,EAAO,IACX,IAAI,CACFA,CAAA,CAAOa,CAAA,CAAmB3H,CAAnB,CADL,CAEF,MAAO4H,EAAP,CAAgB,CAl5CpB1R,OAAQC,CAAAA,KAAR,CAm5CU,uDAAwDlG,CAAAA,OAAxDmG,CAAgE,IAAhEA,CAAsE4K,CAAtE5K,CAn5CV,CAq5CI,IAAK8M,EAAL,CAr5CJhN,OAAQC,CAAAA,KAAR,CAq5CoDyR,EAr5CpD,CAq5CI,KAAkB,MAAMA,GAAN,CAKlBd,CAAA,CAAOpC,EARS,CAWboC,CAAKnX,CAAAA,IAAV;CAAgBmX,CAAKnX,CAAAA,IAArB,CAA4BqR,CAA5B,CACAkB,GAAA,CAAUlB,CAAV,CAAA,CAA0B8F,CAC1BA,EAAKe,CAAAA,aAAL,CAAqBF,CAAmBG,CAAAA,IAAnB,CAAwB,IAAxB,CAA8B9H,CAA9B,CAEjB8G,EAAKf,CAAAA,OAAT,EACEY,EAAA,CAAgBG,CAAKf,CAAAA,OAArB,CAA8B,CAAE/E,aAAAA,CAAF,CAA9B,CApBwD,CA8IxC,CAWlB+G,mBA5HFA,QAA2B,CAAC/G,CAAD,CAAe,CACxC,OAAOkB,EAAA,CAAUlB,CAAV,CACP,KAAK,MAAM6F,CAAX,GAAoBtX,OAAOwF,CAAAA,IAAP,CAAYgR,EAAZ,CAApB,CACMA,EAAA,CAAQc,CAAR,CAAJ,GAAuB7F,CAAvB,EACE,OAAO+E,EAAA,CAAQc,CAAR,CAJ6B,CAiHtB,CAYlBmB,cAjHFA,QAAsB,EAAG,CACvB,MAAOzY,OAAOwF,CAAAA,IAAP,CAAYmN,EAAZ,CADgB,CAqGL,CAalB5B,YAAAA,EAbkB,CAclBqG,gBAAAA,EAdkB,CAelB5B,cAAAA,EAfkB,CAgBlBkD,QAp8BY/X,EAo7BM,CAiBlBgY,UA1DFA,QAAkB,CAACjB,CAAD,CAAS,CACzBD,EAAA,CAAiBC,CAAjB,CACAE,GAAQ1O,CAAAA,IAAR,CAAawO,CAAb,CAFyB,CAyCP,CAkBlBkB,aAnDFA,QAAqB,CAAClB,CAAD,CAAS,CACtB5U,CAAAA,CAAQ8U,EAAQiB,CAAAA,OAAR,CAAgBnB,CAAhB,CACA,EAAC,CAAf,GAAI5U,CAAJ,EACE8U,EAAQ5V,CAAAA,MAAR,CAAec,CAAf,CAAsB,CAAtB,CAH0B,CAiCV,CAApB,CAqBA2N,EAAKqI,CAAAA,SAAL,CAAiBC,QAAQ,EAAG,CAAEpF,EAAA,CAAY,CAAA,CAAd,CAC5BlD,EAAKuI,CAAAA,QAAL,CAAgBC,QAAQ,EAAG,CAAEtF,EAAA,CAAY,CAAA,CAAd,CAC3BlD,EAAKyI,CAAAA,aAAL,CAn/BYhJ,SAq/BZO,EAAKlO,CAAAA,KAAL;AAAa,CACXnB,OAAQA,EADG,CAEXD,UAAWA,EAFA,CAGXc,OAAQA,EAHG,CAIXX,SAAUA,EAJC,CAKXD,iBAAkBA,EALP,CAQb,KAAK,MAAML,CAAX,GAAkBkN,EAAlB,CAE4B,QAA1B,GAAI,MAAOA,EAAA,CAAMlN,CAAN,CAAX,EAEE3B,CAAA,CAAW6O,CAAA,CAAMlN,CAAN,CAAX,CAKJhB,OAAOyF,CAAAA,MAAP,CAAcgL,CAAd,CAAoBvC,CAApB,CAEA,OAAOuC,EAx9BmB,CA49BtBY,GAAAA,CAAYb,EAAA,CAAK,EAAL,CAIlBa,GAAU8H,CAAAA,WAAV,CAAwB,EAAAC,EAAM5I,EAAA,CAAK,EAAL,CAE9BrR,GAAOC,CAAAA,OAAP,CAAiBiS,EACjBA,GAAUgI,CAAAA,WAAV,CAAwBhI,EACxBA,GAAUiI,CAAAA,OAAV,CAAoBjI,EAriFwC;\",\n\"sources\":[\"node_modules/highlight_DOT_js/lib/core.js\"],\n\"sourcesContent\":[\"shadow$provide[9] = function(global,require,module,exports) {\\n/* eslint-disable no-multi-assign */\\n\\nfunction deepFreeze(obj) {\\n  if (obj instanceof Map) {\\n    obj.clear =\\n      obj.delete =\\n      obj.set =\\n        function () {\\n          throw new Error('map is read-only');\\n        };\\n  } else if (obj instanceof Set) {\\n    obj.add =\\n      obj.clear =\\n      obj.delete =\\n        function () {\\n          throw new Error('set is read-only');\\n        };\\n  }\\n\\n  // Freeze self\\n  Object.freeze(obj);\\n\\n  Object.getOwnPropertyNames(obj).forEach((name) => {\\n    const prop = obj[name];\\n    const type = typeof prop;\\n\\n    // Freeze prop if it is an object or function and also not already frozen\\n    if ((type === 'object' || type === 'function') && !Object.isFrozen(prop)) {\\n      deepFreeze(prop);\\n    }\\n  });\\n\\n  return obj;\\n}\\n\\n/** @typedef {import('highlight.js').CallbackResponse} CallbackResponse */\\n/** @typedef {import('highlight.js').CompiledMode} CompiledMode */\\n/** @implements CallbackResponse */\\n\\nclass Response {\\n  /**\\n   * @param {CompiledMode} mode\\n   */\\n  constructor(mode) {\\n    // eslint-disable-next-line no-undefined\\n    if (mode.data === undefined) mode.data = {};\\n\\n    this.data = mode.data;\\n    this.isMatchIgnored = false;\\n  }\\n\\n  ignoreMatch() {\\n    this.isMatchIgnored = true;\\n  }\\n}\\n\\n/**\\n * @param {string} value\\n * @returns {string}\\n */\\nfunction escapeHTML(value) {\\n  return value\\n    .replace(/&/g, '&amp;')\\n    .replace(/</g, '&lt;')\\n    .replace(/>/g, '&gt;')\\n    .replace(/\\\"/g, '&quot;')\\n    .replace(/'/g, '&#x27;');\\n}\\n\\n/**\\n * performs a shallow merge of multiple objects into one\\n *\\n * @template T\\n * @param {T} original\\n * @param {Record<string,any>[]} objects\\n * @returns {T} a single new object\\n */\\nfunction inherit$1(original, ...objects) {\\n  /** @type Record<string,any> */\\n  const result = Object.create(null);\\n\\n  for (const key in original) {\\n    result[key] = original[key];\\n  }\\n  objects.forEach(function(obj) {\\n    for (const key in obj) {\\n      result[key] = obj[key];\\n    }\\n  });\\n  return /** @type {T} */ (result);\\n}\\n\\n/**\\n * @typedef {object} Renderer\\n * @property {(text: string) => void} addText\\n * @property {(node: Node) => void} openNode\\n * @property {(node: Node) => void} closeNode\\n * @property {() => string} value\\n */\\n\\n/** @typedef {{scope?: string, language?: string, sublanguage?: boolean}} Node */\\n/** @typedef {{walk: (r: Renderer) => void}} Tree */\\n/** */\\n\\nconst SPAN_CLOSE = '</span>';\\n\\n/**\\n * Determines if a node needs to be wrapped in <span>\\n *\\n * @param {Node} node */\\nconst emitsWrappingTags = (node) => {\\n  // rarely we can have a sublanguage where language is undefined\\n  // TODO: track down why\\n  return !!node.scope;\\n};\\n\\n/**\\n *\\n * @param {string} name\\n * @param {{prefix:string}} options\\n */\\nconst scopeToCSSClass = (name, { prefix }) => {\\n  // sub-language\\n  if (name.startsWith(\\\"language:\\\")) {\\n    return name.replace(\\\"language:\\\", \\\"language-\\\");\\n  }\\n  // tiered scope: comment.line\\n  if (name.includes(\\\".\\\")) {\\n    const pieces = name.split(\\\".\\\");\\n    return [\\n      `${prefix}${pieces.shift()}`,\\n      ...(pieces.map((x, i) => `${x}${\\\"_\\\".repeat(i + 1)}`))\\n    ].join(\\\" \\\");\\n  }\\n  // simple scope\\n  return `${prefix}${name}`;\\n};\\n\\n/** @type {Renderer} */\\nclass HTMLRenderer {\\n  /**\\n   * Creates a new HTMLRenderer\\n   *\\n   * @param {Tree} parseTree - the parse tree (must support `walk` API)\\n   * @param {{classPrefix: string}} options\\n   */\\n  constructor(parseTree, options) {\\n    this.buffer = \\\"\\\";\\n    this.classPrefix = options.classPrefix;\\n    parseTree.walk(this);\\n  }\\n\\n  /**\\n   * Adds texts to the output stream\\n   *\\n   * @param {string} text */\\n  addText(text) {\\n    this.buffer += escapeHTML(text);\\n  }\\n\\n  /**\\n   * Adds a node open to the output stream (if needed)\\n   *\\n   * @param {Node} node */\\n  openNode(node) {\\n    if (!emitsWrappingTags(node)) return;\\n\\n    const className = scopeToCSSClass(node.scope,\\n      { prefix: this.classPrefix });\\n    this.span(className);\\n  }\\n\\n  /**\\n   * Adds a node close to the output stream (if needed)\\n   *\\n   * @param {Node} node */\\n  closeNode(node) {\\n    if (!emitsWrappingTags(node)) return;\\n\\n    this.buffer += SPAN_CLOSE;\\n  }\\n\\n  /**\\n   * returns the accumulated buffer\\n  */\\n  value() {\\n    return this.buffer;\\n  }\\n\\n  // helpers\\n\\n  /**\\n   * Builds a span element\\n   *\\n   * @param {string} className */\\n  span(className) {\\n    this.buffer += `<span class=\\\"${className}\\\">`;\\n  }\\n}\\n\\n/** @typedef {{scope?: string, language?: string, children: Node[]} | string} Node */\\n/** @typedef {{scope?: string, language?: string, children: Node[]} } DataNode */\\n/** @typedef {import('highlight.js').Emitter} Emitter */\\n/**  */\\n\\n/** @returns {DataNode} */\\nconst newNode = (opts = {}) => {\\n  /** @type DataNode */\\n  const result = { children: [] };\\n  Object.assign(result, opts);\\n  return result;\\n};\\n\\nclass TokenTree {\\n  constructor() {\\n    /** @type DataNode */\\n    this.rootNode = newNode();\\n    this.stack = [this.rootNode];\\n  }\\n\\n  get top() {\\n    return this.stack[this.stack.length - 1];\\n  }\\n\\n  get root() { return this.rootNode; }\\n\\n  /** @param {Node} node */\\n  add(node) {\\n    this.top.children.push(node);\\n  }\\n\\n  /** @param {string} scope */\\n  openNode(scope) {\\n    /** @type Node */\\n    const node = newNode({ scope });\\n    this.add(node);\\n    this.stack.push(node);\\n  }\\n\\n  closeNode() {\\n    if (this.stack.length > 1) {\\n      return this.stack.pop();\\n    }\\n    // eslint-disable-next-line no-undefined\\n    return undefined;\\n  }\\n\\n  closeAllNodes() {\\n    while (this.closeNode());\\n  }\\n\\n  toJSON() {\\n    return JSON.stringify(this.rootNode, null, 4);\\n  }\\n\\n  /**\\n   * @typedef { import(\\\"./html_renderer\\\").Renderer } Renderer\\n   * @param {Renderer} builder\\n   */\\n  walk(builder) {\\n    // this does not\\n    return this.constructor._walk(builder, this.rootNode);\\n    // this works\\n    // return TokenTree._walk(builder, this.rootNode);\\n  }\\n\\n  /**\\n   * @param {Renderer} builder\\n   * @param {Node} node\\n   */\\n  static _walk(builder, node) {\\n    if (typeof node === \\\"string\\\") {\\n      builder.addText(node);\\n    } else if (node.children) {\\n      builder.openNode(node);\\n      node.children.forEach((child) => this._walk(builder, child));\\n      builder.closeNode(node);\\n    }\\n    return builder;\\n  }\\n\\n  /**\\n   * @param {Node} node\\n   */\\n  static _collapse(node) {\\n    if (typeof node === \\\"string\\\") return;\\n    if (!node.children) return;\\n\\n    if (node.children.every(el => typeof el === \\\"string\\\")) {\\n      // node.text = node.children.join(\\\"\\\");\\n      // delete node.children;\\n      node.children = [node.children.join(\\\"\\\")];\\n    } else {\\n      node.children.forEach((child) => {\\n        TokenTree._collapse(child);\\n      });\\n    }\\n  }\\n}\\n\\n/**\\n  Currently this is all private API, but this is the minimal API necessary\\n  that an Emitter must implement to fully support the parser.\\n\\n  Minimal interface:\\n\\n  - addText(text)\\n  - __addSublanguage(emitter, subLanguageName)\\n  - startScope(scope)\\n  - endScope()\\n  - finalize()\\n  - toHTML()\\n\\n*/\\n\\n/**\\n * @implements {Emitter}\\n */\\nclass TokenTreeEmitter extends TokenTree {\\n  /**\\n   * @param {*} options\\n   */\\n  constructor(options) {\\n    super();\\n    this.options = options;\\n  }\\n\\n  /**\\n   * @param {string} text\\n   */\\n  addText(text) {\\n    if (text === \\\"\\\") { return; }\\n\\n    this.add(text);\\n  }\\n\\n  /** @param {string} scope */\\n  startScope(scope) {\\n    this.openNode(scope);\\n  }\\n\\n  endScope() {\\n    this.closeNode();\\n  }\\n\\n  /**\\n   * @param {Emitter & {root: DataNode}} emitter\\n   * @param {string} name\\n   */\\n  __addSublanguage(emitter, name) {\\n    /** @type DataNode */\\n    const node = emitter.root;\\n    if (name) node.scope = `language:${name}`;\\n\\n    this.add(node);\\n  }\\n\\n  toHTML() {\\n    const renderer = new HTMLRenderer(this, this.options);\\n    return renderer.value();\\n  }\\n\\n  finalize() {\\n    this.closeAllNodes();\\n    return true;\\n  }\\n}\\n\\n/**\\n * @param {string} value\\n * @returns {RegExp}\\n * */\\n\\n/**\\n * @param {RegExp | string } re\\n * @returns {string}\\n */\\nfunction source(re) {\\n  if (!re) return null;\\n  if (typeof re === \\\"string\\\") return re;\\n\\n  return re.source;\\n}\\n\\n/**\\n * @param {RegExp | string } re\\n * @returns {string}\\n */\\nfunction lookahead(re) {\\n  return concat('(?=', re, ')');\\n}\\n\\n/**\\n * @param {RegExp | string } re\\n * @returns {string}\\n */\\nfunction anyNumberOfTimes(re) {\\n  return concat('(?:', re, ')*');\\n}\\n\\n/**\\n * @param {RegExp | string } re\\n * @returns {string}\\n */\\nfunction optional(re) {\\n  return concat('(?:', re, ')?');\\n}\\n\\n/**\\n * @param {...(RegExp | string) } args\\n * @returns {string}\\n */\\nfunction concat(...args) {\\n  const joined = args.map((x) => source(x)).join(\\\"\\\");\\n  return joined;\\n}\\n\\n/**\\n * @param { Array<string | RegExp | Object> } args\\n * @returns {object}\\n */\\nfunction stripOptionsFromArgs(args) {\\n  const opts = args[args.length - 1];\\n\\n  if (typeof opts === 'object' && opts.constructor === Object) {\\n    args.splice(args.length - 1, 1);\\n    return opts;\\n  } else {\\n    return {};\\n  }\\n}\\n\\n/** @typedef { {capture?: boolean} } RegexEitherOptions */\\n\\n/**\\n * Any of the passed expresssions may match\\n *\\n * Creates a huge this | this | that | that match\\n * @param {(RegExp | string)[] | [...(RegExp | string)[], RegexEitherOptions]} args\\n * @returns {string}\\n */\\nfunction either(...args) {\\n  /** @type { object & {capture?: boolean} }  */\\n  const opts = stripOptionsFromArgs(args);\\n  const joined = '('\\n    + (opts.capture ? \\\"\\\" : \\\"?:\\\")\\n    + args.map((x) => source(x)).join(\\\"|\\\") + \\\")\\\";\\n  return joined;\\n}\\n\\n/**\\n * @param {RegExp | string} re\\n * @returns {number}\\n */\\nfunction countMatchGroups(re) {\\n  return (new RegExp(re.toString() + '|')).exec('').length - 1;\\n}\\n\\n/**\\n * Does lexeme start with a regular expression match at the beginning\\n * @param {RegExp} re\\n * @param {string} lexeme\\n */\\nfunction startsWith(re, lexeme) {\\n  const match = re && re.exec(lexeme);\\n  return match && match.index === 0;\\n}\\n\\n// BACKREF_RE matches an open parenthesis or backreference. To avoid\\n// an incorrect parse, it additionally matches the following:\\n// - [...] elements, where the meaning of parentheses and escapes change\\n// - other escape sequences, so we do not misparse escape sequences as\\n//   interesting elements\\n// - non-matching or lookahead parentheses, which do not capture. These\\n//   follow the '(' with a '?'.\\nconst BACKREF_RE = /\\\\[(?:[^\\\\\\\\\\\\]]|\\\\\\\\.)*\\\\]|\\\\(\\\\??|\\\\\\\\([1-9][0-9]*)|\\\\\\\\./;\\n\\n// **INTERNAL** Not intended for outside usage\\n// join logically computes regexps.join(separator), but fixes the\\n// backreferences so they continue to match.\\n// it also places each individual regular expression into it's own\\n// match group, keeping track of the sequencing of those match groups\\n// is currently an exercise for the caller. :-)\\n/**\\n * @param {(string | RegExp)[]} regexps\\n * @param {{joinWith: string}} opts\\n * @returns {string}\\n */\\nfunction _rewriteBackreferences(regexps, { joinWith }) {\\n  let numCaptures = 0;\\n\\n  return regexps.map((regex) => {\\n    numCaptures += 1;\\n    const offset = numCaptures;\\n    let re = source(regex);\\n    let out = '';\\n\\n    while (re.length > 0) {\\n      const match = BACKREF_RE.exec(re);\\n      if (!match) {\\n        out += re;\\n        break;\\n      }\\n      out += re.substring(0, match.index);\\n      re = re.substring(match.index + match[0].length);\\n      if (match[0][0] === '\\\\\\\\' && match[1]) {\\n        // Adjust the backreference.\\n        out += '\\\\\\\\' + String(Number(match[1]) + offset);\\n      } else {\\n        out += match[0];\\n        if (match[0] === '(') {\\n          numCaptures++;\\n        }\\n      }\\n    }\\n    return out;\\n  }).map(re => `(${re})`).join(joinWith);\\n}\\n\\n/** @typedef {import('highlight.js').Mode} Mode */\\n/** @typedef {import('highlight.js').ModeCallback} ModeCallback */\\n\\n// Common regexps\\nconst MATCH_NOTHING_RE = /\\\\b\\\\B/;\\nconst IDENT_RE = '[a-zA-Z]\\\\\\\\w*';\\nconst UNDERSCORE_IDENT_RE = '[a-zA-Z_]\\\\\\\\w*';\\nconst NUMBER_RE = '\\\\\\\\b\\\\\\\\d+(\\\\\\\\.\\\\\\\\d+)?';\\nconst C_NUMBER_RE = '(-?)(\\\\\\\\b0[xX][a-fA-F0-9]+|(\\\\\\\\b\\\\\\\\d+(\\\\\\\\.\\\\\\\\d*)?|\\\\\\\\.\\\\\\\\d+)([eE][-+]?\\\\\\\\d+)?)'; // 0x..., 0..., decimal, float\\nconst BINARY_NUMBER_RE = '\\\\\\\\b(0b[01]+)'; // 0b...\\nconst RE_STARTERS_RE = '!|!=|!==|%|%=|&|&&|&=|\\\\\\\\*|\\\\\\\\*=|\\\\\\\\+|\\\\\\\\+=|,|-|-=|/=|/|:|;|<<|<<=|<=|<|===|==|=|>>>=|>>=|>=|>>>|>>|>|\\\\\\\\?|\\\\\\\\[|\\\\\\\\{|\\\\\\\\(|\\\\\\\\^|\\\\\\\\^=|\\\\\\\\||\\\\\\\\|=|\\\\\\\\|\\\\\\\\||~';\\n\\n/**\\n* @param { Partial<Mode> & {binary?: string | RegExp} } opts\\n*/\\nconst SHEBANG = (opts = {}) => {\\n  const beginShebang = /^#![ ]*\\\\//;\\n  if (opts.binary) {\\n    opts.begin = concat(\\n      beginShebang,\\n      /.*\\\\b/,\\n      opts.binary,\\n      /\\\\b.*/);\\n  }\\n  return inherit$1({\\n    scope: 'meta',\\n    begin: beginShebang,\\n    end: /$/,\\n    relevance: 0,\\n    /** @type {ModeCallback} */\\n    \\\"on:begin\\\": (m, resp) => {\\n      if (m.index !== 0) resp.ignoreMatch();\\n    }\\n  }, opts);\\n};\\n\\n// Common modes\\nconst BACKSLASH_ESCAPE = {\\n  begin: '\\\\\\\\\\\\\\\\[\\\\\\\\s\\\\\\\\S]', relevance: 0\\n};\\nconst APOS_STRING_MODE = {\\n  scope: 'string',\\n  begin: '\\\\'',\\n  end: '\\\\'',\\n  illegal: '\\\\\\\\n',\\n  contains: [BACKSLASH_ESCAPE]\\n};\\nconst QUOTE_STRING_MODE = {\\n  scope: 'string',\\n  begin: '\\\"',\\n  end: '\\\"',\\n  illegal: '\\\\\\\\n',\\n  contains: [BACKSLASH_ESCAPE]\\n};\\nconst PHRASAL_WORDS_MODE = {\\n  begin: /\\\\b(a|an|the|are|I'm|isn't|don't|doesn't|won't|but|just|should|pretty|simply|enough|gonna|going|wtf|so|such|will|you|your|they|like|more)\\\\b/\\n};\\n/**\\n * Creates a comment mode\\n *\\n * @param {string | RegExp} begin\\n * @param {string | RegExp} end\\n * @param {Mode | {}} [modeOptions]\\n * @returns {Partial<Mode>}\\n */\\nconst COMMENT = function(begin, end, modeOptions = {}) {\\n  const mode = inherit$1(\\n    {\\n      scope: 'comment',\\n      begin,\\n      end,\\n      contains: []\\n    },\\n    modeOptions\\n  );\\n  mode.contains.push({\\n    scope: 'doctag',\\n    // hack to avoid the space from being included. the space is necessary to\\n    // match here to prevent the plain text rule below from gobbling up doctags\\n    begin: '[ ]*(?=(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):)',\\n    end: /(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):/,\\n    excludeBegin: true,\\n    relevance: 0\\n  });\\n  const ENGLISH_WORD = either(\\n    // list of common 1 and 2 letter words in English\\n    \\\"I\\\",\\n    \\\"a\\\",\\n    \\\"is\\\",\\n    \\\"so\\\",\\n    \\\"us\\\",\\n    \\\"to\\\",\\n    \\\"at\\\",\\n    \\\"if\\\",\\n    \\\"in\\\",\\n    \\\"it\\\",\\n    \\\"on\\\",\\n    // note: this is not an exhaustive list of contractions, just popular ones\\n    /[A-Za-z]+['](d|ve|re|ll|t|s|n)/, // contractions - can't we'd they're let's, etc\\n    /[A-Za-z]+[-][a-z]+/, // `no-way`, etc.\\n    /[A-Za-z][a-z]{2,}/ // allow capitalized words at beginning of sentences\\n  );\\n  // looking like plain text, more likely to be a comment\\n  mode.contains.push(\\n    {\\n      // TODO: how to include \\\", (, ) without breaking grammars that use these for\\n      // comment delimiters?\\n      // begin: /[ ]+([()\\\"]?([A-Za-z'-]{3,}|is|a|I|so|us|[tT][oO]|at|if|in|it|on)[.]?[()\\\":]?([.][ ]|[ ]|\\\\))){3}/\\n      // ---\\n\\n      // this tries to find sequences of 3 english words in a row (without any\\n      // \\\"programming\\\" type syntax) this gives us a strong signal that we've\\n      // TRULY found a comment - vs perhaps scanning with the wrong language.\\n      // It's possible to find something that LOOKS like the start of the\\n      // comment - but then if there is no readable text - good chance it is a\\n      // false match and not a comment.\\n      //\\n      // for a visual example please see:\\n      // https://github.com/highlightjs/highlight.js/issues/2827\\n\\n      begin: concat(\\n        /[ ]+/, // necessary to prevent us gobbling up doctags like /* @author Bob Mcgill */\\n        '(',\\n        ENGLISH_WORD,\\n        /[.]?[:]?([.][ ]|[ ])/,\\n        '){3}') // look for 3 words in a row\\n    }\\n  );\\n  return mode;\\n};\\nconst C_LINE_COMMENT_MODE = COMMENT('//', '$');\\nconst C_BLOCK_COMMENT_MODE = COMMENT('/\\\\\\\\*', '\\\\\\\\*/');\\nconst HASH_COMMENT_MODE = COMMENT('#', '$');\\nconst NUMBER_MODE = {\\n  scope: 'number',\\n  begin: NUMBER_RE,\\n  relevance: 0\\n};\\nconst C_NUMBER_MODE = {\\n  scope: 'number',\\n  begin: C_NUMBER_RE,\\n  relevance: 0\\n};\\nconst BINARY_NUMBER_MODE = {\\n  scope: 'number',\\n  begin: BINARY_NUMBER_RE,\\n  relevance: 0\\n};\\nconst REGEXP_MODE = {\\n  scope: \\\"regexp\\\",\\n  begin: /\\\\/(?=[^/\\\\n]*\\\\/)/,\\n  end: /\\\\/[gimuy]*/,\\n  contains: [\\n    BACKSLASH_ESCAPE,\\n    {\\n      begin: /\\\\[/,\\n      end: /\\\\]/,\\n      relevance: 0,\\n      contains: [BACKSLASH_ESCAPE]\\n    }\\n  ]\\n};\\nconst TITLE_MODE = {\\n  scope: 'title',\\n  begin: IDENT_RE,\\n  relevance: 0\\n};\\nconst UNDERSCORE_TITLE_MODE = {\\n  scope: 'title',\\n  begin: UNDERSCORE_IDENT_RE,\\n  relevance: 0\\n};\\nconst METHOD_GUARD = {\\n  // excludes method names from keyword processing\\n  begin: '\\\\\\\\.\\\\\\\\s*' + UNDERSCORE_IDENT_RE,\\n  relevance: 0\\n};\\n\\n/**\\n * Adds end same as begin mechanics to a mode\\n *\\n * Your mode must include at least a single () match group as that first match\\n * group is what is used for comparison\\n * @param {Partial<Mode>} mode\\n */\\nconst END_SAME_AS_BEGIN = function(mode) {\\n  return Object.assign(mode,\\n    {\\n      /** @type {ModeCallback} */\\n      'on:begin': (m, resp) => { resp.data._beginMatch = m[1]; },\\n      /** @type {ModeCallback} */\\n      'on:end': (m, resp) => { if (resp.data._beginMatch !== m[1]) resp.ignoreMatch(); }\\n    });\\n};\\n\\nvar MODES = /*#__PURE__*/Object.freeze({\\n  __proto__: null,\\n  APOS_STRING_MODE: APOS_STRING_MODE,\\n  BACKSLASH_ESCAPE: BACKSLASH_ESCAPE,\\n  BINARY_NUMBER_MODE: BINARY_NUMBER_MODE,\\n  BINARY_NUMBER_RE: BINARY_NUMBER_RE,\\n  COMMENT: COMMENT,\\n  C_BLOCK_COMMENT_MODE: C_BLOCK_COMMENT_MODE,\\n  C_LINE_COMMENT_MODE: C_LINE_COMMENT_MODE,\\n  C_NUMBER_MODE: C_NUMBER_MODE,\\n  C_NUMBER_RE: C_NUMBER_RE,\\n  END_SAME_AS_BEGIN: END_SAME_AS_BEGIN,\\n  HASH_COMMENT_MODE: HASH_COMMENT_MODE,\\n  IDENT_RE: IDENT_RE,\\n  MATCH_NOTHING_RE: MATCH_NOTHING_RE,\\n  METHOD_GUARD: METHOD_GUARD,\\n  NUMBER_MODE: NUMBER_MODE,\\n  NUMBER_RE: NUMBER_RE,\\n  PHRASAL_WORDS_MODE: PHRASAL_WORDS_MODE,\\n  QUOTE_STRING_MODE: QUOTE_STRING_MODE,\\n  REGEXP_MODE: REGEXP_MODE,\\n  RE_STARTERS_RE: RE_STARTERS_RE,\\n  SHEBANG: SHEBANG,\\n  TITLE_MODE: TITLE_MODE,\\n  UNDERSCORE_IDENT_RE: UNDERSCORE_IDENT_RE,\\n  UNDERSCORE_TITLE_MODE: UNDERSCORE_TITLE_MODE\\n});\\n\\n/**\\n@typedef {import('highlight.js').CallbackResponse} CallbackResponse\\n@typedef {import('highlight.js').CompilerExt} CompilerExt\\n*/\\n\\n// Grammar extensions / plugins\\n// See: https://github.com/highlightjs/highlight.js/issues/2833\\n\\n// Grammar extensions allow \\\"syntactic sugar\\\" to be added to the grammar modes\\n// without requiring any underlying changes to the compiler internals.\\n\\n// `compileMatch` being the perfect small example of now allowing a grammar\\n// author to write `match` when they desire to match a single expression rather\\n// than being forced to use `begin`.  The extension then just moves `match` into\\n// `begin` when it runs.  Ie, no features have been added, but we've just made\\n// the experience of writing (and reading grammars) a little bit nicer.\\n\\n// ------\\n\\n// TODO: We need negative look-behind support to do this properly\\n/**\\n * Skip a match if it has a preceding dot\\n *\\n * This is used for `beginKeywords` to prevent matching expressions such as\\n * `bob.keyword.do()`. The mode compiler automatically wires this up as a\\n * special _internal_ 'on:begin' callback for modes with `beginKeywords`\\n * @param {RegExpMatchArray} match\\n * @param {CallbackResponse} response\\n */\\nfunction skipIfHasPrecedingDot(match, response) {\\n  const before = match.input[match.index - 1];\\n  if (before === \\\".\\\") {\\n    response.ignoreMatch();\\n  }\\n}\\n\\n/**\\n *\\n * @type {CompilerExt}\\n */\\nfunction scopeClassName(mode, _parent) {\\n  // eslint-disable-next-line no-undefined\\n  if (mode.className !== undefined) {\\n    mode.scope = mode.className;\\n    delete mode.className;\\n  }\\n}\\n\\n/**\\n * `beginKeywords` syntactic sugar\\n * @type {CompilerExt}\\n */\\nfunction beginKeywords(mode, parent) {\\n  if (!parent) return;\\n  if (!mode.beginKeywords) return;\\n\\n  // for languages with keywords that include non-word characters checking for\\n  // a word boundary is not sufficient, so instead we check for a word boundary\\n  // or whitespace - this does no harm in any case since our keyword engine\\n  // doesn't allow spaces in keywords anyways and we still check for the boundary\\n  // first\\n  mode.begin = '\\\\\\\\b(' + mode.beginKeywords.split(' ').join('|') + ')(?!\\\\\\\\.)(?=\\\\\\\\b|\\\\\\\\s)';\\n  mode.__beforeBegin = skipIfHasPrecedingDot;\\n  mode.keywords = mode.keywords || mode.beginKeywords;\\n  delete mode.beginKeywords;\\n\\n  // prevents double relevance, the keywords themselves provide\\n  // relevance, the mode doesn't need to double it\\n  // eslint-disable-next-line no-undefined\\n  if (mode.relevance === undefined) mode.relevance = 0;\\n}\\n\\n/**\\n * Allow `illegal` to contain an array of illegal values\\n * @type {CompilerExt}\\n */\\nfunction compileIllegal(mode, _parent) {\\n  if (!Array.isArray(mode.illegal)) return;\\n\\n  mode.illegal = either(...mode.illegal);\\n}\\n\\n/**\\n * `match` to match a single expression for readability\\n * @type {CompilerExt}\\n */\\nfunction compileMatch(mode, _parent) {\\n  if (!mode.match) return;\\n  if (mode.begin || mode.end) throw new Error(\\\"begin & end are not supported with match\\\");\\n\\n  mode.begin = mode.match;\\n  delete mode.match;\\n}\\n\\n/**\\n * provides the default 1 relevance to all modes\\n * @type {CompilerExt}\\n */\\nfunction compileRelevance(mode, _parent) {\\n  // eslint-disable-next-line no-undefined\\n  if (mode.relevance === undefined) mode.relevance = 1;\\n}\\n\\n// allow beforeMatch to act as a \\\"qualifier\\\" for the match\\n// the full match begin must be [beforeMatch][begin]\\nconst beforeMatchExt = (mode, parent) => {\\n  if (!mode.beforeMatch) return;\\n  // starts conflicts with endsParent which we need to make sure the child\\n  // rule is not matched multiple times\\n  if (mode.starts) throw new Error(\\\"beforeMatch cannot be used with starts\\\");\\n\\n  const originalMode = Object.assign({}, mode);\\n  Object.keys(mode).forEach((key) => { delete mode[key]; });\\n\\n  mode.keywords = originalMode.keywords;\\n  mode.begin = concat(originalMode.beforeMatch, lookahead(originalMode.begin));\\n  mode.starts = {\\n    relevance: 0,\\n    contains: [\\n      Object.assign(originalMode, { endsParent: true })\\n    ]\\n  };\\n  mode.relevance = 0;\\n\\n  delete originalMode.beforeMatch;\\n};\\n\\n// keywords that should have no default relevance value\\nconst COMMON_KEYWORDS = [\\n  'of',\\n  'and',\\n  'for',\\n  'in',\\n  'not',\\n  'or',\\n  'if',\\n  'then',\\n  'parent', // common variable name\\n  'list', // common variable name\\n  'value' // common variable name\\n];\\n\\nconst DEFAULT_KEYWORD_SCOPE = \\\"keyword\\\";\\n\\n/**\\n * Given raw keywords from a language definition, compile them.\\n *\\n * @param {string | Record<string,string|string[]> | Array<string>} rawKeywords\\n * @param {boolean} caseInsensitive\\n */\\nfunction compileKeywords(rawKeywords, caseInsensitive, scopeName = DEFAULT_KEYWORD_SCOPE) {\\n  /** @type {import(\\\"highlight.js/private\\\").KeywordDict} */\\n  const compiledKeywords = Object.create(null);\\n\\n  // input can be a string of keywords, an array of keywords, or a object with\\n  // named keys representing scopeName (which can then point to a string or array)\\n  if (typeof rawKeywords === 'string') {\\n    compileList(scopeName, rawKeywords.split(\\\" \\\"));\\n  } else if (Array.isArray(rawKeywords)) {\\n    compileList(scopeName, rawKeywords);\\n  } else {\\n    Object.keys(rawKeywords).forEach(function(scopeName) {\\n      // collapse all our objects back into the parent object\\n      Object.assign(\\n        compiledKeywords,\\n        compileKeywords(rawKeywords[scopeName], caseInsensitive, scopeName)\\n      );\\n    });\\n  }\\n  return compiledKeywords;\\n\\n  // ---\\n\\n  /**\\n   * Compiles an individual list of keywords\\n   *\\n   * Ex: \\\"for if when while|5\\\"\\n   *\\n   * @param {string} scopeName\\n   * @param {Array<string>} keywordList\\n   */\\n  function compileList(scopeName, keywordList) {\\n    if (caseInsensitive) {\\n      keywordList = keywordList.map(x => x.toLowerCase());\\n    }\\n    keywordList.forEach(function(keyword) {\\n      const pair = keyword.split('|');\\n      compiledKeywords[pair[0]] = [scopeName, scoreForKeyword(pair[0], pair[1])];\\n    });\\n  }\\n}\\n\\n/**\\n * Returns the proper score for a given keyword\\n *\\n * Also takes into account comment keywords, which will be scored 0 UNLESS\\n * another score has been manually assigned.\\n * @param {string} keyword\\n * @param {string} [providedScore]\\n */\\nfunction scoreForKeyword(keyword, providedScore) {\\n  // manual scores always win over common keywords\\n  // so you can force a score of 1 if you really insist\\n  if (providedScore) {\\n    return Number(providedScore);\\n  }\\n\\n  return commonKeyword(keyword) ? 0 : 1;\\n}\\n\\n/**\\n * Determines if a given keyword is common or not\\n *\\n * @param {string} keyword */\\nfunction commonKeyword(keyword) {\\n  return COMMON_KEYWORDS.includes(keyword.toLowerCase());\\n}\\n\\n/*\\n\\nFor the reasoning behind this please see:\\nhttps://github.com/highlightjs/highlight.js/issues/2880#issuecomment-747275419\\n\\n*/\\n\\n/**\\n * @type {Record<string, boolean>}\\n */\\nconst seenDeprecations = {};\\n\\n/**\\n * @param {string} message\\n */\\nconst error = (message) => {\\n  console.error(message);\\n};\\n\\n/**\\n * @param {string} message\\n * @param {any} args\\n */\\nconst warn = (message, ...args) => {\\n  console.log(`WARN: ${message}`, ...args);\\n};\\n\\n/**\\n * @param {string} version\\n * @param {string} message\\n */\\nconst deprecated = (version, message) => {\\n  if (seenDeprecations[`${version}/${message}`]) return;\\n\\n  console.log(`Deprecated as of ${version}. ${message}`);\\n  seenDeprecations[`${version}/${message}`] = true;\\n};\\n\\n/* eslint-disable no-throw-literal */\\n\\n/**\\n@typedef {import('highlight.js').CompiledMode} CompiledMode\\n*/\\n\\nconst MultiClassError = new Error();\\n\\n/**\\n * Renumbers labeled scope names to account for additional inner match\\n * groups that otherwise would break everything.\\n *\\n * Lets say we 3 match scopes:\\n *\\n *   { 1 => ..., 2 => ..., 3 => ... }\\n *\\n * So what we need is a clean match like this:\\n *\\n *   (a)(b)(c) => [ \\\"a\\\", \\\"b\\\", \\\"c\\\" ]\\n *\\n * But this falls apart with inner match groups:\\n *\\n * (a)(((b)))(c) => [\\\"a\\\", \\\"b\\\", \\\"b\\\", \\\"b\\\", \\\"c\\\" ]\\n *\\n * Our scopes are now \\\"out of alignment\\\" and we're repeating `b` 3 times.\\n * What needs to happen is the numbers are remapped:\\n *\\n *   { 1 => ..., 2 => ..., 5 => ... }\\n *\\n * We also need to know that the ONLY groups that should be output\\n * are 1, 2, and 5.  This function handles this behavior.\\n *\\n * @param {CompiledMode} mode\\n * @param {Array<RegExp | string>} regexes\\n * @param {{key: \\\"beginScope\\\"|\\\"endScope\\\"}} opts\\n */\\nfunction remapScopeNames(mode, regexes, { key }) {\\n  let offset = 0;\\n  const scopeNames = mode[key];\\n  /** @type Record<number,boolean> */\\n  const emit = {};\\n  /** @type Record<number,string> */\\n  const positions = {};\\n\\n  for (let i = 1; i <= regexes.length; i++) {\\n    positions[i + offset] = scopeNames[i];\\n    emit[i + offset] = true;\\n    offset += countMatchGroups(regexes[i - 1]);\\n  }\\n  // we use _emit to keep track of which match groups are \\\"top-level\\\" to avoid double\\n  // output from inside match groups\\n  mode[key] = positions;\\n  mode[key]._emit = emit;\\n  mode[key]._multi = true;\\n}\\n\\n/**\\n * @param {CompiledMode} mode\\n */\\nfunction beginMultiClass(mode) {\\n  if (!Array.isArray(mode.begin)) return;\\n\\n  if (mode.skip || mode.excludeBegin || mode.returnBegin) {\\n    error(\\\"skip, excludeBegin, returnBegin not compatible with beginScope: {}\\\");\\n    throw MultiClassError;\\n  }\\n\\n  if (typeof mode.beginScope !== \\\"object\\\" || mode.beginScope === null) {\\n    error(\\\"beginScope must be object\\\");\\n    throw MultiClassError;\\n  }\\n\\n  remapScopeNames(mode, mode.begin, { key: \\\"beginScope\\\" });\\n  mode.begin = _rewriteBackreferences(mode.begin, { joinWith: \\\"\\\" });\\n}\\n\\n/**\\n * @param {CompiledMode} mode\\n */\\nfunction endMultiClass(mode) {\\n  if (!Array.isArray(mode.end)) return;\\n\\n  if (mode.skip || mode.excludeEnd || mode.returnEnd) {\\n    error(\\\"skip, excludeEnd, returnEnd not compatible with endScope: {}\\\");\\n    throw MultiClassError;\\n  }\\n\\n  if (typeof mode.endScope !== \\\"object\\\" || mode.endScope === null) {\\n    error(\\\"endScope must be object\\\");\\n    throw MultiClassError;\\n  }\\n\\n  remapScopeNames(mode, mode.end, { key: \\\"endScope\\\" });\\n  mode.end = _rewriteBackreferences(mode.end, { joinWith: \\\"\\\" });\\n}\\n\\n/**\\n * this exists only to allow `scope: {}` to be used beside `match:`\\n * Otherwise `beginScope` would necessary and that would look weird\\n\\n  {\\n    match: [ /def/, /\\\\w+/ ]\\n    scope: { 1: \\\"keyword\\\" , 2: \\\"title\\\" }\\n  }\\n\\n * @param {CompiledMode} mode\\n */\\nfunction scopeSugar(mode) {\\n  if (mode.scope && typeof mode.scope === \\\"object\\\" && mode.scope !== null) {\\n    mode.beginScope = mode.scope;\\n    delete mode.scope;\\n  }\\n}\\n\\n/**\\n * @param {CompiledMode} mode\\n */\\nfunction MultiClass(mode) {\\n  scopeSugar(mode);\\n\\n  if (typeof mode.beginScope === \\\"string\\\") {\\n    mode.beginScope = { _wrap: mode.beginScope };\\n  }\\n  if (typeof mode.endScope === \\\"string\\\") {\\n    mode.endScope = { _wrap: mode.endScope };\\n  }\\n\\n  beginMultiClass(mode);\\n  endMultiClass(mode);\\n}\\n\\n/**\\n@typedef {import('highlight.js').Mode} Mode\\n@typedef {import('highlight.js').CompiledMode} CompiledMode\\n@typedef {import('highlight.js').Language} Language\\n@typedef {import('highlight.js').HLJSPlugin} HLJSPlugin\\n@typedef {import('highlight.js').CompiledLanguage} CompiledLanguage\\n*/\\n\\n// compilation\\n\\n/**\\n * Compiles a language definition result\\n *\\n * Given the raw result of a language definition (Language), compiles this so\\n * that it is ready for highlighting code.\\n * @param {Language} language\\n * @returns {CompiledLanguage}\\n */\\nfunction compileLanguage(language) {\\n  /**\\n   * Builds a regex with the case sensitivity of the current language\\n   *\\n   * @param {RegExp | string} value\\n   * @param {boolean} [global]\\n   */\\n  function langRe(value, global) {\\n    return new RegExp(\\n      source(value),\\n      'm'\\n      + (language.case_insensitive ? 'i' : '')\\n      + (language.unicodeRegex ? 'u' : '')\\n      + (global ? 'g' : '')\\n    );\\n  }\\n\\n  /**\\n    Stores multiple regular expressions and allows you to quickly search for\\n    them all in a string simultaneously - returning the first match.  It does\\n    this by creating a huge (a|b|c) regex - each individual item wrapped with ()\\n    and joined by `|` - using match groups to track position.  When a match is\\n    found checking which position in the array has content allows us to figure\\n    out which of the original regexes / match groups triggered the match.\\n\\n    The match object itself (the result of `Regex.exec`) is returned but also\\n    enhanced by merging in any meta-data that was registered with the regex.\\n    This is how we keep track of which mode matched, and what type of rule\\n    (`illegal`, `begin`, end, etc).\\n  */\\n  class MultiRegex {\\n    constructor() {\\n      this.matchIndexes = {};\\n      // @ts-ignore\\n      this.regexes = [];\\n      this.matchAt = 1;\\n      this.position = 0;\\n    }\\n\\n    // @ts-ignore\\n    addRule(re, opts) {\\n      opts.position = this.position++;\\n      // @ts-ignore\\n      this.matchIndexes[this.matchAt] = opts;\\n      this.regexes.push([opts, re]);\\n      this.matchAt += countMatchGroups(re) + 1;\\n    }\\n\\n    compile() {\\n      if (this.regexes.length === 0) {\\n        // avoids the need to check length every time exec is called\\n        // @ts-ignore\\n        this.exec = () => null;\\n      }\\n      const terminators = this.regexes.map(el => el[1]);\\n      this.matcherRe = langRe(_rewriteBackreferences(terminators, { joinWith: '|' }), true);\\n      this.lastIndex = 0;\\n    }\\n\\n    /** @param {string} s */\\n    exec(s) {\\n      this.matcherRe.lastIndex = this.lastIndex;\\n      const match = this.matcherRe.exec(s);\\n      if (!match) { return null; }\\n\\n      // eslint-disable-next-line no-undefined\\n      const i = match.findIndex((el, i) => i > 0 && el !== undefined);\\n      // @ts-ignore\\n      const matchData = this.matchIndexes[i];\\n      // trim off any earlier non-relevant match groups (ie, the other regex\\n      // match groups that make up the multi-matcher)\\n      match.splice(0, i);\\n\\n      return Object.assign(match, matchData);\\n    }\\n  }\\n\\n  /*\\n    Created to solve the key deficiently with MultiRegex - there is no way to\\n    test for multiple matches at a single location.  Why would we need to do\\n    that?  In the future a more dynamic engine will allow certain matches to be\\n    ignored.  An example: if we matched say the 3rd regex in a large group but\\n    decided to ignore it - we'd need to started testing again at the 4th\\n    regex... but MultiRegex itself gives us no real way to do that.\\n\\n    So what this class creates MultiRegexs on the fly for whatever search\\n    position they are needed.\\n\\n    NOTE: These additional MultiRegex objects are created dynamically.  For most\\n    grammars most of the time we will never actually need anything more than the\\n    first MultiRegex - so this shouldn't have too much overhead.\\n\\n    Say this is our search group, and we match regex3, but wish to ignore it.\\n\\n      regex1 | regex2 | regex3 | regex4 | regex5    ' ie, startAt = 0\\n\\n    What we need is a new MultiRegex that only includes the remaining\\n    possibilities:\\n\\n      regex4 | regex5                               ' ie, startAt = 3\\n\\n    This class wraps all that complexity up in a simple API... `startAt` decides\\n    where in the array of expressions to start doing the matching. It\\n    auto-increments, so if a match is found at position 2, then startAt will be\\n    set to 3.  If the end is reached startAt will return to 0.\\n\\n    MOST of the time the parser will be setting startAt manually to 0.\\n  */\\n  class ResumableMultiRegex {\\n    constructor() {\\n      // @ts-ignore\\n      this.rules = [];\\n      // @ts-ignore\\n      this.multiRegexes = [];\\n      this.count = 0;\\n\\n      this.lastIndex = 0;\\n      this.regexIndex = 0;\\n    }\\n\\n    // @ts-ignore\\n    getMatcher(index) {\\n      if (this.multiRegexes[index]) return this.multiRegexes[index];\\n\\n      const matcher = new MultiRegex();\\n      this.rules.slice(index).forEach(([re, opts]) => matcher.addRule(re, opts));\\n      matcher.compile();\\n      this.multiRegexes[index] = matcher;\\n      return matcher;\\n    }\\n\\n    resumingScanAtSamePosition() {\\n      return this.regexIndex !== 0;\\n    }\\n\\n    considerAll() {\\n      this.regexIndex = 0;\\n    }\\n\\n    // @ts-ignore\\n    addRule(re, opts) {\\n      this.rules.push([re, opts]);\\n      if (opts.type === \\\"begin\\\") this.count++;\\n    }\\n\\n    /** @param {string} s */\\n    exec(s) {\\n      const m = this.getMatcher(this.regexIndex);\\n      m.lastIndex = this.lastIndex;\\n      let result = m.exec(s);\\n\\n      // The following is because we have no easy way to say \\\"resume scanning at the\\n      // existing position but also skip the current rule ONLY\\\". What happens is\\n      // all prior rules are also skipped which can result in matching the wrong\\n      // thing. Example of matching \\\"booger\\\":\\n\\n      // our matcher is [string, \\\"booger\\\", number]\\n      //\\n      // ....booger....\\n\\n      // if \\\"booger\\\" is ignored then we'd really need a regex to scan from the\\n      // SAME position for only: [string, number] but ignoring \\\"booger\\\" (if it\\n      // was the first match), a simple resume would scan ahead who knows how\\n      // far looking only for \\\"number\\\", ignoring potential string matches (or\\n      // future \\\"booger\\\" matches that might be valid.)\\n\\n      // So what we do: We execute two matchers, one resuming at the same\\n      // position, but the second full matcher starting at the position after:\\n\\n      //     /--- resume first regex match here (for [number])\\n      //     |/---- full match here for [string, \\\"booger\\\", number]\\n      //     vv\\n      // ....booger....\\n\\n      // Which ever results in a match first is then used. So this 3-4 step\\n      // process essentially allows us to say \\\"match at this position, excluding\\n      // a prior rule that was ignored\\\".\\n      //\\n      // 1. Match \\\"booger\\\" first, ignore. Also proves that [string] does non match.\\n      // 2. Resume matching for [number]\\n      // 3. Match at index + 1 for [string, \\\"booger\\\", number]\\n      // 4. If #2 and #3 result in matches, which came first?\\n      if (this.resumingScanAtSamePosition()) {\\n        if (result && result.index === this.lastIndex) ; else { // use the second matcher result\\n          const m2 = this.getMatcher(0);\\n          m2.lastIndex = this.lastIndex + 1;\\n          result = m2.exec(s);\\n        }\\n      }\\n\\n      if (result) {\\n        this.regexIndex += result.position + 1;\\n        if (this.regexIndex === this.count) {\\n          // wrap-around to considering all matches again\\n          this.considerAll();\\n        }\\n      }\\n\\n      return result;\\n    }\\n  }\\n\\n  /**\\n   * Given a mode, builds a huge ResumableMultiRegex that can be used to walk\\n   * the content and find matches.\\n   *\\n   * @param {CompiledMode} mode\\n   * @returns {ResumableMultiRegex}\\n   */\\n  function buildModeRegex(mode) {\\n    const mm = new ResumableMultiRegex();\\n\\n    mode.contains.forEach(term => mm.addRule(term.begin, { rule: term, type: \\\"begin\\\" }));\\n\\n    if (mode.terminatorEnd) {\\n      mm.addRule(mode.terminatorEnd, { type: \\\"end\\\" });\\n    }\\n    if (mode.illegal) {\\n      mm.addRule(mode.illegal, { type: \\\"illegal\\\" });\\n    }\\n\\n    return mm;\\n  }\\n\\n  /** skip vs abort vs ignore\\n   *\\n   * @skip   - The mode is still entered and exited normally (and contains rules apply),\\n   *           but all content is held and added to the parent buffer rather than being\\n   *           output when the mode ends.  Mostly used with `sublanguage` to build up\\n   *           a single large buffer than can be parsed by sublanguage.\\n   *\\n   *             - The mode begin ands ends normally.\\n   *             - Content matched is added to the parent mode buffer.\\n   *             - The parser cursor is moved forward normally.\\n   *\\n   * @abort  - A hack placeholder until we have ignore.  Aborts the mode (as if it\\n   *           never matched) but DOES NOT continue to match subsequent `contains`\\n   *           modes.  Abort is bad/suboptimal because it can result in modes\\n   *           farther down not getting applied because an earlier rule eats the\\n   *           content but then aborts.\\n   *\\n   *             - The mode does not begin.\\n   *             - Content matched by `begin` is added to the mode buffer.\\n   *             - The parser cursor is moved forward accordingly.\\n   *\\n   * @ignore - Ignores the mode (as if it never matched) and continues to match any\\n   *           subsequent `contains` modes.  Ignore isn't technically possible with\\n   *           the current parser implementation.\\n   *\\n   *             - The mode does not begin.\\n   *             - Content matched by `begin` is ignored.\\n   *             - The parser cursor is not moved forward.\\n   */\\n\\n  /**\\n   * Compiles an individual mode\\n   *\\n   * This can raise an error if the mode contains certain detectable known logic\\n   * issues.\\n   * @param {Mode} mode\\n   * @param {CompiledMode | null} [parent]\\n   * @returns {CompiledMode | never}\\n   */\\n  function compileMode(mode, parent) {\\n    const cmode = /** @type CompiledMode */ (mode);\\n    if (mode.isCompiled) return cmode;\\n\\n    [\\n      scopeClassName,\\n      // do this early so compiler extensions generally don't have to worry about\\n      // the distinction between match/begin\\n      compileMatch,\\n      MultiClass,\\n      beforeMatchExt\\n    ].forEach(ext => ext(mode, parent));\\n\\n    language.compilerExtensions.forEach(ext => ext(mode, parent));\\n\\n    // __beforeBegin is considered private API, internal use only\\n    mode.__beforeBegin = null;\\n\\n    [\\n      beginKeywords,\\n      // do this later so compiler extensions that come earlier have access to the\\n      // raw array if they wanted to perhaps manipulate it, etc.\\n      compileIllegal,\\n      // default to 1 relevance if not specified\\n      compileRelevance\\n    ].forEach(ext => ext(mode, parent));\\n\\n    mode.isCompiled = true;\\n\\n    let keywordPattern = null;\\n    if (typeof mode.keywords === \\\"object\\\" && mode.keywords.$pattern) {\\n      // we need a copy because keywords might be compiled multiple times\\n      // so we can't go deleting $pattern from the original on the first\\n      // pass\\n      mode.keywords = Object.assign({}, mode.keywords);\\n      keywordPattern = mode.keywords.$pattern;\\n      delete mode.keywords.$pattern;\\n    }\\n    keywordPattern = keywordPattern || /\\\\w+/;\\n\\n    if (mode.keywords) {\\n      mode.keywords = compileKeywords(mode.keywords, language.case_insensitive);\\n    }\\n\\n    cmode.keywordPatternRe = langRe(keywordPattern, true);\\n\\n    if (parent) {\\n      if (!mode.begin) mode.begin = /\\\\B|\\\\b/;\\n      cmode.beginRe = langRe(cmode.begin);\\n      if (!mode.end && !mode.endsWithParent) mode.end = /\\\\B|\\\\b/;\\n      if (mode.end) cmode.endRe = langRe(cmode.end);\\n      cmode.terminatorEnd = source(cmode.end) || '';\\n      if (mode.endsWithParent && parent.terminatorEnd) {\\n        cmode.terminatorEnd += (mode.end ? '|' : '') + parent.terminatorEnd;\\n      }\\n    }\\n    if (mode.illegal) cmode.illegalRe = langRe(/** @type {RegExp | string} */ (mode.illegal));\\n    if (!mode.contains) mode.contains = [];\\n\\n    mode.contains = [].concat(...mode.contains.map(function(c) {\\n      return expandOrCloneMode(c === 'self' ? mode : c);\\n    }));\\n    mode.contains.forEach(function(c) { compileMode(/** @type Mode */ (c), cmode); });\\n\\n    if (mode.starts) {\\n      compileMode(mode.starts, parent);\\n    }\\n\\n    cmode.matcher = buildModeRegex(cmode);\\n    return cmode;\\n  }\\n\\n  if (!language.compilerExtensions) language.compilerExtensions = [];\\n\\n  // self is not valid at the top-level\\n  if (language.contains && language.contains.includes('self')) {\\n    throw new Error(\\\"ERR: contains `self` is not supported at the top-level of a language.  See documentation.\\\");\\n  }\\n\\n  // we need a null object, which inherit will guarantee\\n  language.classNameAliases = inherit$1(language.classNameAliases || {});\\n\\n  return compileMode(/** @type Mode */ (language));\\n}\\n\\n/**\\n * Determines if a mode has a dependency on it's parent or not\\n *\\n * If a mode does have a parent dependency then often we need to clone it if\\n * it's used in multiple places so that each copy points to the correct parent,\\n * where-as modes without a parent can often safely be re-used at the bottom of\\n * a mode chain.\\n *\\n * @param {Mode | null} mode\\n * @returns {boolean} - is there a dependency on the parent?\\n * */\\nfunction dependencyOnParent(mode) {\\n  if (!mode) return false;\\n\\n  return mode.endsWithParent || dependencyOnParent(mode.starts);\\n}\\n\\n/**\\n * Expands a mode or clones it if necessary\\n *\\n * This is necessary for modes with parental dependenceis (see notes on\\n * `dependencyOnParent`) and for nodes that have `variants` - which must then be\\n * exploded into their own individual modes at compile time.\\n *\\n * @param {Mode} mode\\n * @returns {Mode | Mode[]}\\n * */\\nfunction expandOrCloneMode(mode) {\\n  if (mode.variants && !mode.cachedVariants) {\\n    mode.cachedVariants = mode.variants.map(function(variant) {\\n      return inherit$1(mode, { variants: null }, variant);\\n    });\\n  }\\n\\n  // EXPAND\\n  // if we have variants then essentially \\\"replace\\\" the mode with the variants\\n  // this happens in compileMode, where this function is called from\\n  if (mode.cachedVariants) {\\n    return mode.cachedVariants;\\n  }\\n\\n  // CLONE\\n  // if we have dependencies on parents then we need a unique\\n  // instance of ourselves, so we can be reused with many\\n  // different parents without issue\\n  if (dependencyOnParent(mode)) {\\n    return inherit$1(mode, { starts: mode.starts ? inherit$1(mode.starts) : null });\\n  }\\n\\n  if (Object.isFrozen(mode)) {\\n    return inherit$1(mode);\\n  }\\n\\n  // no special dependency issues, just return ourselves\\n  return mode;\\n}\\n\\nvar version = \\\"11.11.1\\\";\\n\\nclass HTMLInjectionError extends Error {\\n  constructor(reason, html) {\\n    super(reason);\\n    this.name = \\\"HTMLInjectionError\\\";\\n    this.html = html;\\n  }\\n}\\n\\n/*\\nSyntax highlighting with language autodetection.\\nhttps://highlightjs.org/\\n*/\\n\\n\\n\\n/**\\n@typedef {import('highlight.js').Mode} Mode\\n@typedef {import('highlight.js').CompiledMode} CompiledMode\\n@typedef {import('highlight.js').CompiledScope} CompiledScope\\n@typedef {import('highlight.js').Language} Language\\n@typedef {import('highlight.js').HLJSApi} HLJSApi\\n@typedef {import('highlight.js').HLJSPlugin} HLJSPlugin\\n@typedef {import('highlight.js').PluginEvent} PluginEvent\\n@typedef {import('highlight.js').HLJSOptions} HLJSOptions\\n@typedef {import('highlight.js').LanguageFn} LanguageFn\\n@typedef {import('highlight.js').HighlightedHTMLElement} HighlightedHTMLElement\\n@typedef {import('highlight.js').BeforeHighlightContext} BeforeHighlightContext\\n@typedef {import('highlight.js/private').MatchType} MatchType\\n@typedef {import('highlight.js/private').KeywordData} KeywordData\\n@typedef {import('highlight.js/private').EnhancedMatch} EnhancedMatch\\n@typedef {import('highlight.js/private').AnnotatedError} AnnotatedError\\n@typedef {import('highlight.js').AutoHighlightResult} AutoHighlightResult\\n@typedef {import('highlight.js').HighlightOptions} HighlightOptions\\n@typedef {import('highlight.js').HighlightResult} HighlightResult\\n*/\\n\\n\\nconst escape = escapeHTML;\\nconst inherit = inherit$1;\\nconst NO_MATCH = Symbol(\\\"nomatch\\\");\\nconst MAX_KEYWORD_HITS = 7;\\n\\n/**\\n * @param {any} hljs - object that is extended (legacy)\\n * @returns {HLJSApi}\\n */\\nconst HLJS = function(hljs) {\\n  // Global internal variables used within the highlight.js library.\\n  /** @type {Record<string, Language>} */\\n  const languages = Object.create(null);\\n  /** @type {Record<string, string>} */\\n  const aliases = Object.create(null);\\n  /** @type {HLJSPlugin[]} */\\n  const plugins = [];\\n\\n  // safe/production mode - swallows more errors, tries to keep running\\n  // even if a single syntax or parse hits a fatal error\\n  let SAFE_MODE = true;\\n  const LANGUAGE_NOT_FOUND = \\\"Could not find the language '{}', did you forget to load/include a language module?\\\";\\n  /** @type {Language} */\\n  const PLAINTEXT_LANGUAGE = { disableAutodetect: true, name: 'Plain text', contains: [] };\\n\\n  // Global options used when within external APIs. This is modified when\\n  // calling the `hljs.configure` function.\\n  /** @type HLJSOptions */\\n  let options = {\\n    ignoreUnescapedHTML: false,\\n    throwUnescapedHTML: false,\\n    noHighlightRe: /^(no-?highlight)$/i,\\n    languageDetectRe: /\\\\blang(?:uage)?-([\\\\w-]+)\\\\b/i,\\n    classPrefix: 'hljs-',\\n    cssSelector: 'pre code',\\n    languages: null,\\n    // beta configuration options, subject to change, welcome to discuss\\n    // https://github.com/highlightjs/highlight.js/issues/1086\\n    __emitter: TokenTreeEmitter\\n  };\\n\\n  /* Utility functions */\\n\\n  /**\\n   * Tests a language name to see if highlighting should be skipped\\n   * @param {string} languageName\\n   */\\n  function shouldNotHighlight(languageName) {\\n    return options.noHighlightRe.test(languageName);\\n  }\\n\\n  /**\\n   * @param {HighlightedHTMLElement} block - the HTML element to determine language for\\n   */\\n  function blockLanguage(block) {\\n    let classes = block.className + ' ';\\n\\n    classes += block.parentNode ? block.parentNode.className : '';\\n\\n    // language-* takes precedence over non-prefixed class names.\\n    const match = options.languageDetectRe.exec(classes);\\n    if (match) {\\n      const language = getLanguage(match[1]);\\n      if (!language) {\\n        warn(LANGUAGE_NOT_FOUND.replace(\\\"{}\\\", match[1]));\\n        warn(\\\"Falling back to no-highlight mode for this block.\\\", block);\\n      }\\n      return language ? match[1] : 'no-highlight';\\n    }\\n\\n    return classes\\n      .split(/\\\\s+/)\\n      .find((_class) => shouldNotHighlight(_class) || getLanguage(_class));\\n  }\\n\\n  /**\\n   * Core highlighting function.\\n   *\\n   * OLD API\\n   * highlight(lang, code, ignoreIllegals, continuation)\\n   *\\n   * NEW API\\n   * highlight(code, {lang, ignoreIllegals})\\n   *\\n   * @param {string} codeOrLanguageName - the language to use for highlighting\\n   * @param {string | HighlightOptions} optionsOrCode - the code to highlight\\n   * @param {boolean} [ignoreIllegals] - whether to ignore illegal matches, default is to bail\\n   *\\n   * @returns {HighlightResult} Result - an object that represents the result\\n   * @property {string} language - the language name\\n   * @property {number} relevance - the relevance score\\n   * @property {string} value - the highlighted HTML code\\n   * @property {string} code - the original raw code\\n   * @property {CompiledMode} top - top of the current mode stack\\n   * @property {boolean} illegal - indicates whether any illegal matches were found\\n  */\\n  function highlight(codeOrLanguageName, optionsOrCode, ignoreIllegals) {\\n    let code = \\\"\\\";\\n    let languageName = \\\"\\\";\\n    if (typeof optionsOrCode === \\\"object\\\") {\\n      code = codeOrLanguageName;\\n      ignoreIllegals = optionsOrCode.ignoreIllegals;\\n      languageName = optionsOrCode.language;\\n    } else {\\n      // old API\\n      deprecated(\\\"10.7.0\\\", \\\"highlight(lang, code, ...args) has been deprecated.\\\");\\n      deprecated(\\\"10.7.0\\\", \\\"Please use highlight(code, options) instead.\\\\nhttps://github.com/highlightjs/highlight.js/issues/2277\\\");\\n      languageName = codeOrLanguageName;\\n      code = optionsOrCode;\\n    }\\n\\n    // https://github.com/highlightjs/highlight.js/issues/3149\\n    // eslint-disable-next-line no-undefined\\n    if (ignoreIllegals === undefined) { ignoreIllegals = true; }\\n\\n    /** @type {BeforeHighlightContext} */\\n    const context = {\\n      code,\\n      language: languageName\\n    };\\n    // the plugin can change the desired language or the code to be highlighted\\n    // just be changing the object it was passed\\n    fire(\\\"before:highlight\\\", context);\\n\\n    // a before plugin can usurp the result completely by providing it's own\\n    // in which case we don't even need to call highlight\\n    const result = context.result\\n      ? context.result\\n      : _highlight(context.language, context.code, ignoreIllegals);\\n\\n    result.code = context.code;\\n    // the plugin can change anything in result to suite it\\n    fire(\\\"after:highlight\\\", result);\\n\\n    return result;\\n  }\\n\\n  /**\\n   * private highlight that's used internally and does not fire callbacks\\n   *\\n   * @param {string} languageName - the language to use for highlighting\\n   * @param {string} codeToHighlight - the code to highlight\\n   * @param {boolean?} [ignoreIllegals] - whether to ignore illegal matches, default is to bail\\n   * @param {CompiledMode?} [continuation] - current continuation mode, if any\\n   * @returns {HighlightResult} - result of the highlight operation\\n  */\\n  function _highlight(languageName, codeToHighlight, ignoreIllegals, continuation) {\\n    const keywordHits = Object.create(null);\\n\\n    /**\\n     * Return keyword data if a match is a keyword\\n     * @param {CompiledMode} mode - current mode\\n     * @param {string} matchText - the textual match\\n     * @returns {KeywordData | false}\\n     */\\n    function keywordData(mode, matchText) {\\n      return mode.keywords[matchText];\\n    }\\n\\n    function processKeywords() {\\n      if (!top.keywords) {\\n        emitter.addText(modeBuffer);\\n        return;\\n      }\\n\\n      let lastIndex = 0;\\n      top.keywordPatternRe.lastIndex = 0;\\n      let match = top.keywordPatternRe.exec(modeBuffer);\\n      let buf = \\\"\\\";\\n\\n      while (match) {\\n        buf += modeBuffer.substring(lastIndex, match.index);\\n        const word = language.case_insensitive ? match[0].toLowerCase() : match[0];\\n        const data = keywordData(top, word);\\n        if (data) {\\n          const [kind, keywordRelevance] = data;\\n          emitter.addText(buf);\\n          buf = \\\"\\\";\\n\\n          keywordHits[word] = (keywordHits[word] || 0) + 1;\\n          if (keywordHits[word] <= MAX_KEYWORD_HITS) relevance += keywordRelevance;\\n          if (kind.startsWith(\\\"_\\\")) {\\n            // _ implied for relevance only, do not highlight\\n            // by applying a class name\\n            buf += match[0];\\n          } else {\\n            const cssClass = language.classNameAliases[kind] || kind;\\n            emitKeyword(match[0], cssClass);\\n          }\\n        } else {\\n          buf += match[0];\\n        }\\n        lastIndex = top.keywordPatternRe.lastIndex;\\n        match = top.keywordPatternRe.exec(modeBuffer);\\n      }\\n      buf += modeBuffer.substring(lastIndex);\\n      emitter.addText(buf);\\n    }\\n\\n    function processSubLanguage() {\\n      if (modeBuffer === \\\"\\\") return;\\n      /** @type HighlightResult */\\n      let result = null;\\n\\n      if (typeof top.subLanguage === 'string') {\\n        if (!languages[top.subLanguage]) {\\n          emitter.addText(modeBuffer);\\n          return;\\n        }\\n        result = _highlight(top.subLanguage, modeBuffer, true, continuations[top.subLanguage]);\\n        continuations[top.subLanguage] = /** @type {CompiledMode} */ (result._top);\\n      } else {\\n        result = highlightAuto(modeBuffer, top.subLanguage.length ? top.subLanguage : null);\\n      }\\n\\n      // Counting embedded language score towards the host language may be disabled\\n      // with zeroing the containing mode relevance. Use case in point is Markdown that\\n      // allows XML everywhere and makes every XML snippet to have a much larger Markdown\\n      // score.\\n      if (top.relevance > 0) {\\n        relevance += result.relevance;\\n      }\\n      emitter.__addSublanguage(result._emitter, result.language);\\n    }\\n\\n    function processBuffer() {\\n      if (top.subLanguage != null) {\\n        processSubLanguage();\\n      } else {\\n        processKeywords();\\n      }\\n      modeBuffer = '';\\n    }\\n\\n    /**\\n     * @param {string} text\\n     * @param {string} scope\\n     */\\n    function emitKeyword(keyword, scope) {\\n      if (keyword === \\\"\\\") return;\\n\\n      emitter.startScope(scope);\\n      emitter.addText(keyword);\\n      emitter.endScope();\\n    }\\n\\n    /**\\n     * @param {CompiledScope} scope\\n     * @param {RegExpMatchArray} match\\n     */\\n    function emitMultiClass(scope, match) {\\n      let i = 1;\\n      const max = match.length - 1;\\n      while (i <= max) {\\n        if (!scope._emit[i]) { i++; continue; }\\n        const klass = language.classNameAliases[scope[i]] || scope[i];\\n        const text = match[i];\\n        if (klass) {\\n          emitKeyword(text, klass);\\n        } else {\\n          modeBuffer = text;\\n          processKeywords();\\n          modeBuffer = \\\"\\\";\\n        }\\n        i++;\\n      }\\n    }\\n\\n    /**\\n     * @param {CompiledMode} mode - new mode to start\\n     * @param {RegExpMatchArray} match\\n     */\\n    function startNewMode(mode, match) {\\n      if (mode.scope && typeof mode.scope === \\\"string\\\") {\\n        emitter.openNode(language.classNameAliases[mode.scope] || mode.scope);\\n      }\\n      if (mode.beginScope) {\\n        // beginScope just wraps the begin match itself in a scope\\n        if (mode.beginScope._wrap) {\\n          emitKeyword(modeBuffer, language.classNameAliases[mode.beginScope._wrap] || mode.beginScope._wrap);\\n          modeBuffer = \\\"\\\";\\n        } else if (mode.beginScope._multi) {\\n          // at this point modeBuffer should just be the match\\n          emitMultiClass(mode.beginScope, match);\\n          modeBuffer = \\\"\\\";\\n        }\\n      }\\n\\n      top = Object.create(mode, { parent: { value: top } });\\n      return top;\\n    }\\n\\n    /**\\n     * @param {CompiledMode } mode - the mode to potentially end\\n     * @param {RegExpMatchArray} match - the latest match\\n     * @param {string} matchPlusRemainder - match plus remainder of content\\n     * @returns {CompiledMode | void} - the next mode, or if void continue on in current mode\\n     */\\n    function endOfMode(mode, match, matchPlusRemainder) {\\n      let matched = startsWith(mode.endRe, matchPlusRemainder);\\n\\n      if (matched) {\\n        if (mode[\\\"on:end\\\"]) {\\n          const resp = new Response(mode);\\n          mode[\\\"on:end\\\"](match, resp);\\n          if (resp.isMatchIgnored) matched = false;\\n        }\\n\\n        if (matched) {\\n          while (mode.endsParent && mode.parent) {\\n            mode = mode.parent;\\n          }\\n          return mode;\\n        }\\n      }\\n      // even if on:end fires an `ignore` it's still possible\\n      // that we might trigger the end node because of a parent mode\\n      if (mode.endsWithParent) {\\n        return endOfMode(mode.parent, match, matchPlusRemainder);\\n      }\\n    }\\n\\n    /**\\n     * Handle matching but then ignoring a sequence of text\\n     *\\n     * @param {string} lexeme - string containing full match text\\n     */\\n    function doIgnore(lexeme) {\\n      if (top.matcher.regexIndex === 0) {\\n        // no more regexes to potentially match here, so we move the cursor forward one\\n        // space\\n        modeBuffer += lexeme[0];\\n        return 1;\\n      } else {\\n        // no need to move the cursor, we still have additional regexes to try and\\n        // match at this very spot\\n        resumeScanAtSamePosition = true;\\n        return 0;\\n      }\\n    }\\n\\n    /**\\n     * Handle the start of a new potential mode match\\n     *\\n     * @param {EnhancedMatch} match - the current match\\n     * @returns {number} how far to advance the parse cursor\\n     */\\n    function doBeginMatch(match) {\\n      const lexeme = match[0];\\n      const newMode = match.rule;\\n\\n      const resp = new Response(newMode);\\n      // first internal before callbacks, then the public ones\\n      const beforeCallbacks = [newMode.__beforeBegin, newMode[\\\"on:begin\\\"]];\\n      for (const cb of beforeCallbacks) {\\n        if (!cb) continue;\\n        cb(match, resp);\\n        if (resp.isMatchIgnored) return doIgnore(lexeme);\\n      }\\n\\n      if (newMode.skip) {\\n        modeBuffer += lexeme;\\n      } else {\\n        if (newMode.excludeBegin) {\\n          modeBuffer += lexeme;\\n        }\\n        processBuffer();\\n        if (!newMode.returnBegin && !newMode.excludeBegin) {\\n          modeBuffer = lexeme;\\n        }\\n      }\\n      startNewMode(newMode, match);\\n      return newMode.returnBegin ? 0 : lexeme.length;\\n    }\\n\\n    /**\\n     * Handle the potential end of mode\\n     *\\n     * @param {RegExpMatchArray} match - the current match\\n     */\\n    function doEndMatch(match) {\\n      const lexeme = match[0];\\n      const matchPlusRemainder = codeToHighlight.substring(match.index);\\n\\n      const endMode = endOfMode(top, match, matchPlusRemainder);\\n      if (!endMode) { return NO_MATCH; }\\n\\n      const origin = top;\\n      if (top.endScope && top.endScope._wrap) {\\n        processBuffer();\\n        emitKeyword(lexeme, top.endScope._wrap);\\n      } else if (top.endScope && top.endScope._multi) {\\n        processBuffer();\\n        emitMultiClass(top.endScope, match);\\n      } else if (origin.skip) {\\n        modeBuffer += lexeme;\\n      } else {\\n        if (!(origin.returnEnd || origin.excludeEnd)) {\\n          modeBuffer += lexeme;\\n        }\\n        processBuffer();\\n        if (origin.excludeEnd) {\\n          modeBuffer = lexeme;\\n        }\\n      }\\n      do {\\n        if (top.scope) {\\n          emitter.closeNode();\\n        }\\n        if (!top.skip && !top.subLanguage) {\\n          relevance += top.relevance;\\n        }\\n        top = top.parent;\\n      } while (top !== endMode.parent);\\n      if (endMode.starts) {\\n        startNewMode(endMode.starts, match);\\n      }\\n      return origin.returnEnd ? 0 : lexeme.length;\\n    }\\n\\n    function processContinuations() {\\n      const list = [];\\n      for (let current = top; current !== language; current = current.parent) {\\n        if (current.scope) {\\n          list.unshift(current.scope);\\n        }\\n      }\\n      list.forEach(item => emitter.openNode(item));\\n    }\\n\\n    /** @type {{type?: MatchType, index?: number, rule?: Mode}}} */\\n    let lastMatch = {};\\n\\n    /**\\n     *  Process an individual match\\n     *\\n     * @param {string} textBeforeMatch - text preceding the match (since the last match)\\n     * @param {EnhancedMatch} [match] - the match itself\\n     */\\n    function processLexeme(textBeforeMatch, match) {\\n      const lexeme = match && match[0];\\n\\n      // add non-matched text to the current mode buffer\\n      modeBuffer += textBeforeMatch;\\n\\n      if (lexeme == null) {\\n        processBuffer();\\n        return 0;\\n      }\\n\\n      // we've found a 0 width match and we're stuck, so we need to advance\\n      // this happens when we have badly behaved rules that have optional matchers to the degree that\\n      // sometimes they can end up matching nothing at all\\n      // Ref: https://github.com/highlightjs/highlight.js/issues/2140\\n      if (lastMatch.type === \\\"begin\\\" && match.type === \\\"end\\\" && lastMatch.index === match.index && lexeme === \\\"\\\") {\\n        // spit the \\\"skipped\\\" character that our regex choked on back into the output sequence\\n        modeBuffer += codeToHighlight.slice(match.index, match.index + 1);\\n        if (!SAFE_MODE) {\\n          /** @type {AnnotatedError} */\\n          const err = new Error(`0 width match regex (${languageName})`);\\n          err.languageName = languageName;\\n          err.badRule = lastMatch.rule;\\n          throw err;\\n        }\\n        return 1;\\n      }\\n      lastMatch = match;\\n\\n      if (match.type === \\\"begin\\\") {\\n        return doBeginMatch(match);\\n      } else if (match.type === \\\"illegal\\\" && !ignoreIllegals) {\\n        // illegal match, we do not continue processing\\n        /** @type {AnnotatedError} */\\n        const err = new Error('Illegal lexeme \\\"' + lexeme + '\\\" for mode \\\"' + (top.scope || '<unnamed>') + '\\\"');\\n        err.mode = top;\\n        throw err;\\n      } else if (match.type === \\\"end\\\") {\\n        const processed = doEndMatch(match);\\n        if (processed !== NO_MATCH) {\\n          return processed;\\n        }\\n      }\\n\\n      // edge case for when illegal matches $ (end of line) which is technically\\n      // a 0 width match but not a begin/end match so it's not caught by the\\n      // first handler (when ignoreIllegals is true)\\n      if (match.type === \\\"illegal\\\" && lexeme === \\\"\\\") {\\n        // advance so we aren't stuck in an infinite loop\\n        modeBuffer += \\\"\\\\n\\\";\\n        return 1;\\n      }\\n\\n      // infinite loops are BAD, this is a last ditch catch all. if we have a\\n      // decent number of iterations yet our index (cursor position in our\\n      // parsing) still 3x behind our index then something is very wrong\\n      // so we bail\\n      if (iterations > 100000 && iterations > match.index * 3) {\\n        const err = new Error('potential infinite loop, way more iterations than matches');\\n        throw err;\\n      }\\n\\n      /*\\n      Why might be find ourselves here?  An potential end match that was\\n      triggered but could not be completed.  IE, `doEndMatch` returned NO_MATCH.\\n      (this could be because a callback requests the match be ignored, etc)\\n\\n      This causes no real harm other than stopping a few times too many.\\n      */\\n\\n      modeBuffer += lexeme;\\n      return lexeme.length;\\n    }\\n\\n    const language = getLanguage(languageName);\\n    if (!language) {\\n      error(LANGUAGE_NOT_FOUND.replace(\\\"{}\\\", languageName));\\n      throw new Error('Unknown language: \\\"' + languageName + '\\\"');\\n    }\\n\\n    const md = compileLanguage(language);\\n    let result = '';\\n    /** @type {CompiledMode} */\\n    let top = continuation || md;\\n    /** @type Record<string,CompiledMode> */\\n    const continuations = {}; // keep continuations for sub-languages\\n    const emitter = new options.__emitter(options);\\n    processContinuations();\\n    let modeBuffer = '';\\n    let relevance = 0;\\n    let index = 0;\\n    let iterations = 0;\\n    let resumeScanAtSamePosition = false;\\n\\n    try {\\n      if (!language.__emitTokens) {\\n        top.matcher.considerAll();\\n\\n        for (;;) {\\n          iterations++;\\n          if (resumeScanAtSamePosition) {\\n            // only regexes not matched previously will now be\\n            // considered for a potential match\\n            resumeScanAtSamePosition = false;\\n          } else {\\n            top.matcher.considerAll();\\n          }\\n          top.matcher.lastIndex = index;\\n\\n          const match = top.matcher.exec(codeToHighlight);\\n          // console.log(\\\"match\\\", match[0], match.rule && match.rule.begin)\\n\\n          if (!match) break;\\n\\n          const beforeMatch = codeToHighlight.substring(index, match.index);\\n          const processedCount = processLexeme(beforeMatch, match);\\n          index = match.index + processedCount;\\n        }\\n        processLexeme(codeToHighlight.substring(index));\\n      } else {\\n        language.__emitTokens(codeToHighlight, emitter);\\n      }\\n\\n      emitter.finalize();\\n      result = emitter.toHTML();\\n\\n      return {\\n        language: languageName,\\n        value: result,\\n        relevance,\\n        illegal: false,\\n        _emitter: emitter,\\n        _top: top\\n      };\\n    } catch (err) {\\n      if (err.message && err.message.includes('Illegal')) {\\n        return {\\n          language: languageName,\\n          value: escape(codeToHighlight),\\n          illegal: true,\\n          relevance: 0,\\n          _illegalBy: {\\n            message: err.message,\\n            index,\\n            context: codeToHighlight.slice(index - 100, index + 100),\\n            mode: err.mode,\\n            resultSoFar: result\\n          },\\n          _emitter: emitter\\n        };\\n      } else if (SAFE_MODE) {\\n        return {\\n          language: languageName,\\n          value: escape(codeToHighlight),\\n          illegal: false,\\n          relevance: 0,\\n          errorRaised: err,\\n          _emitter: emitter,\\n          _top: top\\n        };\\n      } else {\\n        throw err;\\n      }\\n    }\\n  }\\n\\n  /**\\n   * returns a valid highlight result, without actually doing any actual work,\\n   * auto highlight starts with this and it's possible for small snippets that\\n   * auto-detection may not find a better match\\n   * @param {string} code\\n   * @returns {HighlightResult}\\n   */\\n  function justTextHighlightResult(code) {\\n    const result = {\\n      value: escape(code),\\n      illegal: false,\\n      relevance: 0,\\n      _top: PLAINTEXT_LANGUAGE,\\n      _emitter: new options.__emitter(options)\\n    };\\n    result._emitter.addText(code);\\n    return result;\\n  }\\n\\n  /**\\n  Highlighting with language detection. Accepts a string with the code to\\n  highlight. Returns an object with the following properties:\\n\\n  - language (detected language)\\n  - relevance (int)\\n  - value (an HTML string with highlighting markup)\\n  - secondBest (object with the same structure for second-best heuristically\\n    detected language, may be absent)\\n\\n    @param {string} code\\n    @param {Array<string>} [languageSubset]\\n    @returns {AutoHighlightResult}\\n  */\\n  function highlightAuto(code, languageSubset) {\\n    languageSubset = languageSubset || options.languages || Object.keys(languages);\\n    const plaintext = justTextHighlightResult(code);\\n\\n    const results = languageSubset.filter(getLanguage).filter(autoDetection).map(name =>\\n      _highlight(name, code, false)\\n    );\\n    results.unshift(plaintext); // plaintext is always an option\\n\\n    const sorted = results.sort((a, b) => {\\n      // sort base on relevance\\n      if (a.relevance !== b.relevance) return b.relevance - a.relevance;\\n\\n      // always award the tie to the base language\\n      // ie if C++ and Arduino are tied, it's more likely to be C++\\n      if (a.language && b.language) {\\n        if (getLanguage(a.language).supersetOf === b.language) {\\n          return 1;\\n        } else if (getLanguage(b.language).supersetOf === a.language) {\\n          return -1;\\n        }\\n      }\\n\\n      // otherwise say they are equal, which has the effect of sorting on\\n      // relevance while preserving the original ordering - which is how ties\\n      // have historically been settled, ie the language that comes first always\\n      // wins in the case of a tie\\n      return 0;\\n    });\\n\\n    const [best, secondBest] = sorted;\\n\\n    /** @type {AutoHighlightResult} */\\n    const result = best;\\n    result.secondBest = secondBest;\\n\\n    return result;\\n  }\\n\\n  /**\\n   * Builds new class name for block given the language name\\n   *\\n   * @param {HTMLElement} element\\n   * @param {string} [currentLang]\\n   * @param {string} [resultLang]\\n   */\\n  function updateClassName(element, currentLang, resultLang) {\\n    const language = (currentLang && aliases[currentLang]) || resultLang;\\n\\n    element.classList.add(\\\"hljs\\\");\\n    element.classList.add(`language-${language}`);\\n  }\\n\\n  /**\\n   * Applies highlighting to a DOM node containing code.\\n   *\\n   * @param {HighlightedHTMLElement} element - the HTML element to highlight\\n  */\\n  function highlightElement(element) {\\n    /** @type HTMLElement */\\n    let node = null;\\n    const language = blockLanguage(element);\\n\\n    if (shouldNotHighlight(language)) return;\\n\\n    fire(\\\"before:highlightElement\\\",\\n      { el: element, language });\\n\\n    if (element.dataset.highlighted) {\\n      console.log(\\\"Element previously highlighted. To highlight again, first unset `dataset.highlighted`.\\\", element);\\n      return;\\n    }\\n\\n    // we should be all text, no child nodes (unescaped HTML) - this is possibly\\n    // an HTML injection attack - it's likely too late if this is already in\\n    // production (the code has likely already done its damage by the time\\n    // we're seeing it)... but we yell loudly about this so that hopefully it's\\n    // more likely to be caught in development before making it to production\\n    if (element.children.length > 0) {\\n      if (!options.ignoreUnescapedHTML) {\\n        console.warn(\\\"One of your code blocks includes unescaped HTML. This is a potentially serious security risk.\\\");\\n        console.warn(\\\"https://github.com/highlightjs/highlight.js/wiki/security\\\");\\n        console.warn(\\\"The element with unescaped HTML:\\\");\\n        console.warn(element);\\n      }\\n      if (options.throwUnescapedHTML) {\\n        const err = new HTMLInjectionError(\\n          \\\"One of your code blocks includes unescaped HTML.\\\",\\n          element.innerHTML\\n        );\\n        throw err;\\n      }\\n    }\\n\\n    node = element;\\n    const text = node.textContent;\\n    const result = language ? highlight(text, { language, ignoreIllegals: true }) : highlightAuto(text);\\n\\n    element.innerHTML = result.value;\\n    element.dataset.highlighted = \\\"yes\\\";\\n    updateClassName(element, language, result.language);\\n    element.result = {\\n      language: result.language,\\n      // TODO: remove with version 11.0\\n      re: result.relevance,\\n      relevance: result.relevance\\n    };\\n    if (result.secondBest) {\\n      element.secondBest = {\\n        language: result.secondBest.language,\\n        relevance: result.secondBest.relevance\\n      };\\n    }\\n\\n    fire(\\\"after:highlightElement\\\", { el: element, result, text });\\n  }\\n\\n  /**\\n   * Updates highlight.js global options with the passed options\\n   *\\n   * @param {Partial<HLJSOptions>} userOptions\\n   */\\n  function configure(userOptions) {\\n    options = inherit(options, userOptions);\\n  }\\n\\n  // TODO: remove v12, deprecated\\n  const initHighlighting = () => {\\n    highlightAll();\\n    deprecated(\\\"10.6.0\\\", \\\"initHighlighting() deprecated.  Use highlightAll() now.\\\");\\n  };\\n\\n  // TODO: remove v12, deprecated\\n  function initHighlightingOnLoad() {\\n    highlightAll();\\n    deprecated(\\\"10.6.0\\\", \\\"initHighlightingOnLoad() deprecated.  Use highlightAll() now.\\\");\\n  }\\n\\n  let wantsHighlight = false;\\n\\n  /**\\n   * auto-highlights all pre>code elements on the page\\n   */\\n  function highlightAll() {\\n    function boot() {\\n      // if a highlight was requested before DOM was loaded, do now\\n      highlightAll();\\n    }\\n\\n    // if we are called too early in the loading process\\n    if (document.readyState === \\\"loading\\\") {\\n      // make sure the event listener is only added once\\n      if (!wantsHighlight) {\\n        window.addEventListener('DOMContentLoaded', boot, false);\\n      }\\n      wantsHighlight = true;\\n      return;\\n    }\\n\\n    const blocks = document.querySelectorAll(options.cssSelector);\\n    blocks.forEach(highlightElement);\\n  }\\n\\n  /**\\n   * Register a language grammar module\\n   *\\n   * @param {string} languageName\\n   * @param {LanguageFn} languageDefinition\\n   */\\n  function registerLanguage(languageName, languageDefinition) {\\n    let lang = null;\\n    try {\\n      lang = languageDefinition(hljs);\\n    } catch (error$1) {\\n      error(\\\"Language definition for '{}' could not be registered.\\\".replace(\\\"{}\\\", languageName));\\n      // hard or soft error\\n      if (!SAFE_MODE) { throw error$1; } else { error(error$1); }\\n      // languages that have serious errors are replaced with essentially a\\n      // \\\"plaintext\\\" stand-in so that the code blocks will still get normal\\n      // css classes applied to them - and one bad language won't break the\\n      // entire highlighter\\n      lang = PLAINTEXT_LANGUAGE;\\n    }\\n    // give it a temporary name if it doesn't have one in the meta-data\\n    if (!lang.name) lang.name = languageName;\\n    languages[languageName] = lang;\\n    lang.rawDefinition = languageDefinition.bind(null, hljs);\\n\\n    if (lang.aliases) {\\n      registerAliases(lang.aliases, { languageName });\\n    }\\n  }\\n\\n  /**\\n   * Remove a language grammar module\\n   *\\n   * @param {string} languageName\\n   */\\n  function unregisterLanguage(languageName) {\\n    delete languages[languageName];\\n    for (const alias of Object.keys(aliases)) {\\n      if (aliases[alias] === languageName) {\\n        delete aliases[alias];\\n      }\\n    }\\n  }\\n\\n  /**\\n   * @returns {string[]} List of language internal names\\n   */\\n  function listLanguages() {\\n    return Object.keys(languages);\\n  }\\n\\n  /**\\n   * @param {string} name - name of the language to retrieve\\n   * @returns {Language | undefined}\\n   */\\n  function getLanguage(name) {\\n    name = (name || '').toLowerCase();\\n    return languages[name] || languages[aliases[name]];\\n  }\\n\\n  /**\\n   *\\n   * @param {string|string[]} aliasList - single alias or list of aliases\\n   * @param {{languageName: string}} opts\\n   */\\n  function registerAliases(aliasList, { languageName }) {\\n    if (typeof aliasList === 'string') {\\n      aliasList = [aliasList];\\n    }\\n    aliasList.forEach(alias => { aliases[alias.toLowerCase()] = languageName; });\\n  }\\n\\n  /**\\n   * Determines if a given language has auto-detection enabled\\n   * @param {string} name - name of the language\\n   */\\n  function autoDetection(name) {\\n    const lang = getLanguage(name);\\n    return lang && !lang.disableAutodetect;\\n  }\\n\\n  /**\\n   * Upgrades the old highlightBlock plugins to the new\\n   * highlightElement API\\n   * @param {HLJSPlugin} plugin\\n   */\\n  function upgradePluginAPI(plugin) {\\n    // TODO: remove with v12\\n    if (plugin[\\\"before:highlightBlock\\\"] && !plugin[\\\"before:highlightElement\\\"]) {\\n      plugin[\\\"before:highlightElement\\\"] = (data) => {\\n        plugin[\\\"before:highlightBlock\\\"](\\n          Object.assign({ block: data.el }, data)\\n        );\\n      };\\n    }\\n    if (plugin[\\\"after:highlightBlock\\\"] && !plugin[\\\"after:highlightElement\\\"]) {\\n      plugin[\\\"after:highlightElement\\\"] = (data) => {\\n        plugin[\\\"after:highlightBlock\\\"](\\n          Object.assign({ block: data.el }, data)\\n        );\\n      };\\n    }\\n  }\\n\\n  /**\\n   * @param {HLJSPlugin} plugin\\n   */\\n  function addPlugin(plugin) {\\n    upgradePluginAPI(plugin);\\n    plugins.push(plugin);\\n  }\\n\\n  /**\\n   * @param {HLJSPlugin} plugin\\n   */\\n  function removePlugin(plugin) {\\n    const index = plugins.indexOf(plugin);\\n    if (index !== -1) {\\n      plugins.splice(index, 1);\\n    }\\n  }\\n\\n  /**\\n   *\\n   * @param {PluginEvent} event\\n   * @param {any} args\\n   */\\n  function fire(event, args) {\\n    const cb = event;\\n    plugins.forEach(function(plugin) {\\n      if (plugin[cb]) {\\n        plugin[cb](args);\\n      }\\n    });\\n  }\\n\\n  /**\\n   * DEPRECATED\\n   * @param {HighlightedHTMLElement} el\\n   */\\n  function deprecateHighlightBlock(el) {\\n    deprecated(\\\"10.7.0\\\", \\\"highlightBlock will be removed entirely in v12.0\\\");\\n    deprecated(\\\"10.7.0\\\", \\\"Please use highlightElement now.\\\");\\n\\n    return highlightElement(el);\\n  }\\n\\n  /* Interface definition */\\n  Object.assign(hljs, {\\n    highlight,\\n    highlightAuto,\\n    highlightAll,\\n    highlightElement,\\n    // TODO: Remove with v12 API\\n    highlightBlock: deprecateHighlightBlock,\\n    configure,\\n    initHighlighting,\\n    initHighlightingOnLoad,\\n    registerLanguage,\\n    unregisterLanguage,\\n    listLanguages,\\n    getLanguage,\\n    registerAliases,\\n    autoDetection,\\n    inherit,\\n    addPlugin,\\n    removePlugin\\n  });\\n\\n  hljs.debugMode = function() { SAFE_MODE = false; };\\n  hljs.safeMode = function() { SAFE_MODE = true; };\\n  hljs.versionString = version;\\n\\n  hljs.regex = {\\n    concat: concat,\\n    lookahead: lookahead,\\n    either: either,\\n    optional: optional,\\n    anyNumberOfTimes: anyNumberOfTimes\\n  };\\n\\n  for (const key in MODES) {\\n    // @ts-ignore\\n    if (typeof MODES[key] === \\\"object\\\") {\\n      // @ts-ignore\\n      deepFreeze(MODES[key]);\\n    }\\n  }\\n\\n  // merge all the modes/regexes into our main object\\n  Object.assign(hljs, MODES);\\n\\n  return hljs;\\n};\\n\\n// Other names for the variable may break build script\\nconst highlight = HLJS({});\\n\\n// returns a new instance of the highlighter to be used for extensions\\n// check https://github.com/wooorm/lowlight/issues/47\\nhighlight.newInstance = () => HLJS({});\\n\\nmodule.exports = highlight;\\nhighlight.HighlightJS = highlight;\\nhighlight.default = highlight;\\n\\n};\"],\n\"names\":[\"shadow$provide\",\"global\",\"require\",\"module\",\"exports\",\"deepFreeze\",\"obj\",\"Map\",\"clear\",\"delete\",\"set\",\"obj.set\",\"Error\",\"Set\",\"add\",\"obj.delete\",\"Object\",\"freeze\",\"getOwnPropertyNames\",\"forEach\",\"name\",\"prop\",\"type\",\"isFrozen\",\"escapeHTML\",\"value\",\"replace\",\"inherit$1\",\"original\",\"objects\",\"result\",\"create\",\"key\",\"source\",\"re\",\"lookahead\",\"concat\",\"anyNumberOfTimes\",\"optional\",\"args\",\"map\",\"x\",\"join\",\"joined\",\"stripOptionsFromArgs\",\"opts\",\"length\",\"constructor\",\"splice\",\"either\",\"capture\",\"_rewriteBackreferences\",\"regexps\",\"joinWith\",\"numCaptures\",\"regex\",\"offset\",\"out\",\"match\",\"BACKREF_RE\",\"exec\",\"substring\",\"index\",\"String\",\"Number\",\"skipIfHasPrecedingDot\",\"response\",\"input\",\"before\",\"ignoreMatch\",\"scopeClassName\",\"mode\",\"_parent\",\"undefined\",\"className\",\"scope\",\"beginKeywords\",\"parent\",\"begin\",\"split\",\"__beforeBegin\",\"keywords\",\"relevance\",\"compileIllegal\",\"Array\",\"isArray\",\"illegal\",\"compileMatch\",\"end\",\"compileRelevance\",\"compileKeywords\",\"rawKeywords\",\"caseInsensitive\",\"scopeName\",\"DEFAULT_KEYWORD_SCOPE\",\"compileList\",\"keywordList\",\"toLowerCase\",\"keyword\",\"pair\",\"providedScore\",\"COMMON_KEYWORDS\",\"includes\",\"compiledKeywords\",\"keys\",\"assign\",\"remapScopeNames\",\"regexes\",\"scopeNames\",\"emit\",\"positions\",\"i\",\"RegExp\",\"toString\",\"_emit\",\"_multi\",\"MultiClass\",\"beginScope\",\"_wrap\",\"endScope\",\"skip\",\"excludeBegin\",\"returnBegin\",\"console\",\"error\",\"message\",\"MultiClassError\",\"excludeEnd\",\"returnEnd\",\"compileLanguage\",\"language\",\"langRe\",\"case_insensitive\",\"unicodeRegex\",\"buildModeRegex\",\"mm\",\"ResumableMultiRegex\",\"contains\",\"term\",\"addRule\",\"rule\",\"terminatorEnd\",\"compileMode\",\"isCompiled\",\"beforeMatchExt\",\"ext\",\"compilerExtensions\",\"keywordPattern\",\"$pattern\",\"keywordPatternRe\",\"beginRe\",\"endsWithParent\",\"endRe\",\"illegalRe\",\"c\",\"expandOrCloneMode\",\"starts\",\"matcher\",\"MultiRegex\",\"matchIndexes\",\"matchAt\",\"position\",\"push\",\"compile\",\"this.exec\",\"terminators\",\"el\",\"matcherRe\",\"lastIndex\",\"s\",\"findIndex\",\"matchData\",\"rules\",\"multiRegexes\",\"regexIndex\",\"count\",\"getMatcher\",\"slice\",\"resumingScanAtSamePosition\",\"considerAll\",\"m\",\"m2\",\"classNameAliases\",\"dependencyOnParent\",\"variants\",\"cachedVariants\",\"variant\",\"Response\",\"data\",\"isMatchIgnored\",\"scopeToCSSClass\",\"prefix\",\"startsWith\",\"pieces\",\"shift\",\"repeat\",\"HTMLRenderer\",\"parseTree\",\"options\",\"buffer\",\"classPrefix\",\"walk\",\"addText\",\"text\",\"openNode\",\"node\",\"span\",\"closeNode\",\"SPAN_CLOSE\",\"newNode\",\"children\",\"TokenTree\",\"rootNode\",\"stack\",\"top\",\"pop\",\"closeAllNodes\",\"toJSON\",\"JSON\",\"stringify\",\"builder\",\"_walk\",\"child\",\"_collapse\",\"every\",\"TokenTreeEmitter\",\"startScope\",\"__addSublanguage\",\"emitter\",\"root\",\"toHTML\",\"renderer\",\"finalize\",\"BACKSLASH_ESCAPE\",\"APOS_STRING_MODE\",\"QUOTE_STRING_MODE\",\"COMMENT\",\"modeOptions\",\"ENGLISH_WORD\",\"C_LINE_COMMENT_MODE\",\"C_BLOCK_COMMENT_MODE\",\"HASH_COMMENT_MODE\",\"MODES\",\"__proto__\",\"BINARY_NUMBER_MODE\",\"BINARY_NUMBER_RE\",\"C_NUMBER_MODE\",\"C_NUMBER_RE\",\"END_SAME_AS_BEGIN\",\"resp\",\"on:begin\",\"_beginMatch\",\"on:end\",\"IDENT_RE\",\"MATCH_NOTHING_RE\",\"METHOD_GUARD\",\"NUMBER_MODE\",\"NUMBER_RE\",\"PHRASAL_WORDS_MODE\",\"REGEXP_MODE\",\"RE_STARTERS_RE\",\"SHEBANG\",\"beginShebang\",\"binary\",\"TITLE_MODE\",\"UNDERSCORE_IDENT_RE\",\"UNDERSCORE_TITLE_MODE\",\"beforeMatch\",\"originalMode\",\"endsParent\",\"seenDeprecations\",\"warn\",\"log\",\"deprecated\",\"version\",\"HTMLInjectionError\",\"reason\",\"html\",\"NO_MATCH\",\"Symbol\",\"HLJS\",\"hljs\",\"blockLanguage\",\"block\",\"classes\",\"parentNode\",\"languageDetectRe\",\"getLanguage\",\"LANGUAGE_NOT_FOUND\",\"find\",\"_class\",\"noHighlightRe\",\"test\",\"highlight\",\"codeOrLanguageName\",\"optionsOrCode\",\"ignoreIllegals\",\"languageName\",\"code\",\"context\",\"fire\",\"_highlight\",\"codeToHighlight\",\"continuation\",\"processKeywords\",\"modeBuffer\",\"buf\",\"word\",\"kind\",\"keywordRelevance\",\"keywordHits\",\"MAX_KEYWORD_HITS\",\"emitKeyword\",\"processBuffer\",\"subLanguage\",\"languages\",\"continuations\",\"_top\",\"highlightAuto\",\"_emitter\",\"emitMultiClass\",\"max\",\"klass\",\"startNewMode\",\"endOfMode\",\"matchPlusRemainder\",\"matched\",\"processLexeme\",\"textBeforeMatch\",\"lexeme\",\"lastMatch\",\"SAFE_MODE\",\"err\",\"badRule\",\"newMode\",\"beforeCallbacks\",\"cb\",\"resumeScanAtSamePosition\",\"endMode\",\"origin\",\"processed\",\"iterations\",\"md\",\"__emitter\",\"processContinuations\",\"list\",\"current\",\"unshift\",\"item\",\"__emitTokens\",\"processedCount\",\"_illegalBy\",\"resultSoFar\",\"errorRaised\",\"justTextHighlightResult\",\"PLAINTEXT_LANGUAGE\",\"languageSubset\",\"plaintext\",\"results\",\"filter\",\"autoDetection\",\"sorted\",\"sort\",\"a\",\"b\",\"supersetOf\",\"best\",\"secondBest\",\"highlightElement\",\"element\",\"highlighted\",\"dataset\",\"ignoreUnescapedHTML\",\"throwUnescapedHTML\",\"innerHTML\",\"textContent\",\"aliases\",\"classList\",\"highlightAll\",\"boot\",\"document\",\"readyState\",\"wantsHighlight\",\"window\",\"addEventListener\",\"querySelectorAll\",\"blocks\",\"cssSelector\",\"registerAliases\",\"aliasList\",\"alias\",\"lang\",\"disableAutodetect\",\"upgradePluginAPI\",\"plugin\",\"event\",\"plugins\",\"highlightBlock\",\"deprecateHighlightBlock\",\"configure\",\"userOptions\",\"initHighlighting\",\"initHighlightingOnLoad\",\"registerLanguage\",\"languageDefinition\",\"error$1\",\"rawDefinition\",\"bind\",\"unregisterLanguage\",\"listLanguages\",\"inherit\",\"addPlugin\",\"removePlugin\",\"indexOf\",\"debugMode\",\"hljs.debugMode\",\"safeMode\",\"hljs.safeMode\",\"versionString\",\"newInstance\",\"highlight.newInstance\",\"HighlightJS\",\"default\"]\n}\n"]